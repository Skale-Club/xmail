---
phase: 04-code-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/outreach/inboxes/NewInboxPage.tsx
  - src/pages/outreach/SequencesPage.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "npx tsc --noEmit produces zero TS6133 errors in any outreach file"
    - "NewInboxPage.tsx imports apiFetch from lib/api-client, not lib/api"
    - "SequencesPage.tsx does not import Link from wouter"
    - "processOutreachSequences.ts has no sendEmail function (pre-satisfied by Phase 1)"
  artifacts:
    - path: "src/pages/outreach/inboxes/NewInboxPage.tsx"
      provides: "Updated import — lib/api-client"
      contains: "from '../../../lib/api-client'"
    - path: "src/pages/outreach/SequencesPage.tsx"
      provides: "Cleaned import — Link removed"
      contains: "import { useLocation } from 'wouter'"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements status updated"
      contains: "[x] **QUAL-01**"
  key_links:
    - from: "src/pages/outreach/inboxes/NewInboxPage.tsx"
      to: "src/lib/api-client.ts"
      via: "named import apiFetch"
      pattern: "from '../../../lib/api-client'"
---

<objective>
Fix the three import-hygiene requirements for Phase 4: swap NewInboxPage.tsx from the legacy lib/api module to lib/api-client, remove the unused Link import from SequencesPage.tsx, and verify that the sendEmail private function in processOutreachSequences.ts was already removed by Phase 1 (QUAL-03 pre-satisfied). Update REQUIREMENTS.md to reflect final status.

Purpose: Eliminate the last TypeScript TS6133 error in outreach files and align the last outreach page still using the legacy api module with the rest of the codebase.
Output: NewInboxPage.tsx on lib/api-client, SequencesPage.tsx without unused Link import, REQUIREMENTS.md with QUAL-01/QUAL-02/QUAL-03 marked complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap NewInboxPage.tsx import from lib/api to lib/api-client</name>
  <files>src/pages/outreach/inboxes/NewInboxPage.tsx</files>
  <read_first>
    - src/pages/outreach/inboxes/NewInboxPage.tsx (see current import on line 14 — must not assume; read before editing)
    - src/lib/api-client.ts (confirm apiFetch is exported; verify no signature difference that would require call-site changes)
  </read_first>
  <action>
    In src/pages/outreach/inboxes/NewInboxPage.tsx, change line 14 from:

        import { apiFetch } from '../../../lib/api'

    to:

        import { apiFetch } from '../../../lib/api-client'

    That is the only change. Do NOT alter any apiFetch call sites — the function signatures are compatible (both accept (path: string, options?) and return Promise). Do NOT import apiRequest; NewInboxPage.tsx does not use it.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skaleclub-mail && npx tsc --noEmit 2>&1 | grep "NewInboxPage" || echo "NO ERRORS IN NewInboxPage"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/outreach/inboxes/NewInboxPage.tsx line 14 contains: from '../../../lib/api-client'
    - src/pages/outreach/inboxes/NewInboxPage.tsx does NOT contain: from '../../../lib/api'
    - npx tsc --noEmit produces no errors referencing NewInboxPage.tsx
  </acceptance_criteria>
  <done>NewInboxPage.tsx imports apiFetch from lib/api-client; no TypeScript errors in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused Link import from SequencesPage.tsx and verify QUAL-03</name>
  <files>src/pages/outreach/SequencesPage.tsx, src/server/jobs/processOutreachSequences.ts, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/pages/outreach/SequencesPage.tsx (read lines 1-10 to see current wouter import line; confirm Link is present and useLocation is present)
    - src/server/jobs/processOutreachSequences.ts (scan for any function named sendEmail to confirm QUAL-03 is pre-satisfied)
    - .planning/REQUIREMENTS.md (read to find current checkbox state for QUAL-01, QUAL-02, QUAL-03)
  </read_first>
  <action>
    **Step A — Fix SequencesPage.tsx (QUAL-02):**

    In src/pages/outreach/SequencesPage.tsx, change the wouter import line (currently line 3) from:

        import { Link, useLocation } from 'wouter'

    to:

        import { useLocation } from 'wouter'

    Do NOT remove useLocation — it is actively used in the file for navigation. Do NOT touch any other imports or lines.

    **Step B — Verify QUAL-03 (no sendEmail in processOutreachSequences.ts):**

    Read src/server/jobs/processOutreachSequences.ts and confirm there is no function named `sendEmail` (private or exported). Per research, Phase 1 already removed it. No edits needed to that file. This is a read-and-confirm step only.

    **Step C — Update REQUIREMENTS.md:**

    In .planning/REQUIREMENTS.md, update the three QUAL checkboxes:
    - Change `- [ ] **QUAL-01**` to `- [x] **QUAL-01**`
    - Change `- [ ] **QUAL-02**` to `- [x] **QUAL-02**`
    - Change `- [ ] **QUAL-03**` to `- [x] **QUAL-03**`

    Also update the Traceability table at the bottom:
    - Change `| QUAL-01 | Phase 4 | Pending |` to `| QUAL-01 | Phase 4 | Complete |`
    - Change `| QUAL-02 | Phase 4 | Pending |` to `| QUAL-02 | Phase 4 | Complete |`
    - Change `| QUAL-03 | Phase 4 | Pending |` to `| QUAL-03 | Phase 4 | Complete |`
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skaleclub-mail && npx tsc --noEmit 2>&1 | grep -E "outreach.*TS6133|SequencesPage.*TS6133" || echo "NO TS6133 IN OUTREACH FILES"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/outreach/SequencesPage.tsx line 3 contains exactly: import { useLocation } from 'wouter'
    - src/pages/outreach/SequencesPage.tsx does NOT contain: import { Link
    - src/server/jobs/processOutreachSequences.ts does NOT contain any line matching: function sendEmail
    - .planning/REQUIREMENTS.md contains: [x] **QUAL-01**
    - .planning/REQUIREMENTS.md contains: [x] **QUAL-02**
    - .planning/REQUIREMENTS.md contains: [x] **QUAL-03**
    - npx tsc --noEmit output has zero lines referencing any file under src/pages/outreach/ or src/server/jobs/processOutreachSequences.ts with error code TS6133
  </acceptance_criteria>
  <done>SequencesPage.tsx has no unused Link import; processOutreachSequences.ts confirmed to have no sendEmail function; QUAL-01/02/03 marked complete in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
Run the following after both tasks complete:

```bash
cd /c/Users/Vanildo/Dev/skaleclub-mail
npx tsc --noEmit 2>&1 | grep -E "src/pages/outreach|src/components/outreach|src/server/jobs/processOutreach"
```

Expected output: no lines (zero errors in outreach files).

Also confirm:
- grep "from '../../../lib/api'" src/pages/outreach/inboxes/NewInboxPage.tsx → no output
- grep "from '../../../lib/api-client'" src/pages/outreach/inboxes/NewInboxPage.tsx → one match on line 14
- grep "Link" src/pages/outreach/SequencesPage.tsx | head -5 → no import line (Link may appear in JSX elsewhere, but the import line must be gone)
</verification>

<success_criteria>
1. npx tsc --noEmit produces zero TS6133 errors in any outreach file path
2. NewInboxPage.tsx contains from '../../../lib/api-client' and does not contain from '../../../lib/api'
3. SequencesPage.tsx wouter import is: import { useLocation } from 'wouter'
4. processOutreachSequences.ts has no function named sendEmail
5. REQUIREMENTS.md has [x] for QUAL-01, QUAL-02, QUAL-03
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality/04-01-SUMMARY.md` using the summary template.
</output>
