---
phase: 13-medium-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/archive/013_add_performance_indexes.sql
  - package.json
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [QUA-02]
must_haves:
  truths:
    - "`supabase/migrations/013_add_performance_indexes.sql` no longer exists at that path (moved to `supabase/migrations/archive/013_add_performance_indexes.sql`)."
    - "`package.json` `db:generate` and `db:push` scripts either use current drizzle-kit 0.21+ syntax (`generate` / `push --dialect=postgresql`) or are removed; no stale `generate:pg`/`push:pg` remains."
    - "`CLAUDE.md` documents the schema-edit workflow: schema lives in `src/db/schema.ts` + hand-written `supabase/migrations/NNN_*.sql`, `db:generate` is NOT used for new migrations, `db:indexes` for index changes."
    - "A `README.md` Schema/Migrations section (new or updated) mirrors the same workflow doc."
  artifacts:
    - path: "supabase/migrations/archive/013_add_performance_indexes.sql"
      provides: "Archived deprecated migration (preserved for git history, removed from active dir)"
    - path: "package.json"
      provides: "Drizzle scripts using current syntax (or removed if deprecated)"
    - path: "CLAUDE.md"
      provides: "## Schema & Migrations section documenting hand-written SQL workflow"
    - path: "README.md"
      provides: "Schema/Migrations section mirroring CLAUDE.md guidance"
  key_links:
    - from: "CLAUDE.md ## Schema & Migrations"
      to: "supabase/migrations/*.sql"
      via: "explicit canonical-source statement"
      pattern: "schema lives in src/db/schema.ts \\+ supabase/migrations"
    - from: "package.json scripts"
      to: "drizzle-kit 0.20.17 → 0.21+ syntax"
      via: "script command flags"
      pattern: "drizzle-kit (generate|push)( --dialect)?"
---

<objective>
QUA-02 — Eliminate three sources of confusion about the schema/migration workflow: (1) the deprecated `013_add_performance_indexes.sql` migration still sits in the active dir; (2) `package.json` advertises `db:generate`/`db:push` scripts that nobody on the team should run (and that use deprecated drizzle-kit syntax); (3) no documentation explains that schema is hand-rolled SQL, not Drizzle-generated.

Purpose: Future contributors / future Claude executors will repeatedly ask "should I run `npm run db:generate` after editing schema.ts?" until this is unambiguously documented. This plan answers it once and removes the trap (`db:generate` would produce a destructive diff against a long-drifted DB — see audit M3).

Output: One archived migration, two updated config scripts, two updated doc files. Closes audit M2, M3, M4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-medium-consolidation/13-CONTEXT.md
@.planning/debug/system-wide-audit-2026-05-16.md
@CLAUDE.md
@README.md
@package.json
@supabase/migrations/013_add_performance_indexes.sql

Audit context — three findings consolidated:
- M2: migration 013 is marked DEPRECATED in-file but still present in the active dir.
- M3: only one Drizzle migration exists (`drizzle/0000_dear_wolverine.sql`) — schema has drifted massively; `db:generate` would produce a destructive diff.
- M4: `package.json` scripts use drizzle-kit 0.20.17 deprecated syntax (`generate:pg`, `push:pg`).

Carry-forward decision (from STATE.md `Decisions`):
> Defer Drizzle migration regen — Schema drift too large; document `supabase/migrations/*.sql` as canonical (v1.2 Phase 13 QUA-02).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive migration 013 and update package.json scripts</name>
  <files>supabase/migrations/archive/013_add_performance_indexes.sql, package.json</files>
  <action>
    Step 1 — Archive migration 013:
      - Create directory `supabase/migrations/archive/` if it does not exist.
      - Move `supabase/migrations/013_add_performance_indexes.sql` → `supabase/migrations/archive/013_add_performance_indexes.sql`. Use `git mv` to preserve history (POSIX) or `Move-Item` then `git add -A` (Windows PowerShell). Either way the final `git status` should show one rename, not delete+add.
      - At the top of the archived file, prepend a comment block:
        ```sql
        -- ARCHIVED 2026-05-16 (Phase 13 QUA-02 / audit M2).
        -- This migration was marked DEPRECATED in-file but kept in the active migrations dir, causing confusion.
        -- Indexes are now defined in `src/db/schema.ts` (Drizzle `index()`) and applied via `sql/indexes.sql` (CONCURRENTLY).
        -- Run `npm run db:indexes` to apply. DO NOT run this archived file against a live DB.
        ```

    Step 2 — Update package.json scripts:
      - Current state (lines 17-18):
        ```json
        "db:generate": "drizzle-kit generate:pg",
        "db:push": "drizzle-kit push:pg",
        ```
      - Decision (per STATE.md): we are NOT using Drizzle-generated migrations for new schema changes — `db:generate` is a foot-gun against the drifted DB. Two acceptable outcomes:
        (a) REMOVE both scripts entirely from package.json. This is the most aggressive and safest option — the script literally cannot be run by mistake.
        (b) KEEP the scripts but neuter them to current drizzle-kit syntax (`generate`, `push --dialect=postgresql`) AND prepend a `echo` warning.

      - **Choose option (a) — remove**. Rationale: documentation alone (Task 2/3) does not prevent accidental `npm run db:generate`; removing the script does. If a future contributor decides to revive Drizzle-generated migrations, they re-add the script with intent.
      - Remove the `"db:generate"` and `"db:push"` lines from `package.json`'s `scripts` block. KEEP `"db:studio"` (read-only studio is safe), `"db:indexes"`, `"db:rls"`, `"db:audit"` — these are still useful.
      - After the edit, `cat package.json` and confirm valid JSON (no trailing comma left dangling, no broken structure).
  </action>
  <verify>
    <automated>test ! -f supabase/migrations/013_add_performance_indexes.sql &amp;&amp; test -f supabase/migrations/archive/013_add_performance_indexes.sql &amp;&amp; node -e "const p=require('./package.json');if(p.scripts['db:generate']||p.scripts['db:push'])process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>
    - `supabase/migrations/013_add_performance_indexes.sql` does not exist.
    - `supabase/migrations/archive/013_add_performance_indexes.sql` exists, has the ARCHIVED comment block prepended.
    - `package.json` parses as valid JSON.
    - `package.json` scripts object has NO `db:generate` and NO `db:push` keys.
    - `db:studio`, `db:indexes`, `db:rls`, `db:audit` scripts retained unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the schema/migration workflow in CLAUDE.md and README.md</name>
  <files>CLAUDE.md, README.md</files>
  <action>
    Step 1 — CLAUDE.md update:
      - Locate the `## Database` section near the bottom of `CLAUDE.md` (around line referencing `src/db/schema.ts` and migration 001).
      - Append (or replace if a stub is present) a new `### Schema & Migration Workflow` subsection with this exact content:

        ```markdown
        ### Schema & Migration Workflow

        **Canonical sources:**
        - **TypeScript schema:** `src/db/schema.ts` — Drizzle table definitions + types (consumed by application code).
        - **SQL migrations:** `supabase/migrations/NNN_description.sql` — hand-written, hand-numbered, applied in order against the Postgres DB. THIS is the source of truth for the running database.
        - **Indexes:** Defined twice intentionally — in `src/db/schema.ts` via Drizzle `index()` (for type-awareness) AND in `sql/indexes.sql` via `CREATE INDEX CONCURRENTLY IF NOT EXISTS` (for safe production apply). Run `npm run db:indexes` to apply index changes.

        **What we DO:**
        - Edit `src/db/schema.ts` to update TypeScript types.
        - Write a matching hand-rolled SQL migration in `supabase/migrations/NNN_<name>.sql` (take the next free integer). Migrations must be idempotent where reasonable (`IF NOT EXISTS`, `DROP POLICY IF EXISTS ... CREATE POLICY ...`).
        - Apply via `psql "$DATABASE_URL" -f supabase/migrations/NNN_<name>.sql`.
        - For RLS-policy changes, prefer adding to / regenerating the consolidated RLS migration (currently `020_consolidate_rls.sql` — see QUA-03).

        **What we DO NOT do:**
        - **Do NOT run `drizzle-kit generate` to produce migrations.** The Drizzle-generated diff would conflict with the hand-rolled SQL we've accumulated since `drizzle/0000_dear_wolverine.sql`. The `db:generate`/`db:push` scripts have been removed from `package.json` (Phase 13 QUA-02 / audit M3) to prevent accidental destruction. `db:studio` (read-only Drizzle Studio) and `db:indexes` remain available.
        - **Do NOT add Drizzle relations / constraints expecting them to apply automatically.** The TS-side schema is for type information; the DB side comes from the SQL migration.

        **Numbering convention:** Migrations are sequential integers (`001` through `019` as of 2026-05-16; `020_consolidate_rls.sql` is the next planned). When two phases plan migrations in parallel, the second to land takes the next number and rewrites its planning docs accordingly.
        ```

    Step 2 — README.md update:
      - Open `README.md`. If a `## Database` or `## Migrations` section exists, append the `### Schema & Migration Workflow` subsection above (verbatim).
      - If no such section exists, create one near the project structure / commands area. Use the same heading text and same content as in CLAUDE.md.
      - Goal: A new contributor reading either CLAUDE.md OR README.md learns the same rules — no contradictions.

    Step 3 — Verify alignment:
      - `grep -n "db:generate" CLAUDE.md README.md package.json` — the only matches should be in CLAUDE.md/README.md inside the "Do NOT" subsection (warning prose). Zero matches in `package.json`.
  </action>
  <verify>
    <automated>grep -q "Schema &amp; Migration Workflow" CLAUDE.md &amp;&amp; grep -q "Schema &amp; Migration Workflow" README.md &amp;&amp; ! grep -q '"db:generate"' package.json &amp;&amp; ! grep -q '"db:push"' package.json &amp;&amp; echo "DOCS_OK"</automated>
  </verify>
  <done>
    - `CLAUDE.md` contains `### Schema & Migration Workflow` subsection with the exact wording above.
    - `README.md` contains the same subsection.
    - Both docs explicitly state `db:generate` is NOT to be used and explain why.
    - `package.json` does NOT have `db:generate` or `db:push` script keys.
    - The audit M2/M3/M4 trifecta is closed.
  </done>
</task>

</tasks>

<verification>
1. Archived file present: `test -f supabase/migrations/archive/013_add_performance_indexes.sql`.
2. Original removed: `test ! -f supabase/migrations/013_add_performance_indexes.sql`.
3. Package scripts clean: `node -e "const p=require('./package.json');if(p.scripts['db:generate']||p.scripts['db:push'])process.exit(1)"`.
4. Docs updated: `grep -q "Schema & Migration Workflow" CLAUDE.md && grep -q "Schema & Migration Workflow" README.md`.
5. Git status shows: 1 rename, 1 modified package.json, 1 modified CLAUDE.md, 1 modified README.md, 1 modified archive file (or new if archive dir didn't exist).
</verification>

<success_criteria>
ROADMAP success criterion #2 for Phase 13:
> `supabase/migrations/013_add_performance_indexes.sql` is gone from the active migrations dir (archived under `supabase/migrations/archive/`); README / CLAUDE.md documents the schema-edit workflow.
</success_criteria>

<dependencies>
- **Depends on:** Nothing. Wave 1, fully parallelizable with 13-03, 13-04, 13-05, 13-06.
- **Blocks:** Nothing functionally. CLAUDE.md change is informational only.
</dependencies>

<risks>
- **Risk:** Removing `db:generate` breaks somebody's local workflow muscle memory. Mitigation: the explicit doc explains why and offers `db:studio` (read-only).
- **Risk:** README.md may not exist or may be wildly different from CLAUDE.md. Mitigation: Task 2 handles both "exists" and "no DB section" cases.
- **Risk:** Archive directory might already exist with other files. Mitigation: `mkdir -p` semantics ensure idempotence; we add ONE file.
</risks>

<output>
After completion, create `.planning/phases/13-medium-consolidation/13-02-SUMMARY.md` documenting:
- Confirmed git rename of `013_add_performance_indexes.sql` into `archive/`.
- Diff of `package.json` (removed lines).
- New `### Schema & Migration Workflow` subsection added to both CLAUDE.md and README.md (link the section anchors).
- Closes audit findings: M2, M3, M4.
</output>
