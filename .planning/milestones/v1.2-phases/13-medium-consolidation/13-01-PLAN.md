---
phase: 13-medium-consolidation
plan: 01
type: execute
wave: 2
depends_on: [13-06]
files_modified:
  - src/components/AppLogo.tsx
  - src/server/lib/tracking.ts
autonomous: true
requirements: [QUA-01]
must_haves:
  truths:
    - "Running `npx tsc --noEmit -p tsconfig.json` exits with code 0 and zero errors."
    - "Running `npx tsc --noEmit -p tsconfig.server.json` exits with code 0 and zero errors."
    - "Both type-check passes happen AFTER the 13-06 schema field rename, so any straggler callers using the old `owner_id` / `outreach_enabled` field names surface here and are fixed."
  artifacts:
    - path: "src/components/AppLogo.tsx"
      provides: "AppLogo component without unused `isSuccess` destructure (audit M1 fixed)"
    - path: "src/server/lib/tracking.ts"
      provides: "fireWebhooks with `event` typed against `webhookEventEnum.enumValues` instead of `as any` (audit M12 fixed)"
  key_links:
    - from: "tsconfig.json"
      to: "src/components/**/*.tsx"
      via: "tsc strict pass"
      pattern: "no TS6133 / TS2345 errors"
    - from: "tsconfig.server.json"
      to: "src/server/**/*.ts"
      via: "tsc strict pass"
      pattern: "no TS6133 / TS2345 errors"
---

<objective>
QUA-01 — Make `npx tsc --noEmit` pass with zero errors against BOTH `tsconfig.json` (client + shared) and `tsconfig.server.json` (server-only).

Purpose: Once this lands, Phase 14 CI-02 can wire `npx tsc --noEmit` as a required CI gate. Without this, the type-check gate is permanently red and not enforceable.

Output: A clean type-check pass on both configs, with the two known specific errors (M1 + M12) fixed AND any straggler errors surfaced by the 13-06 schema rename also fixed.

Why this plan runs in Wave 2 (after 13-06): The schema field rename `owner_id` → `ownerId` and `outreach_enabled` → `outreachEnabled` in 13-06 touches multiple callers (`src/server/routes/organizations.ts`, `src/server/routes/system.ts`, `src/pages/admin/OrganizationsPage.tsx`, `src/pages/admin/OrganizationDetailPage.tsx`, schema relations). Running tsc AFTER 13-06 is the cheapest way to catch any missed call site — let the compiler enumerate the misses instead of grepping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-medium-consolidation/13-CONTEXT.md
@.planning/phases/13-medium-consolidation/13-06-SUMMARY.md
@.planning/debug/system-wide-audit-2026-05-16.md
@src/components/AppLogo.tsx
@src/server/lib/tracking.ts
@src/db/schema.ts

<interfaces>
Key types relevant to the two known fixes:

From src/db/schema.ts (webhook event enum used in tracking.ts):
```typescript
export const webhookEventEnum = pgEnum('webhook_event', [
  'message_sent', 'message_delivered', 'message_bounced', 'message_held',
  'message_opened', 'link_clicked', 'domain_verified', 'spam_alert', 'test'
])
// Type for a single event value:
type WebhookEvent = typeof webhookEventEnum.enumValues[number]
```

From src/components/AppLogo.tsx (line 12 — known M1 error):
The `useBranding()` hook returns `{ branding, isSuccess, ... }`. Current code destructures `isSuccess` but never uses it → TS6133. Fix is to drop `isSuccess` from the destructure.

From src/server/lib/tracking.ts (line ~266 / ~292 — known M12 error):
Two `event: event as any` casts inside `db.insert(webhookRequests).values(...)`. The `event` parameter of `fireWebhooks(event, ...)` should already be typed as `WebhookEvent` (string literal union from the enum). If the type flows correctly, the `as any` cast is removable. If `webhookRequests.event` column type still complains, use `event as typeof webhookEventEnum.enumValues[number]` (proper narrowing) NOT `as any`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the two known errors (AppLogo.tsx M1 + tracking.ts M12) and run baseline tsc</name>
  <files>src/components/AppLogo.tsx, src/server/lib/tracking.ts</files>
  <action>
    Step 1 — Fix AppLogo.tsx (audit M1, TS6133 "isSuccess declared but never read"):
      - Open `src/components/AppLogo.tsx`. Line 12 destructures `const { branding } = useBranding()` currently — re-read to confirm the exact current shape. If `isSuccess` is in the destructure, REMOVE it. If `isSuccess` is no longer there (already fixed in some prior edit), no change needed; move on.

    Step 2 — Fix tracking.ts (audit M12, `event as any`):
      - Open `src/server/lib/tracking.ts`. Search for `event as any` — there are two occurrences around lines 277 and 292 inside `db.insert(webhookRequests).values({ ..., event: event as any, ... })`.
      - The `fireWebhooks` function signature already accepts a typed event parameter. Confirm the parameter is typed (it should be `string` or already a union). If the parameter type is `string`, narrow it to the enum union: change the signature to `event: typeof webhookEventEnum.enumValues[number]` (import `webhookEventEnum` from `../../db/schema` if not already imported).
      - Remove BOTH `as any` casts. Replace with bare `event,` (when key === value) or `event` (shorthand). Drizzle's typed insert builder will accept the narrowed union.
      - If the column-type complaint remains, the correct cast is `event as typeof webhookEventEnum.enumValues[number]`, NOT `as any`. Do not silence with `any` under any circumstance.

    Step 3 — Run baseline:
      - `npx tsc --noEmit -p tsconfig.json` — capture every error line.
      - `npx tsc --noEmit -p tsconfig.server.json` — capture every error line.
      - If the only errors are the two above and they're now gone → 0 errors expected.
      - If OTHER errors appear (likely from the 13-06 rename callers, e.g. `organizations.owner_id` references that were missed) → leave them for Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "(AppLogo|tracking\.ts.*event as any)" ; echo "exit=$?"</automated>
  </verify>
  <done>
    - `AppLogo.tsx` no longer destructures `isSuccess` (or `isSuccess` is consumed somewhere — either is fine).
    - `tracking.ts` has zero occurrences of `as any` for the `event` field; the type narrows through the typed `fireWebhooks` parameter.
    - Output of `npx tsc --noEmit -p tsconfig.json` no longer mentions `AppLogo.tsx:12` TS6133.
    - Output of `npx tsc --noEmit -p tsconfig.server.json` no longer mentions `tracking.ts` `event as any`.
    - Any REMAINING errors are captured for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hunt and fix remaining tsc errors until both configs exit 0</name>
  <files>(determined by tsc output — likely src/server/routes/organizations.ts, src/server/routes/system.ts, src/pages/admin/OrganizationsPage.tsx, src/pages/admin/OrganizationDetailPage.tsx, src/db/schema.ts, possibly others)</files>
  <action>
    Background: Plan 13-06 renamed `organizations.owner_id` → `ownerId` and `organizations.outreach_enabled` → `outreachEnabled` (TS property names; SQL columns stay snake_case). 13-06's own grep+update should catch most callers — this task catches the rest via the compiler.

    Iterative process:
      1. Run `npx tsc --noEmit -p tsconfig.json`. Read every error. Group by file.
      2. Run `npx tsc --noEmit -p tsconfig.server.json`. Read every error. Group by file.
      3. For each error, fix at the source. DO NOT use `as any`, `// @ts-ignore`, `// @ts-expect-error`, or `// @ts-nocheck` to silence. Only valid fixes:
         - Update an old property name (`owner_id` → `ownerId`, `outreach_enabled` → `outreachEnabled`) at the call site.
         - Remove a genuinely-unused variable.
         - Add a missing import.
         - Narrow a type with a proper cast (e.g., `value as KnownLiteral`) — never `as any`.
      4. Re-run both tsc commands. Repeat until BOTH exit 0.

    Likely hot spots (based on existing grep):
      - `src/server/routes/organizations.ts` — lines 144, 226, 371, 426 use `owner_id` field on org rows.
      - `src/server/routes/system.ts` — lines 411, 412, 416, 453, 455, 462 use `outreach_enabled`.
      - `src/pages/admin/OrganizationsPage.tsx` — line 14 type uses `owner_id`. This is a UI-side type defining what comes back from the API. After 13-06 the API still serializes `ownerId` (Drizzle row → JSON serializes the TS field name). The UI type needs to match.
      - `src/pages/admin/OrganizationDetailPage.tsx` — lines 39, 110 use `owner_id`.
      - `src/db/schema.ts` — line 391 `fields: [organizations.owner_id]` inside `organizationsRelations` — 13-06 already updates this; verify.

    For the UI files: the API response shape changes when the Drizzle TS field name changes. Update the local TypeScript interfaces in `OrganizationsPage.tsx` and `OrganizationDetailPage.tsx` to use `ownerId: string` instead of `owner_id: string`. Update the JSX consumer (`org.owner_id` → `org.ownerId`).

    Special case — `src/server/routes/system.ts:411-416` returns `{ outreach_enabled: ... }` as the API response shape. This is the WIRE format consumed by frontend. Keep the wire format `outreach_enabled` (snake_case is fine in JSON), but read the column via `organizations.outreachEnabled` in the select. Example:
      ```typescript
      const allOrgs = await db.select({ enabled: organizations.outreachEnabled }).from(organizations)
      const enabledOrgs = allOrgs.filter(o =&gt; o.enabled).length
      res.json({ outreach_enabled: enabledOrgs === totalOrgs, ... })
      ```

    If, during the sweep, tsc surfaces errors that have nothing to do with 13-06 (stale code from earlier phases): fix them too. The goal is zero errors, period. Document any non-obvious fix with a brief inline comment referencing `QUA-01`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; npx tsc --noEmit -p tsconfig.server.json &amp;&amp; echo "TSC_CLEAN"</automated>
  </verify>
  <done>
    - `npx tsc --noEmit -p tsconfig.json` exits 0 with no output (or only success/cleanup lines).
    - `npx tsc --noEmit -p tsconfig.server.json` exits 0 with no output.
    - Zero new `as any`, `@ts-ignore`, `@ts-expect-error`, or `@ts-nocheck` annotations introduced.
    - All Drizzle field renames from 13-06 propagated to call sites.
    - UI type interfaces updated to match new Drizzle field names.
  </done>
</task>

</tasks>

<verification>
Phase-level check after both tasks:
1. `npx tsc --noEmit -p tsconfig.json` → exit 0, no errors.
2. `npx tsc --noEmit -p tsconfig.server.json` → exit 0, no errors.
3. `git grep -n "as any" src/server/lib/tracking.ts` → no matches on the `event` field.
4. `git grep -n "owner_id\|outreach_enabled" src/ ':!*.sql' ':!src/db/schema.ts'` → matches ONLY appear (a) inside the Drizzle column-name string (e.g. `text('owner_id')`) or (b) inside JSON wire format keys (string literals in `res.json({ outreach_enabled: ... })`). Zero TypeScript property accesses remain.
5. `npm run lint` still passes (Phase 12 COR-07 gate stays green).
</verification>

<success_criteria>
ROADMAP success criterion #1 for Phase 13:
> `npx tsc --noEmit` returns 0 errors against both tsconfig files.

This is achieved when both tsc commands exit 0.
</success_criteria>

<dependencies>
- **Depends on:** 13-06 (schema field rename). 13-06 lands first so the rename's caller-misses surface here as tsc errors and get swept in Task 2.
- **Blocks:** Phase 14 CI-02 (CI tsc gate) — without this plan, CI gate would fail on day 1.
</dependencies>

<risks>
- **Risk:** 13-06 might miss a rename in a hard-to-grep place (e.g., string-interpolated property access, or a generic-bound type). Mitigation: Task 2 is explicitly an iterative tsc-driven hunt; the compiler is exhaustive.
- **Risk:** A surfaced error might require a non-trivial architectural fix (unlikely but possible). Mitigation: scope of QUA-01 is type-check cleanliness only; if an error reveals a deeper bug, fix the type and file a follow-up todo in STATE.md — don't expand this plan's scope.
- **Risk:** `as any` is tempting for one stubborn cast. Mitigation: explicit ban in Task 2. Use a narrow literal cast or fix the upstream type instead.
</risks>

<output>
After completion, create `.planning/phases/13-medium-consolidation/13-01-SUMMARY.md` documenting:
- Files modified (full list from `git diff --name-only` against pre-plan HEAD).
- The two known fixes (M1, M12) — short description.
- The 13-06-rename-fallout fixes (per-file: which TS property name was updated).
- Final `npx tsc --noEmit` output for both configs (should be empty / exit 0).
- Confirmation that `npm run lint` still passes.
</output>
