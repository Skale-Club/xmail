---
phase: 06-index-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - sql/indexes.sql
autonomous: true
requirements:
  - IDX-01
  - IDX-06

must_haves:
  truths:
    - "Every FK column across core tables has an index in schema.ts"
    - "sql/indexes.sql contains CREATE INDEX CONCURRENTLY for all FK indexes"
    - "Drizzle index() API is used — single source of truth in schema.ts"
    - "db:push generates migrations reflecting the new indexes"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Index definitions via Drizzle index() API"
      contains: "index('idx_"
    - path: "sql/indexes.sql"
      provides: "CONCURRENTLY index creation statements"
      contains: "CREATE INDEX CONCURRENTLY"
  key_links:
    - from: "src/db/schema.ts"
      to: "sql/indexes.sql"
      via: "manual sync — schema.ts is source of truth, indexes.sql is deployment script"
      pattern: "CREATE INDEX CONCURRENTLY"
---

# Phase 06 Plan 01: Foreign Key Indexes (IDX-01, IDX-06)

**Purpose:** Index all FK columns so no query performs a full sequential scan on join columns.

**Output:** Updated schema.ts with Drizzle `index()` definitions, populated sql/indexes.sql with CONCURRENTLY statements.

## Context

@src/db/schema.ts
@sql/indexes.sql

### Current State
- schema.ts has ~25 tables with FK columns but NO index definitions for FK lookups
- Only unique composite indexes exist (org+email, campaign+lead, etc.)
- sql/indexes.sql has a single example index on messages(organization_id)
- `index` import is NOT in the pg-core imports (line 1-12)

### FK Columns to Index (per IDX-01)

**Core tables — organizationId:**
- domains, credentials, outlook_mailboxes, routes, smtp_endpoints, http_endpoints, address_endpoints, messages, webhooks, templates, track_domains, suppressions, statistics, email_accounts, lead_lists, leads, campaigns

**Other FK columns:**
- deliveries.messageId → messages.id
- deliveries.organizationId → organizations.id
- webhook_requests.webhookId → webhooks.id
- email_accounts.outlookMailboxId → outlook_mailboxes.id
- leads.leadListId → lead_lists.id
- sequences.campaignId → campaigns.id
- sequence_steps.sequenceId → sequences.id
- campaign_leads.campaignId → campaigns.id
- campaign_leads.leadId → leads.id
- campaign_leads.assignedEmailAccountId → email_accounts.id
- campaign_leads.currentStepId → sequence_steps.id
- outreach_emails.organizationId → organizations.id
- outreach_emails.campaignId → campaigns.id
- outreach_emails.campaignLeadId → campaign_leads.id
- outreach_emails.sequenceStepId → sequence_steps.id
- outreach_emails.emailAccountId → email_accounts.id
- outreach_analytics.organizationId → organizations.id
- outreach_analytics.campaignId → campaigns.id
- outreach_analytics.emailAccountId → email_accounts.id

**Webmail tables:**
- mailboxes.userId → users.id
- mail_folders.mailboxId → mailboxes.id
- mail_messages.mailboxId → mailboxes.id
- mail_messages.folderId → mail_folders.id
- mail_filters.mailboxId → mailboxes.id
- signatures.mailboxId → mailboxes.id
- contacts.userId → users.id
- user_notifications.userId → users.id

**Note:** organization_users already has `org_user_unique` covering (organizationId, userId). Skip single-column orgId index there. organizations.owner_id, routes.smtpEndpointId/httpEndpointId/addressEndpointId — low-volume tables, skip or add for completeness.

## Tasks

<task type="auto" tdd="false">
  <name>Task 1: Add index() import and define FK indexes in schema.ts</name>
  <files>src/db/schema.ts</files>
  <action>
    1. Add `index` to the imports from `drizzle-orm/pg-core` (line 1-12). Current import: `import { pgTable, uuid, text, timestamp, boolean, integer, jsonb, pgEnum, uniqueIndex, bigint } from 'drizzle-orm/pg-core'` — add `index` after `uniqueIndex`.

    2. Add index() callbacks to each table's pgTable definition. Use the pattern:
    ```typescript
    }, (table) => ({
      idxTableNameColumnName: index('idx_table_name_column_name').on(table.columnName),
    }))
    ```
    If the table already has a callback with uniqueIndex entries, ADD the index calls alongside them (do NOT replace existing unique indexes).

    3. Tables to modify (add FK index per list above):
    - **domains**: add `idx_domains_organization_id` on organizationId
    - **credentials**: add `idx_credentials_organization_id` on organizationId
    - **outlook_mailboxes**: add `idx_outlook_mailboxes_organization_id` on organizationId
    - **routes**: add `idx_routes_organization_id` on organizationId
    - **smtp_endpoints**: add `idx_smtp_endpoints_organization_id` on organizationId
    - **http_endpoints**: add `idx_http_endpoints_organization_id` on organizationId
    - **address_endpoints**: add `idx_address_endpoints_organization_id` on organizationId
    - **messages**: add `idx_messages_organization_id` on organizationId
    - **deliveries**: add `idx_deliveries_message_id` on messageId, `idx_deliveries_organization_id` on organizationId
    - **webhooks**: add `idx_webhooks_organization_id` on organizationId
    - **webhook_requests**: add `idx_webhook_requests_webhook_id` on webhookId
    - **templates**: add `idx_templates_organization_id` on organizationId
    - **track_domains**: add `idx_track_domains_organization_id` on organizationId
    - **suppressions**: add `idx_suppressions_organization_id` on organizationId
    - **statistics**: add `idx_statistics_organization_id` on organizationId
    - **email_accounts**: add `idx_email_accounts_organization_id` on organizationId, add `idx_email_accounts_outlook_mailbox_id` on outlookMailboxId (nullable)
    - **lead_lists**: add `idx_lead_lists_organization_id` on organizationId
    - **leads**: add `idx_leads_organization_id` on organizationId, add `idx_leads_lead_list_id` on leadListId (nullable)
    - **campaigns**: add `idx_campaigns_organization_id` on organizationId
    - **sequences**: add `idx_sequences_campaign_id` on campaignId
    - **sequence_steps**: add `idx_sequence_steps_sequence_id` on sequenceId
    - **campaign_leads**: add `idx_campaign_leads_campaign_id` on campaignId, `idx_campaign_leads_lead_id` on leadId, `idx_campaign_leads_assigned_email_account_id` on assignedEmailAccountId (nullable), `idx_campaign_leads_current_step_id` on currentStepId (nullable)
    - **outreach_emails**: add `idx_outreach_emails_organization_id` on organizationId, `idx_outreach_emails_campaign_id` on campaignId, `idx_outreach_emails_campaign_lead_id` on campaignLeadId, `idx_outreach_emails_sequence_step_id` on sequenceStepId, `idx_outreach_emails_email_account_id` on emailAccountId
    - **outreach_analytics**: add `idx_outreach_analytics_organization_id` on organizationId, `idx_outreach_analytics_campaign_id` on campaignId (nullable), `idx_outreach_analytics_email_account_id` on emailAccountId (nullable)
    - **mailboxes**: add `idx_mailboxes_user_id` on userId
    - **mail_folders**: add `idx_mail_folders_mailbox_id` on mailboxId
    - **mail_messages**: add `idx_mail_messages_mailbox_id` on mailboxId, `idx_mail_messages_folder_id` on folderId
    - **mail_filters**: add `idx_mail_filters_mailbox_id` on mailboxId
    - **signatures**: add `idx_signatures_mailbox_id` on mailboxId
    - **contacts**: add `idx_contacts_user_id` on userId
    - **user_notifications**: add `idx_user_notifications_user_id` on userId

    For tables with NO existing callback (domains, credentials, routes, smtp_endpoints, http_endpoints, address_endpoints, webhooks, templates, track_domains, suppressions, statistics, lead_lists, campaigns, sequences, mailboxes, mail_folders, mail_messages, mail_filters, signatures, user_notifications), add a new callback parameter:
    ```typescript
    }, (table) => ({
      idxTableNameColumnName: index('idx_table_name_column_name').on(table.columnName),
    }))
    ```

    For tables WITH existing callbacks (outlook_mailboxes, email_accounts, leads, campaign_leads, outreach_emails, outreach_analytics, sequence_steps, deliveries, webhook_requests, contacts), add new index entries alongside existing uniqueIndex entries.

    For messages: add `idx_messages_organization_id` on organizationId. The existing example in sql/indexes.sql already has this — ensure schema.ts also defines it via index(). Plan 02 will add the composite (organizationId, status) as a separate index.
  </action>
  <verify>
    <automated>grep -c "index(" src/db/schema.ts — should show many new index() calls</automated>
  </verify>
  <done>
    All FK columns across 25+ tables have index() definitions in schema.ts. Drizzle `index` is imported. No existing uniqueIndex entries removed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate sql/indexes.sql with CONCURRENTLY statements</name>
  <files>sql/indexes.sql</files>
  <action>
    Replace the placeholder comment (line 17) with all FK index CONCURRENTLY statements. Keep the header and example index.

    Add CREATE INDEX CONCURRENTLY IF NOT EXISTS for each index defined in schema.ts Task 1. Use the EXACT same index names as defined in schema.ts (the `index('idx_name')` first argument maps to the SQL index name).

    Pattern:
    ```sql
    CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_table_name_column_name
        ON table_name (column_name);
    ```

    Tables map to snake_case SQL table names:
    - domains → domains
    - credentials → credentials
    - outlook_mailboxes → outlook_mailboxes
    - routes → routes
    - smtp_endpoints → smtp_endpoints
    - http_endpoints → http_endpoints
    - address_endpoints → address_endpoints
    - messages → messages
    - deliveries → deliveries
    - webhooks → webhooks
    - webhook_requests → webhook_requests
    - templates → templates
    - track_domains → track_domains
    - suppressions → suppressions
    - statistics → statistics
    - email_accounts → email_accounts
    - lead_lists → lead_lists
    - leads → leads
    - campaigns → campaigns
    - sequences → sequences
    - sequence_steps → sequence_steps
    - campaign_leads → campaign_leads
    - outreach_emails → outreach_emails
    - outreach_analytics → outreach_analytics
    - mailboxes → mailboxes
    - mail_folders → mail_folders
    - mail_messages → mail_messages
    - mail_filters → mail_filters
    - signatures → signatures
    - contacts → contacts
    - user_notifications → user_notifications

    For nullable FK columns (outlookMailboxId, leadListId, assignedEmailAccountId, currentStepId, campaignId in outreach_analytics, emailAccountId in outreach_analytics), still create the index — NULL values are simply excluded.

    Group by table with section comments for readability.
  </action>
  <verify>
    <automated>grep -c "CREATE INDEX CONCURRENTLY" sql/indexes.sql — should be 30+ statements</automated>
  </verify>
  <done>
    sql/indexes.sql contains CREATE INDEX CONCURRENTLY for every FK index. Index names match schema.ts definitions. Can be applied via `npm run db:indexes`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify schema.ts compiles and generates migration</name>
  <files>src/db/schema.ts</files>
  <action>
    1. Run `npx tsc --noEmit src/db/schema.ts` to verify TypeScript compilation passes
    2. Run `npm run db:generate` to verify Drizzle Kit generates migration SQL including the new indexes
    3. Check the generated migration contains CREATE INDEX statements
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5 — should show no errors</automated>
  </verify>
  <done>
    schema.ts compiles without errors. Drizzle Kit generates migration with index definitions.
  </done>
</task>

## Verification

1. `npx tsc --noEmit` passes
2. `grep -c "index(" src/db/schema.ts` shows 30+ index definitions
3. `grep -c "CREATE INDEX CONCURRENTLY" sql/indexes.sql` shows 30+ statements
4. All index names in sql/indexes.sql match names in schema.ts

## Success Criteria

- [ ] `index` imported from `drizzle-orm/pg-core`
- [ ] Every FK column across all org-scoped tables has an index() definition
- [ ] sql/indexes.sql populated with all FK CONCURRENTLY statements
- [ ] No existing uniqueIndex entries removed
- [ ] TypeScript compiles without errors

## Output

After completion, create `.planning/phases/06-index-foundation/06-01-SUMMARY.md`
