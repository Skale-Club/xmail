---
phase: 23-ai-inbox-automation-and-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/043_ai_inbox_automation_audit.sql
  - src/db/schema.ts
  - src/server/lib/inbox-ai-context.ts
  - src/server/lib/inbox-ai-audit.ts
  - src/server/lib/__tests__/inbox-ai-migration.db.test.ts
  - src/server/lib/inbox-ai-context.test.ts
autonomous: true
requirements: [AI-01, AI-03, AI-05]
must_haves:
  truths:
    - "AI context is a deterministic bounded projection of persisted conversation messages and fails closed without a usable inbound body."
    - "Draft assistance and autonomous sending are separate default-off settings; effective autonomy requires organization and campaign opt-in."
    - "Audit stores stable message references/context hash/model/prompt version/decision/policy/approval/send links without credentials or hidden reasoning."
    - "Migration 043 is provisional and is revalidated/renumbered to the next free slot before implementation."
  artifacts:
    - path: "supabase/migrations/043_ai_inbox_automation_audit.sql"
      provides: "AI settings/run audit schema; final number chosen at execution"
    - path: "src/server/lib/inbox-ai-context.ts"
      provides: "Tenant-safe persisted-message context builder"
      exports: ["buildInboxAiContext"]
    - path: "src/server/lib/inbox-ai-audit.ts"
      provides: "Allowlisted AI run lifecycle persistence"
---
<objective>Create the data/control/audit foundation and trustworthy persisted-message context builder before any AI endpoint or worker can act.</objective>
<execution_context>@23-CONTEXT.md @23-RESEARCH.md @.planning/phases/21-unified-inbox-foundation/*-SUMMARY.md @AGENTS.md</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Add default-off settings and AI-run audit migration</name>
  <read_first>
    - AGENTS.md migration rules
    - supabase/migrations/ (confirm 043 is next; it is provisional)
    - src/db/schema.ts campaign/outreach settings and Phase 21/22 tables
    - supabase/migrations/034_outreach_agentic_followup.sql (legacy fields/backfill)
  </read_first>
  <files>supabase/migrations/043_ai_inbox_automation_audit.sql, src/db/schema.ts, src/server/lib/__tests__/inbox-ai-migration.db.test.ts</files>
  <action>Choose the next free migration number and update all phase references. Hand-write idempotent DDL for separate organization draft/autonomy controls (or extend the finalized settings table) and `outreach_ai_runs` per 23-RESEARCH. Default/backfill both controls false. Add strict statuses/kinds, lease/attempt/idempotency constraints, organization/campaign/conversation/message foreign keys, `(organization_id,idempotency_key)` uniqueness, due/lease/history indexes, timestamps, and defense-in-depth RLS. Preserve legacy campaign columns for compatible rollout but document the effective-control intersection. Mirror in schema.ts. Never run Drizzle generation/push. Add `inbox-ai-migration.db.test.ts`, using only the Phase 18 protected disposable PostgreSQL URL passed explicitly to the migration helper; apply migration 043 twice and assert default-off controls, audit constraints/indexes/RLS shape, and schema-mirror names. Never read/fall back to application `DATABASE_URL`. Keep `psql $env:DATABASE_URL -v ON_ERROR_STOP=1 -f supabase/migrations/043_ai_inbox_automation_audit.sql` only in an explicitly marked manual production runbook.</action>
  <verify><automated>npm run test -- src/server/lib/__tests__/inbox-ai-migration.db.test.ts &amp;&amp; npm run build</automated></verify>
  <done>Final migration is sequential, safe, default-off, indexed for claims/history, and mirrored in TypeScript.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build deterministic persisted-message context</name>
  <read_first>
    - Phase 21 summaries and schema (conversation/message/body/participants/attribution)
    - src/server/lib/outreach-access.ts (`requireOutreachRead`/`requireOutreachWrite`; context services receive verified organization scope)
    - src/server/lib/outreach-followup.ts (legacy context to replace)
  </read_first>
  <files>src/server/lib/inbox-ai-context.ts, src/server/lib/inbox-ai-context.test.ts</files>
  <action>Implement `buildInboxAiContext` with explicit organization scope, deterministic ordered message selection, latest inbound-body requirement, message/character budget, normalized plain text, participant/direction/time/campaign facts, untrusted-content boundaries, canonical serialization, ordered message IDs, and SHA-256 context hash. Never read `campaignLeads.lastReplyText` as source and never include attachment bytes, credentials, provider tokens, raw headers, hidden system prompts, or other-tenant rows. Test long threads, HTML-only normalization, headers-only refusal, prompt-injection text treated as data, stable hash/order, attribution mismatch, and two-tenant isolation.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-ai-context.test.ts && npm run build && npm run lint</automated></verify>
  <done>Context is reproducible from authorized persisted messages and missing bodies produce an explicit no-action error.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Implement redacted AI-run lifecycle audit service</name>
  <read_first>
    - src/server/lib/inbox-ai-context.ts
    - src/server/lib/logger.ts (structured redaction conventions)
    - finalized AI-run schema from Task 1
  </read_first>
  <files>src/server/lib/inbox-ai-audit.ts, src/server/lib/inbox-ai-context.test.ts</files>
  <action>Add create/claim/complete/fail/defer/approve/link-command functions with transactions, bounded error text, stable idempotency, lease recovery, and explicit allowed transitions. Store message refs/hash, prompt version, provider/model/config allowlist, sanitized draft/action, policy code, actor/approval and command/outreach links. Reject/log-redact keys matching authorization/token/secret/api-key and never accept arbitrary request blobs. Test invalid transitions, lease expiry, duplicate key, redaction, approval ownership, and tenant isolation.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-ai-context.test.ts && npm run build</automated></verify>
  <done>Every future model call/action can be reconstructed by references without exposing secrets or allowing cross-tenant audit access.</done>
</task>
</tasks>
<verification>Run the protected `inbox-ai-migration.db.test.ts` (which applies finalized SQL twice), context/audit tests, build, and lint; search AI audit DTO/logging for forbidden secret/header fields.</verification>
<success_criteria>AI-01 foundation and default-off/audit controls for AI-03/05 exist before any model or send path is enabled.</success_criteria>
<output>Create `23-01-SUMMARY.md` with migration number, control intersection, context budget/hash contract, audit fields/redactions, and tests.</output>
