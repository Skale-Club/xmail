---
phase: 23-ai-inbox-automation-and-guardrails
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - src/server/jobs/processFollowUps.ts
  - src/server/jobs/processReplies.ts
  - src/server/lib/outreach-followup.ts
  - src/server/lib/inbox-ai-automation.ts
  - src/server/lib/inbox-ai-automation.test.ts
  - src/server/jobs/index.ts
autonomous: true
requirements: [AI-01, AI-03, AI-04, AI-05]
must_haves:
  truths:
    - "Autonomous work is claimed with a lease and is enabled only when organization and campaign controls are both on and unpaused."
    - "The model cannot choose recipients/account or override deterministic policy."
    - "Every AI send becomes a durable inbox command handled by the single lease-aware executeInboxSendCommand executor; AI never dispatches directly."
    - "Organization/campaign/outreach pause is rechecked immediately before dispatch; ambiguous outcomes are held, not resent."
---
<objective>Replace the legacy direct-send follow-up job with persisted-message, audited, lease-based autonomous decisions that cannot bypass shared delivery controls.</objective>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Schedule auditable automation from persisted inbound messages</name>
  <read_first>
    - src/server/jobs/processReplies.ts (legacy schedule hook)
    - Phase 21 ingestion summaries (post-commit message/conversation events)
    - src/server/lib/inbox-ai-audit.ts and inbox-ai-context.ts
  </read_first>
  <files>src/server/jobs/processReplies.ts, src/server/lib/inbox-ai-automation.ts, src/server/lib/inbox-ai-automation.test.ts</files>
  <action>After a normalized inbound reply commits and attribution succeeds, schedule one AI run keyed by organization+conversation+trigger message only when effective org+campaign autonomy is enabled. Do not schedule DSN/auto-reply/unsubscribed/suppressed/missing-body conversations. Link trigger message and campaign lead; retain `nextFollowUpAt/lastReplyText` only for rollout compatibility, not reasoning. Test duplicate ingestion, controls off/paused, invalid message kinds, and two tenants.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-ai-automation.test.ts && npm run build</automated></verify>
  <done>One eligible persisted inbound message creates at most one auditable run and no send.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Refactor processor to leased decision and shared command/dispatch</name>
  <read_first>
    - src/server/jobs/processFollowUps.ts (legacy direct-send code to remove)
    - src/server/lib/outreach-delivery-policy.ts and outreach-dispatch.ts
    - Phase 22 inbox command summary
    - src/server/lib/inbox-ai-context.ts, inbox-ai-audit.ts, outreach-followup.ts
  </read_first>
  <files>src/server/jobs/processFollowUps.ts, src/server/lib/inbox-ai-automation.ts, src/server/lib/inbox-ai-automation.test.ts, src/server/jobs/index.ts</files>
  <action>Rewrite the job around deterministic ordered lease claims from `outreach_ai_runs`. Every claim/query/update includes organization scope. Recheck org/campaign automation, outreach kill switch, trigger/conversation state, max followups, and persisted context before external call. Persist proposal. For wait/complete/escalate, transition audit only. For draft/send, deterministic code resolves the existing recipient/account, creates a durable Phase 22 inbox command, and either hands an immediate claimed command to `executeInboxSendCommand` or leaves it queued for that executor. AI never evaluates delivery policy or calls `dispatchOutreachMessage` directly. Link run→command→outreach email→unified message after executor outcome. Remove `sendThreadedReply`/dispatcher imports. Test concurrent ticks, restart lease, duplicate key, policy outcomes via executor fake, pause race, invalid recipients, max count, timeout, held result, and exactly-once linkage.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-ai-automation.test.ts && npm run build && npm run lint</automated></verify>
  <done>Agentic processing has one delivery path, bounded retries, durable audit/history, and no policy/provider bypass.</done>
</task>
<task type="auto">
  <name>Task 3: Remove legacy context/direct-send behavior and document rollout kill controls</name>
  <read_first>
    - src/server/lib/outreach-followup.ts and processFollowUps.ts after Tasks 1–2
    - src/server/jobs/index.ts schedules/logging
    - src/db/schema.ts legacy agentic fields
  </read_first>
  <files>src/server/lib/outreach-followup.ts, src/server/jobs/processFollowUps.ts, src/server/jobs/index.ts, src/server/lib/inbox-ai-automation.test.ts</files>
  <action>Delete/deprecate legacy `FollowUpContext.lastReplyText`, partial `enforceGuardrails`, direct sender call, and separate unaudited counter update. Keep compatibility columns only until a future cleanup migration. Structured logs use run/command/org IDs and codes, never bodies. Register one advisory-locked cadence and expose startup log indicating global automation disabled/enabled counts without tenant content. Add a static regression assertion that agentic modules do not import provider adapters or `sendThreadedReply`.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-ai-automation.test.ts && npm run build && npm run lint</automated></verify>
  <done>Legacy bypass is absent and operators have immediate effective kill controls at organization, campaign, and outreach levels.</done>
</task>
</tasks>
<verification>Full automation tests pass; search finds no direct provider/sender in agentic worker; simulate process death after model response and after provider dispatch to prove lease/idempotency/held behavior.</verification>
<success_criteria>AI-03/04 are complete and every autonomous decision/send is grounded, auditable, opt-in, and policy-gated.</success_criteria>
<output>Create `23-03-SUMMARY.md` with state machine, effective control truth table, dispatch linkage, legacy removals, and crash tests.</output>
