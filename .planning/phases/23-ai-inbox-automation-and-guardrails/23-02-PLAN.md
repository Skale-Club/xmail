---
phase: 23-ai-inbox-automation-and-guardrails
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/server/lib/outreach-followup.ts
  - src/server/routes/outreach/unified-inbox.ts
  - src/server/lib/inbox-ai-suggestions.test.ts
  - src/lib/unified-inbox-api.ts
  - src/hooks/useUnifiedInbox.ts
  - src/components/outreach/inbox/AiDraftAssistant.tsx
  - src/components/outreach/inbox/ConversationComposer.tsx
  - src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx
autonomous: true
requirements: [AI-01, AI-02, AI-05, AI-06]
must_haves:
  truths:
    - "Suggestion uses persisted context and creates an audit run; it cannot send or schedule by itself."
    - "Missing config, timeout, malformed/unsafe output, disabled setting, or missing body returns a recoverable no-send result."
    - "Operator explicitly inserts/edits a suggestion before the normal Phase 22 send action."
    - "Public status/history DTOs omit credentials, raw hidden prompt, and cross-tenant message content."
---
<objective>Deliver on-demand, human-controlled AI draft suggestions in the Unified Inbox with fail-closed Xphere integration and a redacted audit trail.</objective>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Refactor Xphere into a strict proposal adapter</name>
  <read_first>
    - src/server/lib/outreach-followup.ts (legacy timeout/parser)
    - src/server/lib/inbox-ai-context.ts and inbox-ai-audit.ts
    - 23-CONTEXT.md decision boundary
  </read_first>
  <files>src/server/lib/outreach-followup.ts, src/server/lib/inbox-ai-suggestions.test.ts</files>
  <action>Refactor/export an adapter that accepts the canonical persisted context plus run/prompt version, calls configured Xphere with timeout, and parses strict `draft|wait|complete|escalate` output. Bound subject/body/delay/outcome lengths, strip disallowed control fields, and return typed error codes. Keep credentials only in environment/Authorization header and redact logs. It may not accept account/recipient/provider/policy overrides. Test timeout, missing config, malformed JSON/schema, oversized output, invented recipients/control fields, valid multilingual draft, and abort.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-ai-suggestions.test.ts && npm run build</automated></verify>
  <done>Xphere is a proposal-only dependency with strict fail-closed output.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add tenant-safe suggestion and redacted history APIs</name>
  <read_first>
    - Phase 22 inbox router/access patterns
    - src/server/lib/inbox-ai-context.ts, inbox-ai-audit.ts, outreach-followup.ts
    - src/server/lib/outreach-access.ts (`requireOutreachRead`/`requireOutreachWrite` canonical route authorization)
  </read_first>
  <files>src/server/routes/outreach/unified-inbox.ts, src/server/lib/inbox-ai-suggestions.test.ts</files>
  <!-- Extend only unified-inbox.ts: suggestion mutation uses requireOutreachWrite; history/status reads use requireOutreachRead; every query/mutation includes verified organization scope. -->
  <action>Add `POST .../conversations/:id/ai-suggestions` with client idempotency key and optional tone goal from an allowlist; verify org member write access and draft-assistance setting; build persisted context; create audit run before external call; complete/fail audit; return suggestion/run ID only—never dispatch. Add organization/conversation-scoped status/history endpoints with allowlisted DTO fields and cursor bounds. Viewer may read history only if final access contract permits and cannot request suggestions. Rate-limit per user/org and prevent duplicate in-flight suggestions. Test two tenants, disabled setting, missing-body, rate limit, duplicate key, external failures, response redaction, and no dispatch mock calls.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-ai-suggestions.test.ts && npm run build && npm run lint</automated></verify>
  <done>Endpoint always produces a persisted suggestion/failure run and provably never sends.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Integrate editable AI suggestion into the Phase 22 composer</name>
  <read_first>
    - src/components/outreach/inbox/ConversationComposer.tsx
    - src/hooks/useUnifiedInbox.ts and unified-inbox-api.ts
    - 22-UI-SPEC composer/failure/accessibility contract
  </read_first>
  <files>src/lib/unified-inbox-api.ts, src/hooks/useUnifiedInbox.ts, src/components/outreach/inbox/AiDraftAssistant.tsx, src/components/outreach/inbox/ConversationComposer.tsx, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>Add a clearly labeled `Suggest draft` action only when enabled. Show progress/cancel/failure inline, then preview the draft with `Insert into reply` and `Discard`; never mutate recipient/account or call send. Insertion copies subject/body into normal editable fields and records explicit acceptance/approval against the run; subsequent send remains the Phase 22 operator action/policy path. Preserve operator text unless they confirm replacement. Expose compact redacted run status/history. Test disabled/timeout/discard/insert/edit/send separation, existing-body replace confirmation, focus/live-region behavior, and absence of secret/prompt fields.</action>
  <verify><automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run build && npm run lint</automated></verify>
  <done>An operator can safely request, inspect, insert, edit, and separately send a suggestion with complete audit linkage.</done>
</task>
</tasks>
<verification>Suggestion/API/UI tests pass; spy proves zero dispatch calls from suggestion endpoint; inspect browser response/logs for redacted fields.</verification>
<success_criteria>AI-02 is complete and AI-01/05/06 are enforced for human-in-the-loop drafts.</success_criteria>
<output>Create `23-02-SUMMARY.md` with adapter schema, endpoint/rate limits, audit linkage, and no-send evidence.</output>
