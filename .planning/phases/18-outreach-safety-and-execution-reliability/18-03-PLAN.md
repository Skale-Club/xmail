---
phase: 18-outreach-safety-and-execution-reliability
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - supabase/migrations/038_outreach_dispatch_state_machine.sql
  - src/db/schema.ts
  - src/server/lib/outreach-dispatch.ts
  - src/server/lib/outreach-sender.ts
  - src/server/lib/__tests__/outreach-dispatch-migration.db.test.ts
  - src/server/lib/__tests__/outreach-dispatch.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-03, SAFE-06]
must_haves:
  truths:
    - "A logical outreach send has one stable organization-scoped idempotency key"
    - "Retryable pre-acceptance failures use bounded attempts and backoff"
    - "A stale claim after dispatch began is held and never blindly resent"
  artifacts:
    - path: supabase/migrations/038_outreach_dispatch_state_machine.sql
      provides: "Durable lease/attempt/idempotency columns, constraints, and indexes"
    - path: src/server/lib/outreach-dispatch.ts
      provides: "Single durable claim-policy-dispatch-finalize state machine"
  key_links:
    - from: src/server/lib/outreach-dispatch.ts
      to: src/server/lib/outreach-delivery-policy.ts
      via: "policy check before claim and immediately before provider dispatch"
---

<objective>
Replace insert-once claims with a durable, bounded dispatch state machine that safely recovers known failures and quarantines ambiguous provider outcomes.
</objective>

<context>
@.planning/phases/18-outreach-safety-and-execution-reliability/18-RESEARCH.md
@src/db/schema.ts
@src/server/lib/outreach-sender.ts
@supabase/migrations/037_reconcile_schema_only_unique_indexes.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the hand-written dispatch-state migration and schema mirror</name>
  <files>supabase/migrations/038_outreach_dispatch_state_machine.sql, src/db/schema.ts, src/server/lib/__tests__/outreach-dispatch-migration.db.test.ts</files>
  <read_first>
    - AGENTS.md (Schema & Migration Workflow)
    - supabase/migrations/035_outreach_reproducibility_and_reply_index.sql
    - supabase/migrations/037_reconcile_schema_only_unique_indexes.sql
    - src/db/schema.ts (outreachEmails)
  </read_first>
  <action>
    Write idempotent migration 038 adding `origin`, `idempotency_key`, `to_address`, `attempt_count`, `max_attempts`, `next_attempt_at`, `lease_token`, `lease_expires_at`, `dispatch_started_at`, `last_attempt_at`, and `last_error_code` to `outreach_emails`. Backfill campaign origin/key/to-address deterministically from campaign-lead/step/lead rows. Make campaign/lead/step FKs and tracking token nullable so the table can represent manual and threaded sends; add origin-shape checks requiring all campaign fields/tracking token for `campaign`, campaign+campaign-lead for `agentic`, and a recipient/idempotency key for every origin. Add checks for origin values and non-negative/bounded attempts, unique `(organization_id, idempotency_key)`, and an eligibility index covering status/next_attempt_at/lease_expires_at. Preserve the existing campaign-lead/step unique index for campaign rows. Mirror every column/nullability/check/index in `src/db/schema.ts`.

    Add `outreach-dispatch-migration.db.test.ts`: obtain only the protected disposable PostgreSQL URL created by the Phase 18 harness, pass it explicitly to the migration helper, apply migration 038 twice, and assert columns, constraints, indexes, defaults, and schema-mirror names. The automated test must never read or fall back to application `DATABASE_URL`.

    Do not execute against production automatically. Do not run Drizzle generate or push. Document this as a manual production runbook only: `psql $env:DATABASE_URL -v ON_ERROR_STOP=1 -f supabase/migrations/038_outreach_dispatch_state_machine.sql`.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-dispatch-migration.db.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Migration 038 is rerunnable where practical and the TypeScript schema matches its names/defaults/constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement claim, recovery, retry classification, and finalize</name>
  <files>src/server/lib/outreach-dispatch.ts, src/server/lib/outreach-sender.ts, src/server/lib/__tests__/outreach-dispatch.test.ts</files>
  <read_first>
    - src/server/lib/outreach-delivery-policy.ts
    - src/server/lib/outreach-sender.ts
    - src/server/jobs/processOutreachSequences.ts (current claim/send/finalize block)
    - src/server/lib/cron-lock.ts
  </read_first>
  <action>
    Implement `dispatchOutreachMessage` with injected repository/provider interfaces and a Drizzle default adapter. Its sequence is: derive/validate idempotency key; evaluate policy; atomically insert or lease an eligible row; evaluate policy again; persist `dispatch_started_at`; call the provider with a stable Message-ID when supported; finalize `sent`; or classify failure. Retry only explicit transient failures known not to have been accepted (connection/DNS/timeout before acceptance, provider 429/5xx with a known negative response), using capped exponential backoff and `max_attempts`. Terminal 4xx/auth/content errors end `failed` with no `next_attempt_at`. Recovery moves stale leases with `dispatch_started_at` to `held`; stale pre-dispatch leases become eligible again. Lease token must be present in every finalize update so an expired worker cannot overwrite a newer owner.

    Keep `sendOutreachEmail` as a provider/content adapter temporarily, but make its result include normalized failure classification and acceptance metadata. Never log body content or credentials. Tests cover duplicate idempotency, lease contention, stale pre-dispatch reclaim, stale post-dispatch hold, retry exhaustion, policy changed between claim/dispatch, and stale-token finalize rejection.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-dispatch.test.ts</automated>
  </verify>
  <done>Known pre-acceptance failures recover within bounds; ambiguous sends cannot be automatically duplicated.</done>
</task>

</tasks>

<verification>
- `npm run test`
- `npm run build`
- `npm run lint`
- `npm run test -- src/server/lib/__tests__/outreach-dispatch-migration.db.test.ts` applies migration 038 twice through the protected disposable-database harness and asserts drift-sensitive schema details.
</verification>

<success_criteria>
1. Claims expire and recover according to a tested durable state model.
2. Provider ambiguity is visible as `held` and is never auto-retried.
3. Every logical send can be addressed by a stable idempotency key.
</success_criteria>
