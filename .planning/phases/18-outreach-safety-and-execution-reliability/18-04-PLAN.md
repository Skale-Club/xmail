---
phase: 18-outreach-safety-and-execution-reliability
plan: 04
type: execute
wave: 3
depends_on: [18-02, 18-03]
files_modified:
  - src/server/jobs/processOutreachSequences.ts
  - src/server/jobs/processFollowUps.ts
  - src/server/jobs/index.ts
  - src/server/routes/outreach/send-message.ts
  - src/server/lib/outreach-sequence-state.ts
  - src/server/lib/__tests__/outreach-scheduling.test.ts
  - src/server/lib/__tests__/outreach-entrypoints.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-03, SAFE-04, SAFE-05, SAFE-06]
must_haves:
  truths:
    - "Campaign, manual, and agentic sends all enter the durable dispatcher"
    - "Due work is deterministic and no blocked account can consume the whole batch"
    - "Campaigns complete when every enrolled lead has completed_at or is replied/interested/not_interested/bounced/unsubscribed"
    - "Scheduler and completion queries import one exhaustive tested terminal-status contract"
  artifacts:
    - path: src/server/jobs/processOutreachSequences.ts
      provides: "Fair selection and dispatcher-driven sequence execution"
  key_links:
    - from: src/server/jobs/processFollowUps.ts
      to: src/server/lib/outreach-dispatch.ts
      via: "origin=agentic"
    - from: src/server/routes/outreach/send-message.ts
      to: src/server/lib/outreach-dispatch.ts
      via: "origin=manual"
---

<objective>
Wire every existing outreach entrypoint into the safe dispatcher, make scheduling fair and forward-progressing, and activate correct campaign completion.
</objective>

<context>
@.planning/phases/18-outreach-safety-and-execution-reliability/18-CONTEXT.md
@src/server/jobs/processOutreachSequences.ts
@src/server/jobs/processFollowUps.ts
@src/server/routes/outreach/send-message.ts
@src/server/jobs/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select due work deterministically and fairly</name>
  <files>src/server/jobs/processOutreachSequences.ts, src/server/lib/outreach-sequence-state.ts, src/server/lib/__tests__/outreach-scheduling.test.ts</files>
  <read_first>
    - src/server/jobs/processOutreachSequences.ts (pendingLeads query, skips, jitter)
    - src/db/schema.ts (campaignLead/account scheduling indexes)
    - src/server/lib/outreach-delivery-policy.ts
  </read_first>
  <action>
    In `outreach-sequence-state.ts`, define one compile-time exhaustive progress map for every `leadStatusEnum` member: `new` and `contacted` are nonterminal; `replied`, `interested`, `not_interested`, `bounced`, and `unsubscribed` are terminal. Export the derived immutable `TERMINAL_CAMPAIGN_LEAD_STATUSES` and `isTerminalCampaignLeadStatus`; adding an enum member must cause a TypeScript error or failing exhaustiveness test until classified.

    Replace the unordered 200-row relation query with a bounded selector that imports that terminal set to prefilter active campaigns, enabled organizations, verified accounts, nonterminal leads, and due timestamps; ranks rows per account by `next_scheduled_at,id`; then takes a deterministic cross-account batch. Hydrate selected IDs and restore selector order. When policy returns a temporal denial, persist its `retryAt`; for outside-window calculate the next opening, for spacing use `lastSentAt + min`, and for daily exhaustion use next UTC reset. Keep a hard scan/work cap and structured deferred counts. Tests cover all seven lead statuses, more than 200 blocked rows plus sendable accounts, stable ordering, and repeated ticks advancing timestamps rather than spinning.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-scheduling.test.ts</automated>
  </verify>
  <done>A blocked inbox/campaign cannot starve another inbox and every temporal denial has a future eligibility timestamp.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route campaign, manual, and agentic sends through dispatch</name>
  <files>src/server/jobs/processOutreachSequences.ts, src/server/jobs/processFollowUps.ts, src/server/routes/outreach/send-message.ts, src/server/lib/__tests__/outreach-entrypoints.test.ts</files>
  <read_first>
    - src/server/lib/outreach-dispatch.ts
    - src/server/jobs/processOutreachSequences.ts
    - src/server/jobs/processFollowUps.ts
    - src/server/routes/outreach/send-message.ts
  </read_first>
  <action>
    Remove direct provider calls and ad-hoc claim creation from all three entrypoints. Campaign keys are `campaign:{campaignLeadId}:{sequenceStepId}`; manual requires a caller request/idempotency key (or a server-generated request ID returned to the client) and resolves any verified provider, not just native; agentic keys include campaign lead, inbound reply ID, and follow-up ordinal. Map policy denials to structured job deferral or HTTP 409/423 without provider work. On success update campaign/lead/account counters only from the dispatch result so retries do not double-count. Persist manual/agentic attempts in the migration-038 `outreach_emails` origin-aware shape; never manufacture fake campaign/sequence rows. Keep SQL, schema, route responses, and tests consistent.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-entrypoints.test.ts</automated>
  </verify>
  <done>There is no provider dispatch in these files outside `dispatchOutreachMessage`, and pausing the organization blocks all three origins.</done>
</task>

<task type="auto">
  <name>Task 3: Complete campaigns from actual terminal progress</name>
  <files>src/server/jobs/processOutreachSequences.ts, src/server/jobs/index.ts, src/server/lib/outreach-sequence-state.ts, src/server/lib/__tests__/outreach-scheduling.test.ts</files>
  <read_first>
    - src/server/jobs/processOutreachSequences.ts (markCompletedCampaigns)
    - src/server/jobs/index.ts (outreach cron)
  </read_first>
  <action>
    Redefine incomplete as `completed_at IS NULL` and status not in the shared exhaustive terminal set (`replied`, `interested`, `not_interested`, `bounced`, `unsubscribed`); do not duplicate a shorter SQL-only list. Require at least one campaign lead so empty active campaigns do not auto-complete. Call completion after each locked outreach tick (including ticks with no due rows), update status/completedAt atomically, and emit IDs/count. Add table-driven tests for every lead status, contacted+completedAt, one pending lead, mixed terminal statuses, and zero-lead campaign. Assert selector exclusion and completion use the identical exported set.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-scheduling.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Active campaigns transition once and only when every enrolled lead is complete/terminal.</done>
</task>

</tasks>

<verification>
- `npm run test`
- `npm run build`
- `npm run lint`
- `rg -n "sendOutreachEmail|sendThreadedReply|relayMessage" src/server/jobs/processOutreachSequences.ts src/server/jobs/processFollowUps.ts src/server/routes/outreach/send-message.ts` returns no direct dispatch usage.
</verification>

<success_criteria>
1. Organization pause blocks all current outreach origins at the last safe boundary.
2. Fair selection makes progress beyond the first 200 due records.
3. Completed sequences and every terminal engagement outcome (`replied`, `interested`, `not_interested`, `bounced`, `unsubscribed`) lead to completed campaigns without rewriting that engagement status.
4. Critical entrypoint, scheduling, retry, and tenant cases are automated.
</success_criteria>
