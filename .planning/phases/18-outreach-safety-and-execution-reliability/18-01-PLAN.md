---
phase: 18-outreach-safety-and-execution-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/test/setup-jsdom.ts
  - src/test/postgres-harness.ts
  - src/test/postgres-global-setup.ts
  - src/test/__tests__/harness-smoke.test.tsx
  - src/test/__tests__/postgres-harness.db.test.ts
  - src/server/lib/outreach-delivery-policy.ts
  - src/server/lib/__tests__/outreach-delivery-policy.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-06]
must_haves:
  truths:
    - "Every outreach origin receives the same explicit allow/deny decision before dispatch"
    - "An organization with outreach_enabled=false is denied with no provider call"
    - "The repository runs TypeScript server tests in Node and TSX component tests in jsdom"
    - "Database tests use a disposable PostgreSQL instance and cannot fall back to production DATABASE_URL"
  artifacts:
    - path: src/server/lib/outreach-delivery-policy.ts
      provides: "Shared delivery policy and stable denial codes"
    - path: vitest.config.ts
      provides: "Node, jsdom/React, and PostgreSQL Vitest projects for TS and TSX"
    - path: src/test/postgres-harness.ts
      provides: "Guarded disposable PostgreSQL lifecycle"
  key_links:
    - from: src/server/lib/outreach-delivery-policy.ts
      to: src/db/schema.ts
      via: "organization/account/campaign/suppression lookups"
---

<objective>
Install the test harness and define the single fail-closed delivery-policy contract that campaign, manual, agentic, and future Unified Inbox sends must call.
</objective>

<context>
@.planning/phases/18-outreach-safety-and-execution-reliability/18-CONTEXT.md
@.planning/phases/18-outreach-safety-and-execution-reliability/18-RESEARCH.md
@src/server/lib/outreach-sender.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vitest projects for server TS and React TSX</name>
  <files>package.json, package-lock.json, vitest.config.ts, src/test/setup-jsdom.ts, src/test/__tests__/harness-smoke.test.tsx</files>
  <read_first>
    - package.json
    - tsconfig.server.json
    - vite.config.ts
    - src/components/ui/button.tsx (small component fixture shape)
  </read_first>
  <action>
    Add `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, and `@testing-library/user-event` as dev dependencies and scripts `test` (`vitest run`) and `test:watch` (`vitest`). Create Vitest projects (or equivalent project config) with: `server` for `src/**/*.{test,spec}.ts` in Node while excluding `*.db.test.ts`/`*.db.spec.ts`; `client` for `src/**/*.{test,spec}.tsx` in jsdom with `src/test/setup-jsdom.ts`; and a separate Node `postgres` project for `src/**/*.db.{test,spec}.ts`. Make project include/exclude patterns mutually exclusive so a database test cannot run a second time in the ordinary server project. Restore mocks between tests and retain Vite's `@` alias. Add a tiny Testing Library TSX smoke test proving jest-dom matchers and cleanup work. Use `npm install` to update the lockfile.

    Standardize documentation and every plan verification on `npm run test -- <arquivo>` (never `npx vitest` or a second script per project). A path selector must select the correct project from its extension/name.
  </action>
  <verify>
    <automated>npm run test -- src/test/__tests__/harness-smoke.test.tsx</automated>
  </verify>
  <done>The canonical command executes TSX in jsdom, while a `.test.ts` fixture is assigned to the Node project.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a disposable PostgreSQL harness with a hard production guard</name>
  <files>package.json, package-lock.json, vitest.config.ts, src/test/postgres-harness.ts, src/test/postgres-global-setup.ts, src/test/__tests__/postgres-harness.db.test.ts</files>
  <read_first>
    - src/db/index.ts (DATABASE_URL initialization)
    - scripts/audit-schema-drift.ts
    - supabase/migrations/037_reconcile_schema_only_unique_indexes.sql
    - .env.example (database variable names only; never print values)
  </read_first>
  <action>
    Add `@testcontainers/postgresql`/`testcontainers` as dev dependencies. Implement a PostgreSQL Vitest global setup/teardown that starts a disposable container with a database named `xmail_test_<run-id>`, exposes only its generated connection URL to integration tests, applies hand-written migrations to that URL, and always stops the container. Before assigning the test URL, capture the pre-existing `DATABASE_URL` and hard-fail if any candidate test URL equals it. The reusable `assertSafeTestDatabaseUrl` must also reject missing run guard, non-PostgreSQL URLs, non-loopback explicitly supplied hosts, and database names without a `test` marker. Migration helpers must require the guarded URL as an explicit argument and must never read/fallback to application `DATABASE_URL`. Redact credentials from every error/log.

    Add tests proving a production-like remote URL, the current `DATABASE_URL`, and a non-test database name are rejected, while the generated container URL is accepted. Apply migration 038 twice in the disposable database and assert its dispatch columns/indexes once Plan 18-03 lands; until then make that assertion fixture conditional on the migration file existing, not on database availability.
  </action>
  <verify>
    <automated>npm run test -- src/test/__tests__/postgres-harness.db.test.ts</automated>
  </verify>
  <done>Database tests create/drop an isolated PostgreSQL target and cannot execute SQL against the application's configured database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the shared delivery policy with injected data access</name>
  <files>src/server/lib/outreach-delivery-policy.ts, src/server/lib/__tests__/outreach-delivery-policy.test.ts</files>
  <read_first>
    - src/db/schema.ts (organizations, campaigns, emailAccounts, leads, suppressions)
    - src/server/lib/outreach-sender.ts (isWithinSendWindow, canSendFromAccount, getEffectiveDailySendLimit)
    - src/server/jobs/processFollowUps.ts
    - src/server/routes/outreach/send-message.ts
  </read_first>
  <action>
    Create `OutreachOrigin`, `DeliveryPolicyCode`, `DeliveryPolicyInput`, `DeliveryPolicyDecision`, and `evaluateOutreachDeliveryPolicy`. Split the pure evaluation from the default Drizzle-backed loader so tests can inject organization/account/campaign/lead/suppression snapshots. Deny, in fail-closed order: missing tenant resource, organization disabled, inactive campaign when campaignId is supplied, non-verified/cross-org account, unsubscribed lead, suppression, outside campaign window, daily/warm-up exhaustion, and per-account spacing. Return `retryAt` for temporal window/limit/spacing denials. Never accept caller-supplied organization/account objects without confirming IDs and organization ownership in the default loader. Keep all denial codes stable and log-safe (no credentials/body content).

    Cover all four origins, cross-tenant account rejection, kill-switch denial, suppression, warm-up daily limit, spacing, and allowed cases. Prove with a provider spy fixture that denied decisions do not reach a dispatch callback.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-delivery-policy.test.ts</automated>
  </verify>
  <done>The policy returns deterministic denial codes/retry times and all origin types are blocked by the organization switch.</done>
</task>

</tasks>

<verification>
- `npm run test`
- `npm run build`
- `npm run lint`
</verification>

<success_criteria>
1. SAFE-01 has one reusable server-side gate instead of route/job-specific booleans.
2. The test runner executes `.ts` in Node, `.tsx` in jsdom/Testing Library, and `.db.test.ts` against disposable PostgreSQL using the same targeted command shape.
3. The PostgreSQL harness has a tested hard guard against production `DATABASE_URL` reuse.
4. Tenant/account ownership and kill-switch behavior are covered by automated tests.
</success_criteria>
