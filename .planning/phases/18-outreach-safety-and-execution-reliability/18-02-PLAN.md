---
phase: 18-outreach-safety-and-execution-reliability
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/server/lib/outreach-sequence-state.ts
  - src/server/lib/__tests__/outreach-sequence-state.test.ts
  - src/server/routes/outreach/campaigns.ts
  - src/server/jobs/processOutreachSequences.ts
autonomous: true
requirements: [SAFE-02, SAFE-06]
must_haves:
  truths:
    - "Only a sequence step whose type is email can request a provider dispatch"
    - "Delay steps transition and schedule; legacy condition steps fail closed"
    - "A campaign containing an unsupported condition step cannot be activated"
  artifacts:
    - path: src/server/lib/outreach-sequence-state.ts
      provides: "Pure ordered step transition contract"
  key_links:
    - from: src/server/jobs/processOutreachSequences.ts
      to: src/server/lib/outreach-sequence-state.ts
      via: "resolveSequenceAction before any claim/send"
---

<objective>
Make sequence execution type-safe so delay/condition rows cannot produce blank messages, while preserving existing email sequence behavior.
</objective>

<context>
@.planning/phases/18-outreach-safety-and-execution-reliability/18-CONTEXT.md
@src/server/jobs/processOutreachSequences.ts
@src/server/routes/outreach/campaigns.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test pure sequence actions</name>
  <files>src/server/lib/outreach-sequence-state.ts, src/server/lib/__tests__/outreach-sequence-state.test.ts</files>
  <read_first>
    - src/server/jobs/processOutreachSequences.ts (getNextStep and calculateNextScheduledAt)
    - src/db/schema.ts (sequenceSteps and campaignLeads)
  </read_first>
  <action>
    Introduce `resolveSequenceAction(steps, currentStep, now, schedule)` returning a discriminated union: `send_email`, `advance_without_send`, `complete`, or `quarantine`. Email steps require a non-empty subject and at least one non-empty body; invalid emails quarantine. Delay steps never emit message content and advance to the next ordered step with `delayHours` applied through the campaign send-window calculator. Condition steps quarantine with `unsupported_condition_step` because no branch payload/targets exist in the current schema. Test unordered input, first/last delay, invalid email, condition, weekend/window rollover, and completion.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-sequence-state.test.ts</automated>
  </verify>
  <done>Every step type maps to one explicit action and only `send_email` contains provider-ready content.</done>
</task>

<task type="auto">
  <name>Task 2: Fail closed at activation and processor boundaries</name>
  <files>src/server/routes/outreach/campaigns.ts, src/server/jobs/processOutreachSequences.ts</files>
  <read_first>
    - src/server/routes/outreach/campaigns.ts (activation/start route and step schemas)
    - src/server/jobs/processOutreachSequences.ts (currentStep through claim creation)
    - src/server/lib/outreach-sequence-state.ts
  </read_first>
  <action>
    Before campaign activation, validate that the canonical selected sequence has at least one valid email, no `condition` steps, unique positive ordering, and valid email content. Return 422 with stable issue codes rather than partially activating. In the processor call `resolveSequenceAction` before generating a token or inserting `outreach_emails`: transition delay rows atomically, complete exhausted leads, and quarantine invalid/condition rows by clearing `nextScheduledAt` and logging a structured configuration error. Add a hard assertion in `sendOutreachEmail` call site that the step is `email`.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-sequence-state.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>No non-email step reaches claim creation or `sendOutreachEmail`; invalid campaigns cannot start.</done>
</task>

</tasks>

<verification>
- `npm run test`
- `npm run build`
- `rg -n "sendOutreachEmail" src/server/jobs/processOutreachSequences.ts` and inspect the immediately preceding discriminated-union guard.
</verification>

<success_criteria>
1. Delay rows create no `outreach_emails` record.
2. Condition and malformed email rows are visible configuration failures, never sends.
3. Existing valid email-only sequences remain executable.
</success_criteria>

