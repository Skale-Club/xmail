---
phase: 21-unified-inbox-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 21-02
  - 21-03
files_modified:
  - src/server/lib/unified-inbox/cursor.ts
  - src/server/lib/unified-inbox/queries.ts
  - src/server/routes/outreach/unified-inbox.ts
  - src/server/routes/outreach/index.ts
  - src/server/lib/unified-inbox/__tests__/cursor.test.ts
  - src/server/routes/outreach/__tests__/unified-inbox.db.test.ts
autonomous: true
requirements:
  - UIF-04
  - UIF-05
must_haves:
  truths:
    - "Authorized users can list/filter/search conversations and hydrate one full ordered thread"
    - "Conversation pagination is opaque, stable, max-100, and cannot be reused with a different filter set"
    - "Unread counts/read-state are per user and mutations are idempotent"
    - "Every query starts from authorized organization scope and never leaks existence/content across tenants"
  artifacts:
    - path: "src/server/routes/outreach/unified-inbox.ts"
      provides: "List/detail/unread/read-state API contract"
    - path: "src/server/lib/unified-inbox/cursor.ts"
      provides: "Validated filter-bound keyset cursor codec"
      exports: ["encodeConversationCursor", "decodeConversationCursor"]
    - path: "src/server/lib/unified-inbox/queries.ts"
      provides: "Tenant-first list/detail/unread projections"
  key_links:
    - from: "src/server/routes/outreach/unified-inbox.ts"
      to: "src/server/lib/outreach-access.ts"
      via: "authorize before query construction"
      pattern: "requireOutreachRead"
    - from: "conversation list cursor"
      to: "last_message_at,id ordering"
      via: "keyset predicate and filter fingerprint"
      pattern: "lastMessageAt"
---

<objective>
Expose the tenant-safe read foundation consumed by the Phase 22 Unified Inbox UI.

Purpose: Provide stable list/detail/search/unread contracts without returning full bodies in list payloads or relying on client-side filtering.

Output: Cursor/query services, mounted organization-scoped routes, contract/security tests, and operational error projection.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/21-unified-inbox-foundation/21-CONTEXT.md
@.planning/phases/21-unified-inbox-foundation/21-RESEARCH.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify cursor and route contracts with isolation tests</name>
  <files>src/server/lib/unified-inbox/__tests__/cursor.test.ts, src/server/routes/outreach/__tests__/unified-inbox.db.test.ts</files>
  <read_first>
    - Phase 20 outreach access helper and role tests
    - foundation schema and materialized fixtures
    - src/server/lib/pagination.ts for existing API conventions
    - src/server/routes/outreach/index.ts mount patterns
  </read_first>
  <action>
    Add tests for default/max/oversize limit, malformed/tampered cursor, filter fingerprint mismatch, stable pagination under equal timestamps, unread/status/campaign/account filters, escaped keyword search, empty results, full chronological detail, no bodies in list response, per-user unread count, read/unread idempotency, viewer access, and org A list/detail/read mutation against org B returning 404/403 without existence leaks. Verify credentials/provider tokens/raw unsafe headers are never serialized.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/cursor.test.ts src/server/routes/outreach/__tests__/unified-inbox.db.test.ts</automated>
  </verify>
  <done>Tests define the complete Phase 21 public API and fail before routes exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement keyset queries, projections, search, and unread state</name>
  <files>src/server/lib/unified-inbox/cursor.ts, src/server/lib/unified-inbox/queries.ts</files>
  <read_first>
    - tests from Task 1
    - schema/indexes from Plan 21-01
    - Phase 20 wildcard escaping and Zod pagination patterns
  </read_first>
  <action>
    Create a versioned opaque cursor encoding `lastMessageAt`, `id`, and canonical filter fingerprint; validate all decoded types and reject cross-filter reuse. Query conversations by `organization_id` first, max 100, order `last_message_at DESC,id DESC`, and return preview/attribution/participants/read state only. Use escaped bounded PostgreSQL search; never load/filter all rows in JS.

    Detail queries scope organization and order messages by effective timestamp plus id, returning safe headers and full bodies. Implement unread as `last_inbound_at > read_at`/no row. Read mutation upserts current user's state; marking unread deletes/resets it without affecting other users.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/cursor.test.ts src/server/routes/outreach/__tests__/unified-inbox.db.test.ts</automated>
  </verify>
  <done>Cursor/query tests pass and list projection stays bounded/lightweight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount Unified Inbox routes with canonical tenant authorization</name>
  <files>src/server/routes/outreach/unified-inbox.ts, src/server/routes/outreach/index.ts</files>
  <read_first>
    - query service from Task 2
    - src/server/lib/outreach-access.ts
    - existing outreach router registration and error conventions
    - Phase 19 provider cursor/error fields safe for operational summary
  </read_first>
  <action>
    Implement the four locked endpoints from 21-CONTEXT. Parse organization id and all filters with Zod, call canonical read access before query, return 404 for inaccessible conversation ids, and allow viewers to mutate only their own read state. Include a safe per-account sync status summary (last success, degraded flag, sanitized error category) where useful; never expose provider cursor tokens/delta links or credentials.

    Mount under `/api/outreach/unified-inbox`, add structured request/result counts without content, and keep response DTOs explicit for Phase 22.
  </action>
  <verify>
    <automated>npm run test -- src/server/routes/outreach/__tests__/unified-inbox.db.test.ts src/server/lib/unified-inbox/__tests__/cursor.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>All endpoints pass contract/isolation tests and are registered under outreach auth/rate limits.</done>
</task>

</tasks>

<verification>
- Walk at least three pages with tied timestamps: no duplicate/missing ids.
- Reusing a cursor after changing any filter/search/organization fails 400.
- Org A cannot distinguish missing from org B conversation detail.
- Two users maintain independent read states/unread counts.
- List response has no full message bodies; detail response has complete ordered thread.
- Targeted tests, build, and lint pass.
</verification>

<success_criteria>
1. List/detail/filter/search APIs are bounded, stable, and tenant-safe.
2. Per-user unread/read state is correct and idempotent.
3. Response projections expose useful content without secrets or list over-fetch.
4. Phase 22 has a documented stable API contract to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/21-unified-inbox-foundation/21-04-SUMMARY.md` with endpoint DTOs, cursor version/ordering, filter matrix, read-state semantics, and isolation-test results.
</output>
