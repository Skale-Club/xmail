---
phase: 21-unified-inbox-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/server/lib/unified-inbox/normalize.ts
  - src/server/lib/unified-inbox/attribute.ts
  - src/server/lib/unified-inbox/ingest.ts
  - src/server/jobs/materializeUnifiedInbox.ts
  - src/server/jobs/processReplies.ts
  - src/server/jobs/index.ts
  - src/server/lib/unified-inbox/__tests__/normalize.test.ts
  - src/server/lib/unified-inbox/__tests__/ingest.db.test.ts
  - src/server/jobs/__tests__/materializeUnifiedInbox.db.test.ts
autonomous: true
requirements:
  - UIF-02
  - UIF-03
  - UIF-05
must_haves:
  truths:
    - "One provider-neutral transaction materializes staged events into conversations/messages"
    - "Materialization claims use the Phase 21 status/lease/attempt lifecycle and never overload provider-event processed_at"
    - "Replaying or concurrently consuming an event creates no duplicate message or side effect"
    - "Thread attribution uses scoped Message-ID/References first and a bounded address heuristic last"
    - "DSN and auto-reply classification precedes human-reply counters/follow-up scheduling"
  artifacts:
    - path: "src/server/lib/unified-inbox/ingest.ts"
      provides: "Idempotent transactional event-to-conversation materializer"
      exports: ["materializeProviderEvent"]
    - path: "src/server/lib/unified-inbox/attribute.ts"
      provides: "Tenant/account-scoped thread and outreach attribution"
    - path: "src/server/jobs/materializeUnifiedInbox.ts"
      provides: "Bounded, leased staging consumer"
  key_links:
    - from: "src/server/jobs/materializeUnifiedInbox.ts"
      to: "outreach_provider_events"
      via: "compare-and-swap claim of pending/expired materialization lifecycle rows"
      pattern: "materializeProviderEvent"
    - from: "src/server/lib/unified-inbox/ingest.ts"
      to: "outreach_conversation_messages"
      via: "unique source-key transaction before reply side effects"
      pattern: "onConflict"
---

<objective>
Materialize Phase 19 provider events into full, attributed, idempotent conversations and make that durable message the source of reply context.

Purpose: Existing reply processing mutates counters from headers without storing a conversation/body and can repeat side effects.

Output: Normalizer, attribution service, transactional materializer, bounded job, reply integration, and replay/isolation tests.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/21-unified-inbox-foundation/21-CONTEXT.md
@.planning/phases/21-unified-inbox-foundation/21-RESEARCH.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build normalization, classification, attribution, and replay fixtures</name>
  <files>src/server/lib/unified-inbox/__tests__/normalize.test.ts, src/server/lib/unified-inbox/__tests__/ingest.db.test.ts, src/server/jobs/__tests__/materializeUnifiedInbox.db.test.ts</files>
  <read_first>
    - Phase 19 provider-event schema and ingestion tests
    - src/server/jobs/processReplies.ts matcher/auto-reply logic
    - src/server/jobs/processBounces.ts DSN classifier
    - src/server/lib/mail.ts parsed email shape
    - foundation schema from Plan 21-01
  </read_first>
  <action>
    Add fixtures for bracketed/case-varied Message-IDs, multi-token References, missing ids, duplicate provider replay, concurrent replay, auto-reply, DSN, same email in two organizations, same lead in two accounts, ambiguous address heuristic, and a complete text/html/attachment-metadata reply. Assert match strategy/confidence and no cross-tenant/address-only false attachment. Assert only the first committed human reply increments counters/schedules follow-up/notification. Cover `processed_at` already set while materialization remains pending, concurrent materialization claim, stale lease recovery, bounded attempts, and normalized-message link written only after commit.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/normalize.test.ts src/server/lib/unified-inbox/__tests__/ingest.db.test.ts src/server/jobs/__tests__/materializeUnifiedInbox.db.test.ts</automated>
  </verify>
  <done>Tests fail only on missing normalized materialization behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the idempotent materializer and scoped attribution</name>
  <files>src/server/lib/unified-inbox/normalize.ts, src/server/lib/unified-inbox/attribute.ts, src/server/lib/unified-inbox/ingest.ts</files>
  <read_first>
    - tests from Task 1
    - Phase 20 canonical outreach access/organization conventions
    - outreachEmails/campaignLeads/message-id indexes
    - Phase 19 shared provider event classifier
  </read_first>
  <action>
    Centralize normalization of addresses, Message-ID tokens, references, subject roots, safe headers, previews, and source keys. Validate that provider event organization/account match before any lookup. Resolve conversation/outreach attribution in strict order: In-Reply-To, References, trustworthy provider thread id, then same-org/account known-lead recent-outbound heuristic with a bounded lookback. Preserve unmatched/ambiguous messages as unattributed conversations.

    In one DB transaction, check/insert the unique normalized message, find/create thread, upsert participants, update summary timestamps/preview, and update the claimed provider event to `materialized` with `materialized_at` and normalized-message id. Return `{ inserted, classification, attribution }` only after that transaction commits. Phase 21 never re-invokes Phase 19 reply/bounce/auto-reply counters, notification, or follow-up side effects; their independent `processed_at` lifecycle remains authoritative. Never log bodies/headers/addresses.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/normalize.test.ts src/server/lib/unified-inbox/__tests__/ingest.db.test.ts</automated>
  </verify>
  <done>All normalization, duplicate, concurrent, attribution, and tenant-isolation cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Consume staged events in bounded leases and integrate durable reply context</name>
  <files>src/server/jobs/materializeUnifiedInbox.ts, src/server/jobs/processReplies.ts, src/server/jobs/index.ts</files>
  <read_first>
    - Phase 18 queue lease/retry primitive
    - Phase 19 provider-event lifecycle and job schedule
    - src/server/jobs/index.ts advisory-lock conventions
    - src/server/lib/outreach-followup.ts reply-context consumer
  </read_first>
  <action>
    Add a bounded materialization job using the Phase 18 lease/backoff conventions and the dedicated Phase 21 provider-event materialization fields. Claim deterministic pending/expired rows with one compare-and-swap statement (`FOR UPDATE SKIP LOCKED` or equivalent returning claim), increment attempts, and call the service that atomically commits normalized message plus materialized timestamp/link. There is no separate success acknowledgement window. On transaction failure, retry sanitized errors with backoff and move max-attempt poison events to failed. Never filter, claim, or update Phase 19 `processed_at`. Do not add another provider polling loop.

    Reconcile `lastReplyText`/future AI context from the persisted normalized message without incrementing reply counters or scheduling another follow-up; Phase 19 owns those first-processing effects. Schedule the materializer after provider staging with a named advisory lock and safe overlap behavior.
  </action>
  <verify>
    <automated>npm run test -- src/server/jobs/__tests__/materializeUnifiedInbox.db.test.ts src/server/lib/unified-inbox/__tests__/ingest.db.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>Staged events materialize restart-safely, reply context contains persisted body text, duplicates have no repeated side effects, and build/lint pass.</done>
</task>

</tasks>

<verification>
- Replay and concurrent replay return the same normalized message id and one side effect.
- Header/thread matches outrank bounded address heuristic; ambiguity remains unattributed.
- DSNs never appear as human replies and auto-replies do not stop/increment as humans.
- A crash after message commit but before job acknowledgement is safe on retry.
- Provider classification `processed_at` and inbox materialization status can progress independently without starvation or duplicate work.
- Logs contain ids/counters only, no body or credential content.
</verification>

<success_criteria>
1. Every provider event can become one durable normalized message/conversation.
2. Attribution is deterministic, confidence-tagged, bounded, and tenant-safe.
3. Reply context and side effects derive from persisted first-ingestion state.
4. The consumer is restart-safe and operationally observable.
</success_criteria>

<output>
After completion, create `.planning/phases/21-unified-inbox-foundation/21-02-SUMMARY.md` with normalization rules, match precedence, event lifecycle, job bounds, and side-effect idempotency evidence.
</output>
