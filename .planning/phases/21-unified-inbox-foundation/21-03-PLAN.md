---
phase: 21-unified-inbox-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - src/server/jobs/processReplies.ts
  - src/server/jobs/processBounces.ts
  - src/server/lib/outlook.ts
  - src/server/lib/unified-inbox/providers/native.ts
  - src/server/lib/unified-inbox/providers/imap.ts
  - src/server/lib/unified-inbox/providers/outlook.ts
  - src/server/lib/unified-inbox/outbound.ts
  - src/server/lib/outreach-dispatch.ts
  - src/server/jobs/backfillUnifiedInbox.ts
  - src/server/jobs/index.ts
  - src/server/lib/unified-inbox/__tests__/providers.db.test.ts
  - src/server/lib/unified-inbox/__tests__/outbound.db.test.ts
  - src/server/jobs/__tests__/backfillUnifiedInbox.db.test.ts
autonomous: true
requirements:
  - UIF-02
  - UIF-03
  - UIF-05
must_haves:
  truths:
    - "Native, IMAP, and Outlook staged events contain equivalent full normalized message fields"
    - "Attachment data stored in the foundation is metadata only; no arbitrary binary is written to PostgreSQL"
    - "Provider cursor recovery is bounded/resumable and never advances beyond an unpersisted event"
    - "Recent historical outreach replies can be backfilled restart-safely without duplicate effects"
    - "Every durably sent outreach_email becomes one outbound conversation message with source_key outreach-email:<id>"
    - "An inbound reply referencing a live-hooked or backfilled outbound message joins the same conversation"
  artifacts:
    - path: "src/server/lib/unified-inbox/providers/imap.ts"
      provides: "Raw MIME to Phase 19 provider-event adapter"
    - path: "src/server/lib/unified-inbox/providers/outlook.ts"
      provides: "Graph delta message to provider-event adapter"
    - path: "src/server/jobs/backfillUnifiedInbox.ts"
      provides: "Bounded anti-join backfill for sent outreach emails and staged inbound events"
    - path: "src/server/lib/unified-inbox/outbound.ts"
      provides: "Idempotent outreach_email to outbound conversation-message materializer"
      exports: ["materializeOutboundEmail"]
  key_links:
    - from: "provider adapters"
      to: "outreach_provider_events"
      via: "same normalized fields/source identity across native, IMAP, Graph"
      pattern: "attachments"
    - from: "src/server/jobs/backfillUnifiedInbox.ts"
      to: "materializeProviderEvent"
      via: "reuse live idempotent path for historical rows"
      pattern: "materializeProviderEvent"
    - from: "src/server/lib/outreach-dispatch.ts"
      to: "src/server/lib/unified-inbox/outbound.ts"
      via: "non-fatal post-dispatch materialization hook backed by anti-join repair"
      pattern: "materializeOutboundEmail"
---

<objective>
Close provider completeness gaps, materialize outbound outreach history, and provide a safe recent-history rollout path on top of Phase 19 staging and Plan 21-02 materialization.

Purpose: Unified Inbox is only credible if native, IMAP, Outlook, and every successful outbound outreach email yield equivalent threaded history.

Output: Provider adapters/fixtures, outbound materializer/hook, bounded cursor recovery validation, and restartable recent backfill.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/21-unified-inbox-foundation/21-CONTEXT.md
@.planning/phases/21-unified-inbox-foundation/21-RESEARCH.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider-equivalence and recovery fixtures</name>
  <files>src/server/lib/unified-inbox/__tests__/providers.db.test.ts, src/server/lib/unified-inbox/__tests__/outbound.db.test.ts, src/server/jobs/__tests__/backfillUnifiedInbox.db.test.ts</files>
  <read_first>
    - Phase 19 native/IMAP/Graph ingest implementations and tests
    - src/server/lib/mail-sync.ts complete MIME parsing
    - src/server/lib/outlook.ts Graph/token helpers
    - Phase 19 provider cursor schema
    - src/server/lib/outreach-dispatch.ts durable sent-state update and return contract
    - src/server/jobs/processOutreachSequences.ts, src/server/jobs/processFollowUps.ts, and src/server/routes/outreach/send-message.ts (read-only confirmation that entrypoints call the dispatcher and contain no inbox hook)
  </read_first>
  <action>
    Add equivalent native row, raw IMAP MIME, and Graph delta fixtures with text/html, ids/references, to/cc, timestamp, and inline/file attachment metadata. Assert each maps to the same provider-neutral shape and source identity. Cover IMAP UIDVALIDITY reset, Graph expired delta token, partial batch failure, malformed poison message, cursor retry, and backfill replay. Assert cursor does not skip a failed-to-stage message and binary attachment content is absent.

    Add outbound fixtures for campaign, manual, and agentic sends persisted as successful `outreach_emails`. Assert deterministic `outreach-email:<id>` source key, replay/concurrent dedupe, message-id/thread headers and rendered bodies preserved, failed/queued sends excluded, a referencing inbound event joins the outbound conversation, and outbound/inbound replay changes neither row counts nor side effects.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/providers.db.test.ts src/server/lib/unified-inbox/__tests__/outbound.db.test.ts src/server/jobs/__tests__/backfillUnifiedInbox.db.test.ts</automated>
  </verify>
  <done>Tests expose any remaining provider-field/cursor differences after Phase 19.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize complete native, IMAP, and Graph event payloads</name>
  <files>src/server/jobs/processReplies.ts, src/server/jobs/processBounces.ts, src/server/lib/outlook.ts, src/server/lib/unified-inbox/providers/native.ts, src/server/lib/unified-inbox/providers/imap.ts, src/server/lib/unified-inbox/providers/outlook.ts</files>
  <read_first>
    - provider tests from Task 1
    - Phase 19 staging insert API and cursor rules
    - `mailparser` ParsedMail fields already used by mail-sync
    - Outlook OAuth scopes and Graph send client
  </read_first>
  <action>
    Adapt each Phase 19 ingest path to a shared complete event-input contract. Native maps `mail_messages`; IMAP fetches/parses full raw MIME only for the bounded claimed UID batch; Graph requests body/header/recipient/attachment metadata and preserves immutable id/thread id. Store only allowlisted headers and attachment descriptors (`providerId`, sanitized filename, MIME type, size, inline, contentId), never blobs.

    Reuse Phase 19 cursor leases/retry/delta/UID state. On insufficient Outlook read scopes, mark account sync degraded with reconnect guidance; do not report success. Advance provider cursor only after durable staging, with bounded resync on UIDVALIDITY/delta expiry.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/providers.db.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>All three providers satisfy the same message contract and recovery tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Materialize outbound sends live and repair all history with bounded anti-join backfill</name>
  <files>src/server/lib/unified-inbox/outbound.ts, src/server/lib/outreach-dispatch.ts, src/server/jobs/backfillUnifiedInbox.ts, src/server/lib/unified-inbox/__tests__/outbound.db.test.ts, src/server/jobs/__tests__/backfillUnifiedInbox.db.test.ts, src/server/jobs/index.ts</files>
  <read_first>
    - materializer from Plan 21-02
    - existing native mail_messages indexes and Phase 19 provider events
    - outreachEmails schema and Phase 18/19 shared send persistence contract
    - campaign/manual/agentic entrypoints (read only: verify all delegate to outreach-dispatch and do not own materialization hooks)
    - job lock/lease and structured logging conventions
  </read_first>
  <action>
    Implement `materializeOutboundEmail(outreachEmailId)`: load a tenant/account-scoped, durably successful row with rendered subject/bodies and Message-ID; derive `outreach-email:<id>`; find/create the campaign-lead conversation; insert direction=outbound and participants idempotently; and update the summary in one transaction. It never dispatches email and may be safely retried.

    Invoke it exclusively inside `src/server/lib/outreach-dispatch.ts`, immediately after provider success and the `outreach_emails.sent_at/status/message_id` update commits durably. Do not add or retain hook calls in campaign, manual, agentic, route, or job entrypoints. Treat materialization failure as a structured repairable error, not a reason to report the already-dispatched email as unsent or resend it.

    Implement an explicit bounded backfill for (a) successful `outreach_emails` missing `source_key=outreach-email:<id>` and (b) staged provider events missing normalized-message links. Use tenant-leading `NOT EXISTS` anti-joins, deterministic `(sent_at,id)` / `(received_at,id)` ordering, configurable lookback/batch limits, and the live materializers. Because completed rows disappear from the anti-join, interruption/restart converges without a separate checkpoint or full-table repeat. Run outbound batches before inbound batches so historical References can attach. Prefer an operator-invoked command/flag; report scanned/inserted/duplicate/unmatched/error counts and mutate no counters directly.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/outbound.db.test.ts src/server/jobs/__tests__/backfillUnifiedInbox.db.test.ts src/server/lib/unified-inbox/__tests__/providers.db.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>Live successful sends appear once, inbound replies thread to them, and interrupted outbound-first anti-join backfill converges without duplicates or resend.</done>
</task>

</tasks>

<verification>
- Provider fixture snapshots share all required normalized fields.
- IMAP/Graph cursor failure cases replay the unpersisted message and continue after recovery.
- Outlook accounts lacking read scope show degraded/reconnect state.
- Backfill can be interrupted twice and converges to the same row counts as one run.
- Live and backfilled outbound rows use identical source keys; an inbound References/In-Reply-To fixture attaches to the same conversation.
- A post-dispatch materialization failure never triggers a provider resend and is repaired by the next bounded backfill.
- No attachment content/body enters structured logs.
</verification>

<success_criteria>
1. Native, IMAP, and Outlook messages are equivalent at the materializer boundary.
2. Provider recovery is bounded and lossless.
3. Attachments are metadata-only in PostgreSQL.
4. Successful outbound outreach history is materialized live and recent inbound/outbound history can be populated without downtime, resend, or duplicate side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/21-unified-inbox-foundation/21-03-SUMMARY.md` with provider field matrix, cursor recovery behavior, OAuth scope changes, and backfill runbook.
</output>
