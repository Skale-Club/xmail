---
phase: 21-unified-inbox-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - supabase/migrations/041_unified_inbox_foundation.sql
  - src/server/lib/unified-inbox/types.ts
  - src/server/lib/unified-inbox/__tests__/schema.db.test.ts
autonomous: true
requirements:
  - UIF-01
must_haves:
  truths:
    - "Conversation, normalized message, participant, and per-user read-state rows are explicitly organization scoped"
    - "Provider staging/cursors from Phase 19 remain the single provider-ingestion boundary"
    - "Inbox materialization claims have status/lease/attempt/error/message-link state independent from provider-event processed_at"
    - "Provider replay and duplicate RFC Message-IDs cannot create duplicate normalized messages"
    - "Tenant-leading indexes support list, thread, attribution, unread, and source-id lookups"
  artifacts:
    - path: "supabase/migrations/041_unified_inbox_foundation.sql"
      provides: "Hand-written Unified Inbox tables, constraints, indexes, and defense-in-depth RLS"
    - path: "src/db/schema.ts"
      provides: "Drizzle declarations/types matching the SQL source of truth"
    - path: "src/server/lib/unified-inbox/types.ts"
      provides: "Provider-neutral conversation/message/read-state contracts"
  key_links:
    - from: "outreach_conversation_messages.source_key"
      to: "outreach_provider_events"
      via: "one normalized message per organization/account/provider event identity"
      pattern: "sourceKey"
    - from: "outreach_provider_events.materialization_status"
      to: "outreach_conversation_messages.id"
      via: "leased compare-and-swap lifecycle completed only after normalized-message commit"
      pattern: "materializationStatus"
    - from: "outreach_conversation_reads"
      to: "users and outreach_conversations"
      via: "per-user read state inside same organization"
      pattern: "conversationReads"
---

<objective>
Create the durable, tenant-safe relational foundation for Unified Inbox without duplicating Phase 19 provider staging/cursors.

Purpose: Conversations need immutable normalized messages, attribution, participants, and user-specific unread state before ingestion or APIs can be correct.

Output: Migration 041 (subject to revalidation), matching Drizzle schema/types, and constraint/index tests.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/21-unified-inbox-foundation/21-CONTEXT.md
@.planning/phases/21-unified-inbox-foundation/21-RESEARCH.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define schema invariants with disposable-database tests</name>
  <files>src/server/lib/unified-inbox/__tests__/schema.db.test.ts, src/server/lib/unified-inbox/types.ts</files>
  <read_first>
    - Phase 19 summary/schema for outreach_provider_events and outreach_provider_cursors
    - src/db/schema.ts organizations/users/emailAccounts/leads/campaigns/outreachEmails
    - supabase/migrations/039 provider staging migration after it lands
    - Phase 18 test database setup
  </read_first>
  <action>
    Specify types and failing database assertions for organization/account ownership, unique source identity, non-unique RFC Message-ID, conversation thread uniqueness, participant uniqueness, read-state uniqueness, required direction/provider/classification values, cascades/restrict behavior, and tenant-leading indexes. Include negative cross-organization foreign-key scenarios; composite tenant FKs or trigger checks must prevent an org A conversation linking an org B account/lead/campaign/user state.

    Also assert the Phase 21 extension on `outreach_provider_events`: `materialization_status` defaults pending; lease/attempt/error/materialized-at/normalized-message-link fields transition independently from Phase 19 `processed_at`; only one worker can change pending or expired-processing to processing; and a materialized row links to exactly one same-tenant conversation message. Include stale lease reclaim and max-attempt terminal failure fixtures. This `.db.test.ts` obtains only the Phase 18 guarded disposable URL and passes it explicitly to DB/migration helpers; it never reads or falls back to application `DATABASE_URL`.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/schema.db.test.ts</automated>
  </verify>
  <done>Tests compile and fail because the four foundation tables/constraints do not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Write and apply the hand-authored foundation migration</name>
  <files>supabase/migrations/041_unified_inbox_foundation.sql</files>
  <read_first>
    - schema tests and type contract from Task 1
    - current last migration and Phase 19 provider staging migration
    - supabase/migrations/020_consolidate_rls.sql policy style
    - AGENTS.md schema and migration workflow
  </read_first>
  <action>
    Re-list migrations immediately before writing. `041` is reserved after 038–040 but must be renumbered if the tree changed.

    Create `outreach_conversations`, `outreach_conversation_messages`, `outreach_conversation_participants`, and `outreach_conversation_reads` with UUID keys, explicit organization ids, timestamps, checks, composite/trigger tenant consistency, and indexes described in 21-RESEARCH. Link messages to the Phase 19 provider event by immutable source identity or event id, while preserving a unique `(organization_id,email_account_id,source_key)`. Add optional attribution FKs and last-message relationship safely (deferred FK or follow-up ALTER to avoid creation cycle).

    Extend `outreach_provider_events` with a dedicated materialization lifecycle: status enum/check (`pending|processing|materialized|failed`), lease-until, attempts, sanitized last error, materialized-at, and nullable unique normalized-message FK. Add a partial claim index over pending/expired processing rows and constraints tying `materialized` to timestamp/message link. This lifecycle must not read or overwrite Phase 19 `processed_at`; that field retains provider classification/domain-side-effect semantics.

    Enable RLS and add organization-membership policies as defense-in-depth, while noting application JS remains authoritative. Do not backfill historical mail in the migration. Do not create another provider cursor table. Never use Drizzle generation/db:push.

    Extend `schema.db.test.ts` to apply migration 041 twice using the Phase 18 migration helper with its protected harness URL passed as an explicit function argument. No test/automated command may invoke `psql` or use `DATABASE_URL`. For manual production rollout only, document the PowerShell runbook command: `psql $env:DATABASE_URL -v ON_ERROR_STOP=1 -f supabase/migrations/041_unified_inbox_foundation.sql`.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/unified-inbox/__tests__/schema.db.test.ts</automated>
  </verify>
  <done>Migration applies cleanly, schema tests pass, and invalid cross-tenant/source duplicates are rejected.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror SQL in Drizzle and prove zero new drift</name>
  <files>src/db/schema.ts, src/server/lib/unified-inbox/types.ts</files>
  <read_first>
    - applied SQL from Task 2
    - existing Drizzle relation/type/export conventions
    - Phase 18 protected-harness schema assertion helpers
  </read_first>
  <action>
    Add enums/tables/index declarations, relations, insert/select schemas, and inferred types matching the applied SQL exactly. Extend (do not duplicate) Phase 19 provider-event declarations with the materialization lifecycle fields/index/link; leave provider cursors owned by Phase 19. Update explicit domain DTOs only where inferred JSON types are too broad. Do not imply a Drizzle declaration applies database constraints.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run test -- src/server/lib/unified-inbox/__tests__/schema.db.test.ts</automated>
  </verify>
  <done>Drizzle compiles, protected-harness schema assertions show no new drift, and foundation constraints remain covered.</done>
</task>

</tasks>

<verification>
- Two replays of the same provider event/source key produce at most one normalized-message row.
- Phase 19 `processed_at` can be populated while materialization is pending (and vice versa); neither lifecycle claims the other.
- Two concurrent claims return one winner; an expired lease is reclaimable; materialized rows link one normalized message.
- Duplicate RFC Message-ID from distinct scoped sources is allowed and indexable.
- Cross-tenant account/lead/campaign/read-state links fail.
- Conversation list and message-order EXPLAIN plans use tenant-leading indexes on representative data.
- Migration, schema tests, audit, build, and lint pass.
</verification>

<success_criteria>
1. Four foundation tables exist with explicit organization ownership.
2. Phase 19 staging/cursors are reused, with a separate explicit materialization lifecycle added to provider events.
3. Source identity and tenant consistency are database-enforced.
4. SQL and TypeScript schema stay synchronized.
</success_criteria>

<output>
After completion, create `.planning/phases/21-unified-inbox-foundation/21-01-SUMMARY.md` with actual migration number, table/index inventory, RLS caveat, and Phase 19 staging linkage.
</output>
