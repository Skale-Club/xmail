---
phase: 22-unified-inbox-operator-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/042_unified_inbox_operator_workflows.sql
  - src/db/schema.ts
  - src/server/routes/outreach/unified-inbox.ts
  - src/server/lib/inbox-operator.ts
  - src/server/lib/inbox-attachments.ts
  - src/server/lib/inbox-command-dispatch.ts
  - src/server/jobs/processInboxCommands.ts
  - src/server/jobs/index.ts
  - src/server/routes/outreach/__tests__/inbox-operator-migration.db.test.ts
  - src/server/routes/outreach/__tests__/inbox-operator.test.ts
autonomous: true
requirements: [UIX-04, UIX-05]
must_haves:
  truths:
    - "Labels, archive state, reminders, snippets, and send commands are durable, organization-scoped records rather than browser state."
    - "Bulk operations have a hard server-side item limit and cannot mutate another organization's conversations."
    - "Scheduled commands and reminders use leases/idempotency and recover after a Node process restart."
    - "Migration numbering is revalidated at execution start and no Drizzle generator/push command is used."
  artifacts:
    - path: "supabase/migrations/042_unified_inbox_operator_workflows.sql"
      provides: "Operator workflow tables/indexes/RLS defense-in-depth; filename number is provisional"
      contains: "inbox_send_commands"
    - path: "src/server/lib/inbox-operator.ts"
      provides: "Tenant-scoped operator mutations and bounded bulk service"
      exports: ["bulkUpdateConversations", "createInboxReminder", "createInboxSendCommand"]
    - path: "src/server/lib/inbox-attachments.ts"
      provides: "Private Supabase Storage signed-intent/finalize/download/orphan lifecycle service backed by inbox_attachments"
    - path: "src/server/jobs/processInboxCommands.ts"
      provides: "Advisory-locked lease processor for scheduled commands/reminders"
  key_links:
    - from: "src/server/routes/outreach/unified-inbox.ts"
      to: "src/server/lib/outreach-access.ts"
      via: "requireOutreachRead/requireOutreachWrite plus organization predicate on every query/mutation"
      pattern: "requireOutreach(Read|Write)"
    - from: "src/server/jobs/processInboxCommands.ts"
      to: "src/server/lib/inbox-command-dispatch.ts"
      via: "due commands pass only through the lease-aware executeInboxSendCommand entrypoint"
      pattern: "executeInboxSendCommand"
---

<objective>
Create the durable operator workflow layer that the Unified Inbox UI will call: labels, archive/read status mutations, bounded bulk actions, reminders, snippets, and restart-safe send commands. This plan does not build the page or send composer; it establishes the database/API/job contract they consume.
</objective>

<execution_context>
@.planning/phases/22-unified-inbox-operator-experience/22-CONTEXT.md
@.planning/phases/22-unified-inbox-operator-experience/22-RESEARCH.md
@.planning/phases/21-unified-inbox-foundation/*-SUMMARY.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile Phase 21 and add the hand-written operator workflow migration</name>
  <read_first>
    - AGENTS.md (schema/migration workflow and JS-side tenant authorization)
    - .planning/phases/21-unified-inbox-foundation/*-SUMMARY.md (final table names, keys, attachment storage, and API contract)
    - src/db/schema.ts (Phase 21 conversation/message declarations and current outreach tables)
    - supabase/migrations/ (list filenames to determine the next free integer)
    - supabase/migrations/037_reconcile_schema_only_unique_indexes.sql (current idempotent style reference)
  </read_first>
  <files>supabase/migrations/042_unified_inbox_operator_workflows.sql, src/db/schema.ts, src/server/routes/outreach/__tests__/inbox-operator-migration.db.test.ts</files>
  <action>
    First list all migration filenames. `042` is provisional: confirm it is the next unused sequential integer, and rename the migration/update every later reference if it is not. Never run `drizzle-kit generate`, `db:generate`, or `db:push`.

    Extend (do not duplicate) Phase 21's model with organization-scoped `inbox_labels`, conversation-label join, `inbox_reminders`, `inbox_snippets`, and `inbox_send_commands`. If Phase 21 already supplied any item, reuse it and omit duplicate DDL. Send commands must include actor user, organization/conversation/source-message/account, mode (`reply|reply_all|forward`), explicit recipient snapshots, subject/body, attachment references, status, scheduled/due time, lease token/expiry, bounded attempts, last error/policy code, stable idempotency key, resulting unified message/outreach-email IDs, and timestamps. Add unique `(organization_id,idempotency_key)` plus due/lease and conversation/status indexes. Add cascade/restrict behavior deliberately so deleting a label cannot delete a conversation and audit records survive normal UI actions.

    Also create tenant-owned `inbox_attachments` with lifecycle/size constraints. Idempotently configure one private Supabase Storage bucket; object paths are organization/command scoped and never public. Enable RLS and add defense-in-depth organization-member policies consistent with current migrations, while documenting that workers/routes still enforce scope in JS. Mirror final columns/enums/index declarations in `src/db/schema.ts`.

    Add `inbox-operator-migration.db.test.ts`: obtain only the Phase 18 protected disposable PostgreSQL URL, pass it explicitly to the migration helper, apply migration 042 twice, and assert workflow/attachment tables, tenant keys, leases, bounds, indexes, bucket privacy, and schema-mirror names. Never read or fall back to application `DATABASE_URL`. Keep `psql $env:DATABASE_URL -v ON_ERROR_STOP=1 -f supabase/migrations/042_unified_inbox_operator_workflows.sql` only in an explicitly marked manual production runbook.
  </action>
  <verify>
    <automated>npm run test -- src/server/routes/outreach/__tests__/inbox-operator-migration.db.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Final migration number is free, SQL is idempotent where practical, schema mirror compiles, uniqueness/lease/bounds/tenant indexes exist, and no generated migration artifacts appear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tenant-safe labels, reminders, snippets, archive, and bounded bulk APIs</name>
  <read_first>
    - src/server/routes/outreach/unified-inbox.ts (Phase 21 router to extend; never create a parallel router)
    - src/server/lib/outreach-access.ts (`requireOutreachRead`/`requireOutreachWrite` canonical helpers)
    - .planning/phases/21-unified-inbox-foundation/*-SUMMARY.md (conversation ownership and route shapes)
    - src/server/routes/outreach/settings.ts (Zod/error conventions)
    - src/server/routes/notifications.ts (authenticated-user extraction pattern)
    - src/server/test/ or test configuration created by Phase 18 (final harness conventions)
  </read_first>
  <files>src/server/lib/inbox-operator.ts, src/server/routes/outreach/unified-inbox.ts, src/server/routes/outreach/__tests__/inbox-operator.test.ts</files>
  <action>
    Extend only Phase 21's `unified-inbox.ts` router; do not create another router. Every read uses `requireOutreachRead`, every mutation uses `requireOutreachWrite`, and every underlying query/mutation includes the verified organization predicate. Extend its list query with label and reminder-state filters while preserving the opaque cursor/max-100 contract.

    Extend Phase 21's inbox router rather than starting a second router. Add service functions and Zod-validated endpoints for label CRUD/attach/detach, explicit conversation read/status mutation, bulk read/status/label with a hard maximum of 100 IDs, reminder CRUD/due summary, and snippet CRUD. The server derives user ID from authenticated request state and verifies both organization membership and conversation/resource organization on every call. `viewer` remains read-only; member/admin can mutate per the finalized Phase 20 access contract.

    Bulk mutations must use a transaction, report `matched/updated/skipped`, reject duplicate IDs, and never broaden a partial/empty filter to the full organization. Mutations return updated resource/version fields for optimistic-cache reconciliation. Label name uniqueness is case-insensitive within an organization. Reminder ownership is explicit: organization members may see conversation reminders according to the chosen contract, while only creator/admin may edit another user's reminder. Snippet content is plain/rich text only—no scriptable HTML.

    Add tests that use two organizations and cover list/mutation isolation, viewer denial, invalid organization/conversation combinations, bulk limit/empty input, label uniqueness, reminder ownership, idempotent attach/detach, and error payloads.
  </action>
  <verify>
    <automated>npm run test -- src/server/routes/outreach/__tests__/inbox-operator.test.ts && npm run build && npm run lint</automated>
  </verify>
  <done>All operator endpoints are mounted under the Phase 21 inbox router, two-tenant tests pass, and no route trusts a resource ID without organization ownership verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement durable command/reminder claiming and restart recovery</name>
  <read_first>
    - src/server/jobs/index.ts (cron registration and shutdown model)
    - src/server/lib/cron-lock.ts (advisory-lock pattern)
    - src/server/jobs/processOutreachSequences.ts (bounded claim/lease conventions after Phase 18)
    - src/server/lib/outreach-dispatch.ts (single dispatch entry and idempotency contract from Phases 18–19)
    - src/server/lib/outreach-delivery-policy.ts (policy denial/retryAt contract)
    - src/server/lib/inbox-operator.ts (send-command/reminder service from Task 2)
  </read_first>
  <files>src/server/lib/inbox-command-dispatch.ts, src/server/jobs/processInboxCommands.ts, src/server/jobs/index.ts, src/server/routes/outreach/__tests__/inbox-operator.test.ts</files>
  <action>
    Implement/export the single lease-aware `executeInboxSendCommand` executor. It revalidates organization ownership, evaluates policy, and is the only inbox/AI module allowed to call `dispatchOutreachMessage`. The worker below must hand claimed leases to it rather than dispatching directly.

    Add an advisory-locked worker that claims a deterministic bounded batch of due `inbox_send_commands` with lease token/expiry and attempts. It must recover expired leases, skip/cancel stale commands safely, and call only `executeInboxSendCommand` with the claimed lease and stable idempotency key. Policy denials with `retryAt` reschedule without losing the draft; terminal denials/failures remain inspectable. Ambiguous provider outcomes follow held/manual-review semantics and are never blindly resent.

    The same tick (or a separate small advisory-locked function in the file) marks due reminders and creates deduplicated `user_notifications`; it never sends email just because a reminder is due. Register the worker in `src/server/jobs/index.ts` at a one-minute cadence without overlapping runs.

    Test claim concurrency, expired-lease recovery, duplicate tick idempotency, recoverable versus terminal policy denial, ambiguous held outcome, command cancellation race, reminder deduplication, and organization-scoped notification targets. Use fake clocks and provider/dispatcher fakes; no external network calls.
  </action>
  <verify>
    <automated>npm run test -- src/server/routes/outreach/__tests__/inbox-operator.test.ts && npm run build</automated>
  </verify>
  <done>A scheduled command survives restart, dispatches at most once for its idempotency key, preserves actionable failure state, and due reminders notify once.</done>
</task>

</tasks>

<verification>
1. `npm run test -- src/server/routes/outreach/__tests__/inbox-operator-migration.db.test.ts` applies the finalized migration twice through the protected disposable-database harness.
2. `npm run test -- src/server/routes/outreach/__tests__/inbox-operator.test.ts` passes tenant, bulk, lease, idempotency, and reminder cases.
3. `npm run build` and `npm run lint` pass.
4. Static search confirms routes/jobs/AI never call `dispatchOutreachMessage`; only `executeInboxSendCommand` does, and no provider adapter is imported in the operator route/job.
</verification>

<success_criteria>
- UI workflow state is durable and tenant-safe.
- Bulk operations are bounded and transactional.
- Scheduled work is lease-based, idempotent, and restart-safe.
- Phase 22 UI plans have a stable typed server contract.
</success_criteria>

<output>Create `22-01-SUMMARY.md` with final migration number, table/API contract, worker cadence, test command, and any Phase 21 name reconciliation.</output>
