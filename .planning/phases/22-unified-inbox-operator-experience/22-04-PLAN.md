---
phase: 22-unified-inbox-operator-experience
plan: 04
type: execute
wave: 4
depends_on: [22-03]
files_modified:
  - src/server/lib/inbox-command-dispatch.ts
  - src/server/routes/outreach/unified-inbox.ts
  - src/server/lib/inbox-command-dispatch.test.ts
  - src/lib/unified-inbox-api.ts
  - src/hooks/useUnifiedInbox.ts
  - src/components/outreach/inbox/ConversationComposer.tsx
  - src/components/outreach/inbox/ConversationThread.tsx
  - src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx
autonomous: true
requirements: [UIX-03, UIX-05]
must_haves:
  truths:
    - "Reply/reply-all/forward recipients and RFC threading headers are resolved/validated server-side from persisted messages."
    - "Every immediate or scheduled send is a durable command handed to the single lease-aware executeInboxSendCommand executor; routes never dispatch directly."
    - "Attachments are bounded, organization-owned, and never base64 JSON blobs."
    - "Policy denial preserves the draft and exposes a safe retry/defer result."
  artifacts:
    - path: "src/server/lib/inbox-command-dispatch.ts"
      provides: "Conversation-aware command validation and dispatch adapter"
    - path: "src/components/outreach/inbox/ConversationComposer.tsx"
      provides: "Reply/reply-all/forward editor with snippets, attachments, and schedule"
  key_links:
    - from: "inbox-command-dispatch.ts"
      to: "src/server/lib/outreach-dispatch.ts"
      via: "executeInboxSendCommand owns policy evaluation and the sole low-level dispatch call"
      pattern: "executeInboxSendCommand"
---

<objective>Make the Unified Inbox replyable across supported providers using durable commands and the shared delivery-policy/dispatch path, including attachments, snippets, and scheduled replies.</objective>

<execution_context>
@22-CONTEXT.md
@22-UI-SPEC.md
@.planning/phases/22-unified-inbox-operator-experience/22-01-SUMMARY.md
@src/server/lib/outreach-dispatch.ts
@src/server/lib/outreach-delivery-policy.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resolve conversation send commands and dispatch through the shared gate</name>
  <read_first>
    - Phase 21 summaries (message participants, provider IDs, attachments)
    - src/server/lib/outreach-dispatch.ts and outreach-delivery-policy.ts
    - src/server/lib/outreach-sender.ts (thread header/provider adapter behavior only)
    - 22-01-SUMMARY.md (send command schema and endpoints)
  </read_first>
  <files>src/server/lib/inbox-command-dispatch.ts, src/server/routes/outreach/unified-inbox.ts, src/server/lib/inbox-command-dispatch.test.ts</files>
  <!-- Extend only unified-inbox.ts. Reads use requireOutreachRead; mutations use requireOutreachWrite; every service query/mutation includes verified organization scope. Routes create/hand off durable commands to executeInboxSendCommand and never call the low-level dispatcher. -->
  <action>Implement server-side command creation/validation for reply, reply-all, and forward. Resolve organization/conversation/source message and eligible account; derive reply recipients while excluding the sending account and deduplicating case-insensitively; require explicit forward recipients; build `In-Reply-To` and ordered `References` from persisted normalized headers with bounded length. Never trust client-supplied provider IDs or hidden recipients. Immediate commands are handed to `executeInboxSendCommand`; scheduled commands stay queued for the same executor via the 22-01 worker. Only that executor evaluates policy and calls the low-level dispatcher. Persist resulting unified outbound message/outreach-attempt links and command state transactionally where possible. Test all modes, injection, cross-tenant IDs, missing headers, idempotency, policy denial/retryAt, and held outcome.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-command-dispatch.test.ts && npm run build</automated></verify>
  <done>All modes produce correct recipients/thread headers and no route/provider bypass exists.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add bounded attachment upload/download ownership</name>
  <read_first>
    - Phase 21 summary for attachment storage abstraction/metadata
    - src/server/routes/mail/send.ts and src/components/mail/ComposeDialog.tsx (existing multipart conventions)
    - src/lib/api.ts (`fetchWithAuth` FormData behavior)
  </read_first>
  <files>src/server/routes/outreach/unified-inbox.ts, src/server/lib/inbox-attachments.ts, src/server/lib/inbox-command-dispatch.ts, src/server/lib/inbox-command-dispatch.test.ts, src/lib/unified-inbox-api.ts</files>
  <action>Implement migration-042 `inbox_attachments` with the single private Supabase Storage bucket. A `requireOutreachWrite` create-intent validates filename/MIME/declared bytes/count/aggregate and returns a short-lived signed upload token; the browser streams directly to Storage. Finalize verifies object path, actual bytes/type and organization/command ownership before `ready`. `requireOutreachRead` download returns only a short-lived signed URL. Delete/cancel removes row+object, and a lifecycle job removes expired intents/orphan rows and objects. No public URL, base64 JSON, unbounded buffering, or caller-selected path. Executor resolves only ready attachments owned by command/org. Test actual-vs-declared mismatch, limits/type, expired token, path injection, cross-tenant IDs, download auth, cancel and orphan cleanup.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-command-dispatch.test.ts && npm run build && npm run lint</automated></verify>
  <done>Attachment content is bounded, owned, and provider-dispatchable without credential leakage.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Build composer, schedule, snippet, and command-state UI</name>
  <read_first>
    - 22-UI-SPEC.md (composer/mobile/policy feedback contract)
    - src/components/mail/ComposeDialog.tsx and RichTextEditor.tsx (behavior reference, not state reuse)
    - 22-01-SUMMARY.md (snippet/schedule APIs)
    - src/components/outreach/inbox/ConversationThread.tsx
  </read_first>
  <files>src/lib/unified-inbox-api.ts, src/hooks/useUnifiedInbox.ts, src/components/outreach/inbox/ConversationComposer.tsx, src/components/outreach/inbox/ConversationThread.tsx, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>Create a controlled thread composer for the three modes. Show resolved From/To/Cc/subject before send, insert organization snippets, upload/remove bounded attachments, choose immediate/scheduled time with organization timezone, and preserve body on any failure. Submit with one idempotency key per user intent and disable duplicate clicks. Render durable command states in thread and allow cancel/retry only when server state permits. On mobile use a full-height stage and confirm unsaved exit. Display specific policy denial codes with next action; never silently switch accounts or send later unless operator selected scheduling/queueing. Test modes, duplicate submit, schedule timezone, attachment failures, unsaved exit, denial, cancellation race, and success reconciliation.</action>
  <verify><automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run build && npm run lint</automated></verify>
  <done>Operators can safely reply/reply-all/forward immediately or later, with recoverable drafts and visible durable state.</done>
</task>
</tasks>
<verification>Server tests prove thread/tenant/policy/idempotency rules; UI tests prove composer recovery and command states; manual provider smoke sends one native, one IMAP/SMTP, and one Outlook reply where Phase 19 marks each provider supported.</verification>
<success_criteria>UIX-03 and send/schedule/attachment/snippet portions of UIX-05 are complete without direct provider or policy bypass.</success_criteria>
<output>Create `22-04-SUMMARY.md` with provider matrix, attachment limits/storage, command lifecycle, and smoke evidence.</output>
