---
phase: 22-unified-inbox-operator-experience
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/main.tsx
  - src/components/outreach/OutreachLayout.tsx
  - src/pages/outreach/UnifiedInboxPage.tsx
  - src/components/outreach/inbox/InboxFilterRail.tsx
  - src/components/outreach/inbox/ConversationList.tsx
  - src/components/outreach/inbox/ConversationThread.tsx
  - src/components/outreach/inbox/InboxSyncStatus.tsx
  - src/hooks/useUnifiedInbox.ts
  - src/lib/unified-inbox-api.ts
  - src/lib/unified-inbox-url.ts
  - src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx
autonomous: true
requirements: [UIX-01, UIX-02]
must_haves:
  truths:
    - "One organization-scoped route shows a server-paginated conversation list and complete normalized thread across accounts."
    - "Filters/search are validated URL state and round-trip to the server; organization changes cannot reuse another tenant's cached data."
    - "Desktop, tablet, and mobile follow 22-UI-SPEC instead of shrinking a three-column grid."
    - "Loading, empty, error, and success states are independently recoverable for list and thread."
  artifacts:
    - path: "src/pages/outreach/UnifiedInboxPage.tsx"
      provides: "Responsive workspace state coordinator"
    - path: "src/hooks/useUnifiedInbox.ts"
      provides: "Organization-keyed TanStack Query hooks for Phase 21 APIs"
    - path: "src/lib/unified-inbox-url.ts"
      provides: "Schema-validated URL filter parse/serialize helpers"
  key_links:
    - from: "src/components/outreach/inbox/ConversationList.tsx"
      to: "GET /api/outreach/unified-inbox/conversations"
      via: "cursor query using validated URL filters"
      pattern: "useInboxConversations"
    - from: "src/components/outreach/inbox/ConversationThread.tsx"
      to: "EmailHtmlViewer"
      via: "sanitized/isolated rendering of persisted message bodies"
      pattern: "EmailHtmlViewer"
---

<objective>
Build the read-only Unified Inbox workspace and wire it to Phase 21's real list/detail APIs. The result is useful before actions/composer land: operators can navigate all conversations, filter/search them, inspect attribution, and read complete threads on desktop or mobile.
</objective>

<execution_context>
@.planning/phases/22-unified-inbox-operator-experience/22-UI-SPEC.md
@.planning/phases/21-unified-inbox-foundation/*-SUMMARY.md
@src/components/outreach/OutreachLayout.tsx
@src/components/mail/EmailThread.tsx
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed API hooks and validated URL state</name>
  <read_first>
    - .planning/phases/21-unified-inbox-foundation/*-SUMMARY.md (final endpoint/payload/cursor names)
    - src/lib/api.ts (typed auth fetch and FormData behavior)
    - src/hooks/useOrganization.tsx (organization selection semantics)
    - src/pages/outreach/LeadsPage.tsx (query-key and pagination precedent)
    - src/main.tsx (wouter/useSearch usage)
  </read_first>
  <files>src/lib/unified-inbox-api.ts, src/hooks/useUnifiedInbox.ts, src/lib/unified-inbox-url.ts, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>
    Define API DTOs by importing/reproducing Phase 21's exact public response contract, not Drizzle row types. Implement list/detail/unread/health fetchers and TanStack Query hooks whose keys begin `['outreach-inbox', organizationId, ...]`; disable them with no organization. Preserve previous list page only within the same organization/filter signature. On organization change, selected conversation is cleared/revalidated and no previous tenant detail is rendered.

    Implement Zod-backed parse/serialize for `conversation`, `q`, `unread`, `status`, `campaign`, `account`, repeated labels, reminder, and cursor. Trim/bound `q`, omit defaults, drop invalid enums/UUIDs/cursors, and produce deterministic ordering. Add tests for round-trips, invalid values, repeated labels, empty org, cache-key separation, and cursor reset when any filter changes.
  </action>
  <verify>
    <automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run build</automated>
  </verify>
  <done>Hooks use the Phase 21 API, query keys are tenant-separated, and URL helper tests prove shareable bounded filter state.</done>
</task>

<task type="auto">
  <name>Task 2: Add route/navigation and responsive workspace shell</name>
  <read_first>
    - src/main.tsx (lazy routes and the organization-member outreach gate after Phase 20)
    - src/components/outreach/OutreachLayout.tsx (navigation, active route, mobile sidebar)
    - .planning/phases/22-unified-inbox-operator-experience/22-UI-SPEC.md (navigation and responsive contract)
    - src/components/ui/Skeleton.tsx and src/components/ui/button.tsx (existing primitives)
  </read_first>
  <files>src/main.tsx, src/components/outreach/OutreachLayout.tsx, src/pages/outreach/UnifiedInboxPage.tsx, src/components/outreach/inbox/InboxFilterRail.tsx, src/components/outreach/inbox/InboxSyncStatus.tsx</files>
  <action>
    Lazy-load `/outreach/unified-inbox` inside the corrected organization outreach gate. Add `Inbox` after Dashboard and rename the sender manager label to `Sending accounts` without changing its URL. Build a viewport-height workspace matching 22-UI-SPEC: three regions on desktop, collapsed filter control on tablet, one staged view on mobile. Use CSS media/layout state without JavaScript-only width assumptions where possible.

    Add semantic landmarks/headings, visible focus, active-filter count, organization/sync context, and the specified loading/empty/error placeholders. Do not import `MailboxProvider`, `useMailbox`, mock mail data, or personal `ComposeProvider`.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>Route is reachable by authorized organization users, navigation distinguishes inbox from sending accounts, and shell renders the correct regions/stages at all breakpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render cursor list, attribution, and safe full thread</name>
  <read_first>
    - src/components/mail/EmailThread.tsx (message expansion and action placement reference)
    - src/components/mail/EmailHtmlViewer.tsx (safe HTML renderer contract)
    - .planning/phases/21-unified-inbox-foundation/*-SUMMARY.md (normalized message/attachment/attribution DTOs)
    - src/components/ui/PaginationControls.tsx (existing bounded pagination behavior)
    - src/lib/utils.ts (avatar/format helpers)
  </read_first>
  <files>src/components/outreach/inbox/ConversationList.tsx, src/components/outreach/inbox/ConversationThread.tsx, src/pages/outreach/UnifiedInboxPage.tsx, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>
    Render server cursor pages without client-side organization-wide filtering. Conversation rows include accessible unread, campaign/account badges, snippet, timestamp, attachment/reminder cues, and stable selection. Load-more retains prior pages, exposes retry, and prevents duplicate cursor requests.

    The thread pane renders exact persisted participants/direction/timestamps, attribution links, provider/message state, bodies through `EmailHtmlViewer`, and authorized attachment metadata/download affordances. Latest inbound/unread messages expand initially; quoted content collapse is keyboard-operable. Unknown attribution is explicit. Opening a thread updates URL and mobile stage but does not mark read until the Phase 21 read mutation confirms/Plan 22-03 wires it.

    Test initial/list/thread loading independently, global/filter/search empty states, list and thread failures/retry, malformed HTML isolation, pagination dedupe, unknown attribution, mobile back state, and organization switch clearing selected content.
  </action>
  <verify>
    <automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run build && npm run lint</automated>
  </verify>
  <done>Operators can find and read real Phase 21 conversations across accounts with no mock fallback, tenant cache bleed, unbounded client filtering, or inaccessible mobile squeeze.</done>
</task>

</tasks>

<verification>
- API/mock contract tests cover URL and async states.
- Build/lint pass with no personal-mail provider dependency in unified inbox files.
- Manual responsive smoke at 1440×900, 1024×768, and 390×844 confirms three-region, two-region, and staged layouts.
</verification>

<success_criteria>
- UIX-01 and UIX-02 are usable end-to-end against Phase 21.
- Selection/filter state is shareable, bounded, and tenant-safe.
- Thread bodies and attachments are rendered using safe server-authorized paths.
</success_criteria>

<output>Create `22-02-SUMMARY.md` documenting final API DTOs, URL schema, responsive behavior, and screenshots/UAT notes.</output>
