---
phase: 22-unified-inbox-operator-experience
plan: 03
type: execute
wave: 3
depends_on: [22-01, 22-02]
files_modified:
  - src/lib/unified-inbox-api.ts
  - src/hooks/useUnifiedInbox.ts
  - src/components/outreach/inbox/ConversationActions.tsx
  - src/components/outreach/inbox/ConversationList.tsx
  - src/components/outreach/inbox/ConversationThread.tsx
  - src/components/outreach/inbox/InboxFilterRail.tsx
  - src/pages/outreach/UnifiedInboxPage.tsx
  - src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx
autonomous: true
requirements: [UIX-04, UIX-05]
must_haves:
  truths:
    - "Read/unread, labels, archive, reminders, and bulk mutations update both list and thread caches and rollback on failure."
    - "Suppression/blocklist requires explicit scope confirmation and is executed server-side with organization authorization."
    - "Bulk selection is bounded and copy never implies unseen filter-wide selection."
  artifacts:
    - path: "src/components/outreach/inbox/ConversationActions.tsx"
      provides: "Accessible single/bulk actions and destructive confirmations"
    - path: "src/hooks/useUnifiedInbox.ts"
      provides: "Optimistic mutations with snapshot rollback and targeted invalidation"
  key_links:
    - from: "ConversationActions.tsx"
      to: "22-01 operator APIs"
      via: "typed mutations and ConfirmDialog"
      pattern: "ConfirmDialog"
---

<objective>Add all non-send operator actions to the read workspace while preserving tenant scope, bounded bulk behavior, accessible focus, and cache correctness.</objective>

<execution_context>
@.planning/phases/22-unified-inbox-operator-experience/22-UI-SPEC.md
@.planning/phases/22-unified-inbox-operator-experience/22-01-SUMMARY.md
@.planning/phases/22-unified-inbox-operator-experience/22-02-SUMMARY.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add cache-safe single and bulk mutation hooks</name>
  <read_first>
    - src/hooks/useUnifiedInbox.ts and src/lib/unified-inbox-api.ts (Plan 22-02 contracts)
    - .planning/phases/22-unified-inbox-operator-experience/22-01-SUMMARY.md (final endpoints/bulk limit)
    - src/hooks/useMail.ts (existing optimistic mutation precedent)
  </read_first>
  <files>src/lib/unified-inbox-api.ts, src/hooks/useUnifiedInbox.ts, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>Implement typed mutations for read/unread, archive/restore, label attach/detach/create, reminder CRUD, and bounded bulk actions. Snapshot every affected organization/filter list plus detail, optimistically patch only deterministic fields, rollback on error, reconcile from server response, and invalidate unread aggregates. Idempotency keys protect retries. Test success/rollback, concurrent mutation ordering, 403/409, organization switch during mutation, and partial bulk results.</action>
  <verify><automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run build</automated></verify>
  <done>All mutations reconcile list/detail/unread caches and a failed request restores exact prior state.</done>
</task>
<task type="auto">
  <name>Task 2: Build accessible actions, labels, reminders, and bounded bulk mode</name>
  <read_first>
    - 22-UI-SPEC.md (bulk, focus, async, mobile requirements)
    - src/components/ui/ConfirmDialog.tsx and Dialog.tsx
    - src/components/outreach/inbox/ConversationList.tsx and ConversationThread.tsx
  </read_first>
  <files>src/components/outreach/inbox/ConversationActions.tsx, src/components/outreach/inbox/ConversationList.tsx, src/components/outreach/inbox/ConversationThread.tsx, src/components/outreach/inbox/InboxFilterRail.tsx, src/pages/outreach/UnifiedInboxPage.tsx</files>
  <action>Add single actions to thread/header/rows and an explicit bulk mode for the currently loaded set only. Labels are named controls; reminders accept due time/timezone and show pending/due state. Actions have 44px mobile targets, accessible names, focus restoration, specific progress/error feedback, and no color-only state. Keep the selected thread usable after list mutations; archived items disappear only when current filters require it.</action>
  <verify><automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run lint</automated></verify>
  <done>Keyboard and pointer users can apply every safe action; bulk scope is honest and bounded.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Integrate sender/domain suppression preview and confirmation</name>
  <read_first>
    - src/server/lib/suppression.ts or final suppression service after Phase 19
    - 22-01-SUMMARY.md (suppression preview/confirm route if implemented)
    - src/components/ui/ConfirmDialog.tsx
  </read_first>
  <files>src/lib/unified-inbox-api.ts, src/hooks/useUnifiedInbox.ts, src/components/outreach/inbox/ConversationActions.tsx, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>Call a server preview before confirmation; show exact sender/domain, organization-wide consequence, selected count, and server warnings. Require a second explicit confirm for domain scope. Reject public/free-mail-domain blocks according to server response. On success invalidate affected conversations/leads/suppressions; on failure leave selection and focus intact. Test cancel/no-call, email scope, safe domain scope, rejected public domain, tenant denial, and duplicate suppression idempotency.</action>
  <verify><automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run build && npm run lint</automated></verify>
  <done>No destructive suppression occurs without informed confirmation and all results remain organization-scoped.</done>
</task>
</tasks>

<verification>`npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx`, build, and lint pass; keyboard smoke confirms focus restoration and live announcements; network inspection shows no unbounded/filter-wide bulk request.</verification>
<success_criteria>UIX-04 is complete and reminder/label portions of UIX-05 are durable, accessible, and failure-safe.</success_criteria>
<output>Create `22-03-SUMMARY.md` with action matrix, bulk bound, suppression semantics, and cache strategy.</output>
