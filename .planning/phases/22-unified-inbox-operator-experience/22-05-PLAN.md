---
phase: 22-unified-inbox-operator-experience
plan: 05
type: execute
wave: 5
depends_on: [22-03, 22-04]
files_modified:
  - src/server/routes/outreach/unified-inbox.ts
  - src/server/lib/inbox-events.ts
  - src/server/lib/inbox-events.test.ts
  - src/hooks/useUnifiedInboxEvents.ts
  - src/components/outreach/inbox/InboxSyncStatus.tsx
  - src/components/outreach/OutreachLayout.tsx
  - src/pages/outreach/UnifiedInboxPage.tsx
  - src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx
  - .planning/phases/22-unified-inbox-operator-experience/22-UAT.md
autonomous: true
requirements: [UIX-01, UIX-02, UIX-03, UIX-04, UIX-05, UIX-06]
must_haves:
  truths:
    - "One organization-scoped lightweight stream invalidates aggregates; message bodies are never broadcast."
    - "Disconnect falls back to bounded polling with visible stale/sync state and does not make threads unreadable."
    - "Unread badge, list, and open thread converge without stealing focus or overwriting an active composer."
    - "Responsive/accessibility UAT covers every async state and destructive/send flow."
  artifacts:
    - path: "src/hooks/useUnifiedInboxEvents.ts"
      provides: "Bearer-authenticated fetch/ReadableStream SSE reconnect/fallback and targeted Query invalidation"
    - path: "22-UAT.md"
      provides: "Executable desktop/tablet/mobile/accessibility/provider verification script"
---

<objective>Close the operator loop with low-cost new-reply/unread updates, sync-health degradation, and full-story automated/manual verification.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add secure aggregate event stream and polling fallback contract</name>
  <read_first>
    - Phase 21 ingestion summaries (where conversation changes commit)
    - src/server/index.ts (HTTP middleware/timeouts)
    - src/server/routes/notifications.ts (user-scoped aggregate precedent)
    - deployment notes in AGENTS.md (Traefik/long-running Node)
  </read_first>
  <files>src/server/lib/inbox-events.ts, src/server/routes/outreach/unified-inbox.ts, src/server/lib/inbox-events.test.ts</files>
  <action>Publish only small organization-scoped events after committed ingestion/mutations: conversation ID/version, event kind, unread aggregate, sync-health/cursor timestamp. Extend only `unified-inbox.ts`; apply `requireOutreachRead` and an organization predicate. Add bearer-authenticated SSE with heartbeat, cleanup, bounded subscribers, and no bodies/addresses/credentials. If multi-process fanout is unavailable, document single-container scope and make list/unread polling authoritative. Test cross-tenant denial, redaction, cleanup, heartbeat, and post-commit publication.</action>
  <verify><automated>npm run test -- src/server/lib/inbox-events.test.ts && npm run build</automated></verify>
  <done>Events cannot leak content/tenant data and disconnects cleanly release resources.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reconcile unread/list/thread state without disrupting operators</name>
  <read_first>
    - src/hooks/useUnifiedInbox.ts and all inbox components
    - src/components/outreach/OutreachLayout.tsx
    - 22-UI-SPEC.md async/accessibility sections
  </read_first>
  <files>src/hooks/useUnifiedInboxEvents.ts, src/components/outreach/inbox/InboxSyncStatus.tsx, src/components/outreach/OutreachLayout.tsx, src/pages/outreach/UnifiedInboxPage.tsx, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>Open one stream for the selected organization with authenticated `fetch`, parse `Response.body` through `ReadableStream`, and close/restart it with `AbortController`; never use `EventSource`. Reconnect with capped exponential backoff and fall back to list/unread/health polling—not per-thread polling. Merge by version so stale events cannot regress state. Show unread badge and delayed/last-synced state. New replies never steal focus or overwrite composer state. Tear down on org switch/logout. Test auth header, parsing, abort, reconnect, stale event, org switch, composer preservation, badge, fallback, and recovery.</action>
  <verify><automated>npm run test -- src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx && npm run build && npm run lint</automated></verify>
  <done>UI converges promptly under healthy SSE and remains accurate/usable under disconnect.</done>
</task>
<task type="auto">
  <name>Task 3: Execute full responsive, accessibility, tenant, restart, and provider UAT</name>
  <read_first>
    - 22-UI-SPEC.md (all required states/interactions)
    - 22-01..04 SUMMARY files (final commands/limits/provider support)
    - Phase 18 test harness summary
  </read_first>
  <files>.planning/phases/22-unified-inbox-operator-experience/22-UAT.md, src/pages/outreach/__tests__/UnifiedInboxPage.test.tsx</files>
  <action>Write and execute a deterministic UAT script with two organizations/users and fixtures for unread, attachments, unknown attribution, sync error, scheduled send, reminder, and policy denial. Cover 1440×900, 1024×768, 390×844; keyboard-only list/thread/actions/composer; focus/dialog/live region; light/dark contrast; list/thread loading/empty/error/success; bulk bound; suppression cancel/confirm; process restart before scheduled due; and supported native/SMTP-Outlook reply smoke. Add automated regression tests for any defect found. Record evidence and remaining provider-gated limitations—do not mark unsupported parity as pass.</action>
  <verify><automated>npm run test &amp;&amp; npm run build &amp;&amp; npm run lint</automated></verify>
  <done>All automated gates pass and 22-UAT.md records successful acceptance evidence or explicit unresolved blockers.</done>
</task>
</tasks>
<verification>Full test/build/lint suite plus recorded UAT. Inspect SSE payloads to confirm no message bodies/addresses. Restart worker and verify one scheduled dispatch only.</verification>
<success_criteria>All UIX requirements are demonstrated end-to-end; degraded sync is safe/visible; Unified Inbox is operational across supported accounts.</success_criteria>
<output>Create `22-05-SUMMARY.md` with test totals, UAT evidence, event/fallback behavior, provider matrix, and any blocked acceptance item.</output>
