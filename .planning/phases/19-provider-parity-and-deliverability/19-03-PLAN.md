---
phase: 19-provider-parity-and-deliverability
plan: 03
type: execute
wave: 2
depends_on: [18-04]
files_modified:
  - supabase/migrations/039_outreach_provider_events.sql
  - src/db/schema.ts
  - src/server/lib/outreach-inbound.ts
  - src/server/jobs/processReplies.ts
  - src/server/jobs/processBounces.ts
  - src/server/lib/__tests__/outreach-provider-events-migration.db.test.ts
  - src/server/lib/__tests__/outreach-inbound.test.ts
autonomous: true
requirements: [PROV-04]
must_haves:
  truths:
    - "Inbound provider messages are deduplicated durably before reply/bounce side effects"
    - "DSN classification runs before human-reply classification"
    - "Native and IMAP scans are bounded and resumable without consuming user unread state"
  artifacts:
    - path: supabase/migrations/039_outreach_provider_events.sql
      provides: "Provider event staging and cursor tables"
    - path: src/server/lib/outreach-inbound.ts
      provides: "Provider-neutral message normalization and classification"
  key_links:
    - from: src/server/jobs/processReplies.ts
      to: outreach_provider_events
      via: "consume classification=reply"
    - from: src/server/jobs/processBounces.ts
      to: outreach_provider_events
      via: "consume classification=bounce"
---

<objective>
Create a durable provider-neutral inbound staging layer and eliminate reply/bounce races and unbounded rescans for native and IMAP accounts.
</objective>

<context>
@.planning/phases/19-provider-parity-and-deliverability/19-RESEARCH.md
@src/server/jobs/processReplies.ts
@src/server/jobs/processBounces.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider event and cursor tables with migration 039</name>
  <files>supabase/migrations/039_outreach_provider_events.sql, src/db/schema.ts, src/server/lib/__tests__/outreach-provider-events-migration.db.test.ts</files>
  <read_first>
    - AGENTS.md (manual migration rules)
    - supabase/migrations/029_rls_system_integrations_outreach_settings.sql
    - supabase/migrations/035_outreach_reproducibility_and_reply_index.sql
    - src/db/schema.ts (emailAccounts, outlookMailboxes, mailMessages)
  </read_first>
  <action>
    Hand-write migration 039 for `outreach_provider_cursors` and `outreach_provider_events`. Cursor rows include organization/account/provider, opaque delta cursor, UID validity/high-water or native time/id state, lease token/expiry, last success/error, retryAt, and timestamps; unique organization/account/provider. Event rows include organization/account/provider/provider-message ID, internet Message-ID/In-Reply-To/References, classification (`reply`, `bounce`, `auto_reply`, `other`), sender/recipients, subject, text/html body, selected headers JSON, attachment metadata JSON (provider ID/name/type/size/inline/contentId, no binary), receivedAt, processedAt, processingError, and timestamps. Add unique `(organization_id,email_account_id,provider,provider_message_id)`, tenant/time/classification indexes, FKs, checks, and defense-in-depth RLS policies matching existing organization membership patterns. Mirror schema/relations/types in `src/db/schema.ts`.

    Add `outreach-provider-events-migration.db.test.ts`: use only the Phase 18 protected disposable PostgreSQL URL passed explicitly to the migration helper, apply migration 039 twice, and assert tenant keys, cursor/event constraints, indexes, and schema-mirror names. Never read or fall back to application `DATABASE_URL`.

    Migration starts at 039 and is idempotent where practical. For the manual production runbook only, document `psql $env:DATABASE_URL -v ON_ERROR_STOP=1 -f supabase/migrations/039_outreach_provider_events.sql`; never generate/push with Drizzle.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-provider-events-migration.db.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Events/cursors are tenant-bound, deduplicated, indexed, and represented identically in SQL and TypeScript.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize/classify native and IMAP messages before side effects</name>
  <files>src/server/lib/outreach-inbound.ts, src/server/jobs/processReplies.ts, src/server/jobs/processBounces.ts, src/server/lib/__tests__/outreach-inbound.test.ts</files>
  <read_first>
    - src/server/jobs/processReplies.ts (native/IMAP scans and matchReplyToOutreach)
    - src/server/jobs/processBounces.ts (isBounceEmail, parseBounceMessage, scans)
    - src/server/lib/mail-sync.ts (IMAP cursor patterns)
    - src/server/lib/outreach-inbound.ts
  </read_first>
  <action>
    Extract a provider-neutral normalized message and a single classifier ordered DSN/bounce → auto-reply → human reply → other. Persist with on-conflict idempotency before mutations. Refactor native and IMAP scans into bounded pages (default 200, hard max 500) driven by provider cursor, not `isRead`/`\\Seen`; never change user read flags solely for outreach processing. IMAP records UIDVALIDITY and resets safely on change. Fetch bodies only for candidate reply/bounce/auto messages and retain text/html plus attachment metadata. Reply/bounce consumers query durable unprocessed events by classification and atomically claim/process them. Preserve unmatched mail as `other` for Phase 21. Test DSN-before-reply, duplicate provider ID, cursor restart, UIDVALIDITY reset, hard page cap, and exactly-once counters/suppression.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-inbound.test.ts</automated>
  </verify>
  <done>A native DSN cannot be consumed as a reply; IMAP/native restarts do not rescan the full inbox or duplicate effects.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/server/lib/__tests__/outreach-inbound.test.ts`
- `npm run build`
- `npm run lint`
- `npm run test -- src/server/lib/__tests__/outreach-provider-events-migration.db.test.ts` applies migration 039 twice through the protected disposable-database harness.
</verification>

<success_criteria>
1. Provider cursor, not user unread state, controls ingestion progress.
2. Every inbound provider item has one durable event key and one classification.
3. Bounce/reply side effects are idempotent and bounded.
</success_criteria>
