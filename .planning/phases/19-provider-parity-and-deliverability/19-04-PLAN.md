---
phase: 19-provider-parity-and-deliverability
plan: 04
type: execute
wave: 3
depends_on: [19-02, 19-03]
files_modified:
  - src/server/lib/outlook.ts
  - src/server/lib/outreach-inbound.ts
  - src/server/routes/outreach/email-accounts.ts
  - src/server/jobs/processReplies.ts
  - src/server/jobs/processBounces.ts
  - src/server/jobs/processFollowUps.ts
  - src/server/lib/__tests__/outlook-inbound.test.ts
  - src/server/lib/__tests__/provider-parity.test.ts
autonomous: true
requirements: [PROV-02, PROV-04, PROV-05]
must_haves:
  truths:
    - "A verified Outlook outreach account has proven read, write, and send capability"
    - "Graph delta pages create the same normalized reply/bounce events as IMAP/native"
    - "Reply body context and all follow-up sends use durable shared contracts"
  artifacts:
    - path: src/server/lib/outlook.ts
      provides: "Bounded Graph delta reader with resumable deltaLink"
    - path: src/server/lib/__tests__/provider-parity.test.ts
      provides: "Cross-provider contract suite"
  key_links:
    - from: src/server/lib/outlook.ts
      to: outreach_provider_cursors
      via: "deltaLink after fully persisted page chain"
    - from: outreach_provider_events
      to: campaign_leads.last_reply_text
      via: "idempotent reply side-effect consumer"
---

<objective>
Complete Outlook inbound parity, populate real reply context, and prove that provider and send-origin choices cannot bypass safety/history contracts.
</objective>

<context>
@.planning/phases/19-provider-parity-and-deliverability/19-CONTEXT.md
@src/server/lib/outlook.ts
@src/server/routes/outreach/email-accounts.ts
@src/server/jobs/processReplies.ts
@src/server/jobs/processFollowUps.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded Graph delta ingestion</name>
  <files>src/server/lib/outlook.ts, src/server/lib/outreach-inbound.ts, src/server/lib/__tests__/outlook-inbound.test.ts</files>
  <read_first>
    - src/server/lib/outlook.ts (OAuth scopes, token refresh, Graph fetch conventions)
    - src/server/lib/outreach-inbound.ts
    - src/db/schema.ts (outlookMailboxes, outreachProviderCursors/events)
  </read_first>
  <action>
    Add a Graph inbox delta reader selecting stable Graph ID, internetMessageId, internetMessageHeaders, from/to/cc, subject, text/html body, receivedDateTime, and attachment metadata. Follow `@odata.nextLink` only within a hard per-account event/page/time budget; store `@odata.deltaLink` only after every event in the completed chain is durably upserted. Refresh tokens once on auth expiry; persist retry/error without discarding the prior good cursor. Normalize and classify through `outreach-inbound.ts`. Tests mock initial delta, multi-page continuation, replayed IDs, delta reset/410 recovery with bounded lookback, throttling/Retry-After, token refresh, and partial-page failure.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outlook-inbound.test.ts</automated>
  </verify>
  <done>Graph messages resume from a durable cursor, replay safely, and retain bodies/headers/provider identifiers.</done>
</task>

<task type="auto">
  <name>Task 2: Gate Outlook verification on full outreach capability</name>
  <files>src/server/routes/outreach/email-accounts.ts, src/server/lib/outlook.ts</files>
  <read_first>
    - src/server/routes/outreach/email-accounts.ts (Outlook verify early return)
    - src/server/lib/outlook.ts (OUTLOOK_SCOPES and access token helper)
  </read_first>
  <action>
    Replace the unconditional Outlook verified response with capability validation: mailbox belongs to the account organization/address, granted scopes include Mail.Read/Mail.ReadWrite/Mail.Send, a bounded read/delta call succeeds, and a non-sending MIME validation endpoint/helper confirms send capability without emailing a real recipient. If Graph cannot offer a zero-send capability probe, validate token scopes plus `/me` and keep the account pending until initial inbound sync succeeds; document that exact gate in the response. Store sanitized error codes only. Never activate a send-only Outlook account.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outlook-inbound.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Outlook status `verified` means both outbound and inbound prerequisites are working.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Consume replies/bounces with bodies and enforce cross-provider parity</name>
  <files>src/server/jobs/processReplies.ts, src/server/jobs/processBounces.ts, src/server/jobs/processFollowUps.ts, src/server/lib/__tests__/provider-parity.test.ts</files>
  <read_first>
    - src/server/jobs/processReplies.ts (markAsReplied and scheduleAgenticFollowUpIfEnabled)
    - src/server/jobs/processBounces.ts (markAsBounced)
    - src/server/jobs/processFollowUps.ts
    - src/server/lib/outreach-dispatch.ts
    - src/server/lib/outreach-inbound.ts
  </read_first>
  <action>
    Make reply/bounce consumers provider-agnostic over `outreach_provider_events`. On reply, set `lastReplyMessageId`, `lastReplyAt`, and `lastReplyText` from normalized full text (HTML-to-text fallback, bounded storage), then schedule agentic work only after the event side effect commits. On bounce, preserve existing hard/soft suppression policy with the provider event as idempotency source. Ensure agentic/manual/campaign sends all remain dispatcher records and all provider adapters increment budgets/counters exactly once. Add a table-driven parity suite that runs equivalent SMTP/native/Outlook fixtures and asserts matching headers, Message-ID normalization, reply/bounce state, kill-switch/suppression/daily/warm-up/spacing denials, attempt history, and no duplicate counters on event replay.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/provider-parity.test.ts</automated>
  </verify>
  <done>Provider choice changes transport only; reply context, bounce effects, policy, budget, threading, and durable history remain equivalent.</done>
</task>

</tasks>

<verification>
- `npm run test`
- `npm run build`
- `npm run lint`
- Manual Outlook sandbox gate: connect a test mailbox, verify initial delta cursor, send one MIME campaign fixture, reply, and generate/ingest a DSN; confirm one provider event and one side effect per message.
</verification>

<success_criteria>
1. Outlook is no longer send-only and cannot be verified without inbound capability.
2. Full reply text reaches follow-up context from every provider.
3. DSNs and replies are bounded, resumable, and idempotent across native, IMAP, and Graph.
4. Manual and agentic sends have the same policy, counters, headers, and attempt ledger as campaign sends.
</success_criteria>

