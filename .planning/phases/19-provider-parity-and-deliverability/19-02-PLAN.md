---
phase: 19-provider-parity-and-deliverability
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/server/lib/outreach-provider.ts
  - src/server/lib/outreach-sender.ts
  - src/server/lib/outlook.ts
  - src/server/lib/outreach-dispatch.ts
  - src/server/lib/__tests__/outreach-provider.test.ts
autonomous: true
requirements: [PROV-03, PROV-05]
must_haves:
  truths:
    - "SMTP, native, and Outlook send the same composed MIME headers and body alternatives"
    - "Outlook campaign/manual/agentic replies return a stable Message-ID"
    - "List-Unsubscribe and threading headers are verified before provider dispatch"
  artifacts:
    - path: src/server/lib/outreach-provider.ts
      provides: "Provider adapter interface and normalized accepted/failure result"
  key_links:
    - from: src/server/lib/outreach-dispatch.ts
      to: src/server/lib/outreach-provider.ts
      via: "single provider dispatch boundary"
    - from: src/server/lib/outreach-provider.ts
      to: src/server/lib/outlook.ts
      via: "Graph MIME send"
---

<objective>
Compose outreach MIME once and dispatch it through equivalent SMTP, native, and Outlook adapters, preserving deliverability and threading headers for every origin.
</objective>

<context>
@.planning/phases/19-provider-parity-and-deliverability/19-CONTEXT.md
@src/server/lib/outreach-sender.ts
@src/server/lib/outlook.ts
@src/server/lib/outreach-dispatch.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract compose-once MIME and provider adapters</name>
  <files>src/server/lib/outreach-provider.ts, src/server/lib/outreach-sender.ts, src/server/lib/__tests__/outreach-provider.test.ts</files>
  <read_first>
    - src/server/lib/outreach-sender.ts (sendOutreachEmail and sendThreadedReply)
    - src/server/lib/native-send.ts
    - src/server/lib/outreach-dispatch.ts
    - src/server/lib/smtp-security.ts
  </read_first>
  <action>
    Define a provider adapter returning normalized `accepted`, `messageId`, `providerId`, and failure classification. Build one Nodemailer stream-composed MIME message with stable Message-ID and the appropriate From/To/Cc/Bcc, Reply-To, List-Unsubscribe, List-Unsubscribe-Post, In-Reply-To, References, text/html, and attachments. Campaign sends include unsubscribe headers; manual/thread replies preserve threading and include unsubscribe metadata only when legally/product appropriate. SMTP dispatch uses the shared TLS resolver; native dispatch relays the same raw buffer and stores the sent copy. Remove provider-specific re-composition. Test raw MIME headers and body alternatives for campaign and threaded reply fixtures.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-provider.test.ts</automated>
  </verify>
  <done>All providers receive bytes derived from one compose contract and the stable Message-ID is known before dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Send MIME through Outlook Graph and connect the dispatcher</name>
  <files>src/server/lib/outlook.ts, src/server/lib/outreach-provider.ts, src/server/lib/outreach-dispatch.ts, src/server/lib/__tests__/outreach-provider.test.ts</files>
  <read_first>
    - src/server/lib/outlook.ts (token refresh and sendMessageWithOutlook)
    - src/server/lib/outreach-provider.ts
    - src/server/lib/outreach-dispatch.ts
  </read_first>
  <action>
    Add an Outlook MIME send function using the documented Graph `/me/sendMail` MIME request, retaining token refresh/timeout/status handling. Do not use the JSON message shape for outreach dispatch. Validate the composed buffer contains the required headers before calling Graph and return its precomposed Message-ID after HTTP acceptance. Route dispatcher origins through the adapter so campaign, manual, agentic, and future Unified Inbox sends work with Outlook. Mock `fetch` to verify content type/body encoding, token refresh retry behavior, 429/5xx transient classification, 4xx terminal classification, and that no Graph request occurs when policy denies.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-provider.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Outlook no longer loses unsubscribe/threading headers and uses the same durable attempt/policy path as other providers.</done>
</task>

</tasks>

<verification>
- `npm run test`
- `npm run build`
- `npm run lint`
- Inspect a test-decoded Graph MIME body for `Message-ID`, `List-Unsubscribe`, and `List-Unsubscribe-Post`.
</verification>

<success_criteria>
1. Provider choice does not change outreach compliance headers or attempt history.
2. Threaded Outlook replies preserve In-Reply-To/References.
3. Dispatcher retry policy receives normalized provider outcomes.
</success_criteria>

