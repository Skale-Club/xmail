---
phase: 19-provider-parity-and-deliverability
plan: 01
type: execute
wave: 1
depends_on: [18-04]
files_modified:
  - src/server/lib/smtp-security.ts
  - src/server/lib/outreach-sender.ts
  - src/server/routes/outreach/email-accounts.ts
  - src/pages/outreach/inboxes/NewInboxPage.tsx
  - src/pages/outreach/inboxes/parse-mailbox-csv.ts
  - src/server/lib/__tests__/smtp-security.test.ts
autonomous: true
requirements: [PROV-01]
must_haves:
  truths:
    - "Port 465 uses implicit TLS while port 587 uses STARTTLS semantics"
    - "Verification and delivery derive identical Nodemailer transport options"
    - "UI/CSV presets no longer persist secure=true for standard port 587"
  artifacts:
    - path: src/server/lib/smtp-security.ts
      provides: "One SMTP transport option resolver"
  key_links:
    - from: src/server/routes/outreach/email-accounts.ts
      to: src/server/lib/smtp-security.ts
      via: "verification transporter"
    - from: src/server/lib/outreach-sender.ts
      to: src/server/lib/smtp-security.ts
      via: "send transporter"
---

<objective>
Correct SMTP TLS behavior and remove configuration drift between presets, account verification, and actual delivery.
</objective>

<context>
@.planning/phases/19-provider-parity-and-deliverability/19-RESEARCH.md
@src/server/lib/outreach-sender.ts
@src/server/routes/outreach/email-accounts.ts
@src/pages/outreach/inboxes/NewInboxPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize SMTP security resolution</name>
  <files>src/server/lib/smtp-security.ts, src/server/lib/__tests__/smtp-security.test.ts</files>
  <read_first>
    - src/server/lib/outreach-sender.ts (createSmtpTransporter)
    - src/server/routes/outreach/email-accounts.ts (verify transporter)
    - src/server/lib/mail.ts and src/server/lib/route-matcher.ts (existing TLS conventions)
  </read_first>
  <action>
    Create a pure resolver returning Nodemailer `secure`, `requireTLS`, and safe TLS defaults from port plus the stored setting. Define 465 as implicit TLS; 587 as STARTTLS (`secure:false`, `requireTLS:true`); 25 as opportunistic by default unless an explicit require-TLS mode is available. Preserve explicit nonstandard implicit-TLS configuration without treating `smtpSecure=true` on historical port-587 rows as implicit TLS; normalize that legacy combination to STARTTLS and log a non-secret warning. Test 465/587/25/nonstandard ports and explicit override cases.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/smtp-security.test.ts</automated>
  </verify>
  <done>The resolver produces valid, deterministic Nodemailer options for every supported submission mode.</done>
</task>

<task type="auto">
  <name>Task 2: Use one resolver in send, verify, and account presets</name>
  <files>src/server/lib/outreach-sender.ts, src/server/routes/outreach/email-accounts.ts, src/pages/outreach/inboxes/NewInboxPage.tsx, src/pages/outreach/inboxes/parse-mailbox-csv.ts</files>
  <read_first>
    - src/server/lib/smtp-security.ts
    - src/server/lib/outreach-sender.ts
    - src/server/routes/outreach/email-accounts.ts
    - src/pages/outreach/inboxes/NewInboxPage.tsx
    - src/pages/outreach/inboxes/parse-mailbox-csv.ts
  </read_first>
  <action>
    Replace duplicated `secure: account.smtpSecure ?? true` configuration with the resolver in both transporter creation paths. Update provider presets/form defaults and CSV normalization so port 587 persists STARTTLS-compatible data and port 465 implicit TLS. Validate incompatible port/security input at API write time with a clear 422 code, while allowing legacy records to verify/send through normalization. Do not expose decrypted credentials in errors or logs.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/smtp-security.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>A given account resolves to the same transport options during verification and delivery, and new 587 accounts do not store implicit TLS.</done>
</task>

</tasks>

<verification>
- `npm run test`
- `npm run build`
- `npm run lint`
</verification>

<success_criteria>
1. Standard 587 providers verify and send through STARTTLS.
2. Port 465 remains implicit TLS.
3. Presets, bulk imports, API validation, verification, and send share one documented meaning.
</success_criteria>

