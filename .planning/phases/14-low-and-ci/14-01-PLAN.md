---
phase: 14
plan_id: 14-01
plan_name: cosmetic-cleanup
status: pending
wave: 1
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03, CLN-04]
---

# Plan 14-01: Cosmetic Cleanup

## Goal
Eliminate the 4 LOW-severity cosmetic findings (L1, L2, L3, L4, L5) from the 2026-05-16 audit.

## Scope
- In: `mail-diag` query-param refactor, `nul` file removal, `scripts/_*.ts` rename/delete, `index.html` Vite warning fix, `MAX_WEBHOOK_RESPONSE_BODY` constant.
- Out: anything not in the 5 audit LOW findings.

## Tasks

### Task 1: CLN-01 — mail-diag testEmail
- File: `src/server/routes/system.ts` (`/mail-diag` endpoint, lines ~568-577).
- Action: Replace hardcoded `const testEmail = 'vanildo@skale.club'` with `const testEmail = req.query.testEmail as string | undefined`. Run diagnostic ONLY if `testEmail` is provided; default response excludes the diagnosticTest field.
- Validate: optional Zod `z.string().email().optional()`.

### Task 2: CLN-02 — repo cleanup
- Delete `nul` file at repo root: `git rm nul` (or just `rm nul` since untracked).
- Rename `scripts/_check-db.ts` → `scripts/check-db.ts` (or delete if obsolete — inspect first).
- Rename `scripts/_setup-user.ts` → `scripts/setup-user.ts` (or delete if obsolete).

### Task 3: CLN-03 — index.html script tag
- File: `index.html`.
- Action: Suppress Vite warning. Add `type="text/javascript"` explicitly to `<script src="/app-config.js">` tag, OR use `<script type="module" src="/app-config.js"></script>` if module-friendly, OR add `vite-ignore` directive. Goal: `npm run build` has no "can't be bundled without type=module" warning.

### Task 4: CLN-04 — magic constant
- File: `src/server/lib/tracking.ts`.
- Action: Add `const MAX_WEBHOOK_RESPONSE_BODY = 5000;` at top. Replace `responseBody.substring(0, 5000)` (and any other 5000 magic numbers in this file) with the constant. Optionally export for testability.

## Verification
- `grep "vanildo@skale.club" src/` → no matches.
- `git ls-files | grep -E "^(nul|scripts/_)"` → empty.
- `npm run build 2>&1 | grep "can't be bundled"` → no output.
- `grep "MAX_WEBHOOK_RESPONSE_BODY" src/server/lib/tracking.ts` → 2+ matches (const + usage).
- `npm run lint` exits 0. `npx tsc --noEmit -p tsconfig.server.json` exits 0.

## Files to touch
- `src/server/routes/system.ts`
- `src/server/lib/tracking.ts`
- `index.html`
- `scripts/_check-db.ts` → renamed/deleted
- `scripts/_setup-user.ts` → renamed/deleted
- `nul` → removed

## Dependencies
None.

## Risks
- `scripts/_*.ts` may be referenced from package.json or other scripts — grep before deleting.
- Renaming script files breaks any script-of-script that imports them — sweep references.
