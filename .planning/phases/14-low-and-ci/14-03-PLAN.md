---
phase: 14
plan_id: 14-03
plan_name: runbook-and-error-sink-decision
status: pending
wave: 1
autonomous: true
requirements: [CI-03, CI-04]
---

# Plan 14-03: Runbook + Error Sink Decision

## Goal
Document operational behavior of `/health/ready` and record the error-log-sink decision.

## Scope
- In: `docs/runbook.md` (or README section) for `/health/ready`; CI-04 decision in PROJECT.md.
- Out: actual Sentry/Datadog wiring.

## Tasks

### Task 1: CI-03 — runbook
- Create `docs/runbook.md` (or add a section to README.md if preferred).
- Document:
  - `/health/ready` endpoint behavior: 200 OK when healthy, 503 with `database.ok=false` on DB outage.
  - Recommended probe interval (e.g., 10s) for K8s/uptime monitors.
  - Failure modes and triage notes.
  - Optional: same treatment for `/health/live` if exists.

### Task 2: CI-04 — error sink decision
- Add to `.planning/PROJECT.md` "Key Decisions" table:
  - Decision: Defer error log sink (Sentry/Datadog) to v1.3.
  - Rationale: Requires budget + ops infra decision; `/health/ready` + CI lint+tsc gates provide first defense.
  - Outcome: v1.3 candidate (track in `Future Requirements`).
- Also note in REQUIREMENTS.md: CI-04 status [x] with note "deferred to v1.3".

## Verification
- `docs/runbook.md` exists with `/health/ready` section.
- `grep "error.*sink\|Sentry\|Datadog" .planning/PROJECT.md` → at least 1 match in Key Decisions table.

## Files to touch
- `docs/runbook.md` (new)
- `.planning/PROJECT.md`

## Dependencies
None.

## Risks
- README.md vs docs/runbook.md placement — pick one, document the choice.
