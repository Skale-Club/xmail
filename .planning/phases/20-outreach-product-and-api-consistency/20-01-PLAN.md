---
phase: 20-outreach-product-and-api-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - supabase/migrations/040_outreach_product_consistency.sql
  - src/server/lib/outreach-sequences.ts
  - src/server/routes/outreach/campaigns.ts
  - src/pages/outreach/SequencesPage.tsx
  - src/pages/outreach/sequences/NewSequencePage.tsx
  - src/pages/outreach/campaigns/tabs/SequenceTab.tsx
  - src/server/lib/__tests__/outreach-sequences.db.test.ts
  - src/server/lib/__tests__/outreach-sequences-migration.db.test.ts
  - src/server/routes/outreach/__tests__/campaign-sequences.db.test.ts
autonomous: true
requirements:
  - CONS-01
  - CONS-07
must_haves:
  truths:
    - "Every campaign resolves exactly one canonical sequence without relying on first-row ordering"
    - "Saving a sequence is a transactional replace/upsert and cannot append duplicate positions or leave a partial edit"
    - "Step order/content invariants match in Zod, TypeScript schema declarations, and the running PostgreSQL schema"
    - "Historical outreach email and campaign-lead references are preserved or the edit is rejected with an actionable conflict"
  artifacts:
    - path: "src/server/lib/outreach-sequences.ts"
      provides: "Canonical sequence resolution, validation, and transactional replacement service"
      exports: ["sequencePayloadSchema", "getCanonicalSequence", "replaceCanonicalSequence"]
    - path: "supabase/migrations/040_outreach_product_consistency.sql"
      provides: "One-sequence-per-campaign and step check constraints"
      contains: "UNIQUE"
    - path: "src/server/routes/outreach/campaigns.ts"
      provides: "Canonical sequence GET/PUT API and transactional campaign creation"
  key_links:
    - from: "src/server/routes/outreach/campaigns.ts"
      to: "src/server/lib/outreach-sequences.ts"
      via: "route delegates all sequence writes to one service"
      pattern: "replaceCanonicalSequence"
    - from: "src/db/schema.ts"
      to: "supabase/migrations/040_outreach_product_consistency.sql"
      via: "matching uniqueness and check constraints"
      pattern: "sequence.*campaign.*unique"
---

<objective>
Collapse the ambiguous many-sequences behavior into one safe campaign sequence contract and reconcile every validation layer.

Purpose: Enrollment, processing, and three UI surfaces currently assume `sequences[0]` while the API can create several rows and append steps. This plan makes that assumption explicit, transactional, and database-enforced.

Output: Hand-written migration, canonical sequence service, one GET/PUT route contract, migrated UI callers, and regression tests.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/20-outreach-product-and-api-consistency/20-CONTEXT.md
@.planning/phases/20-outreach-product-and-api-consistency/20-RESEARCH.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify canonical sequence and step invariants in failing tests</name>
  <files>src/server/lib/__tests__/outreach-sequences.db.test.ts, src/server/routes/outreach/__tests__/campaign-sequences.db.test.ts</files>
  <read_first>
    - src/server/routes/outreach/campaigns.ts (campaign creation, sequence CRUD, enrollment lookup)
    - src/db/schema.ts (sequences, sequenceSteps, campaignLeads, outreachEmails foreign keys)
    - src/pages/outreach/SequencesPage.tsx (current append save flow)
    - src/pages/outreach/sequences/NewSequencePage.tsx (oldest-sequence workaround)
    - test setup introduced in Phase 18
  </read_first>
  <action>
    Add Vitest coverage before implementation. Prove: campaign+sequence creation rolls back together; second sequence creation is rejected/returns the canonical resource; step order starts at 1 and is contiguous; email steps require subject and text or html; non-email steps never accept sendable content; duplicate replacement is idempotent; a failed step in a replacement rolls back the complete change; cross-tenant and viewer writes are denied; active campaign edits are rejected; and replacing a step referenced by `campaign_leads`/`outreach_emails` either preserves its id or returns 409 without deleting history.

    Use the Phase 18 disposable test database/transaction fixture. Do not mock authorization or the database for route isolation cases.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-sequences.db.test.ts src/server/routes/outreach/__tests__/campaign-sequences.db.test.ts</automated>
  </verify>
  <done>Tests compile and fail only because the canonical service/PUT contract and SQL constraints do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile legacy rows and enforce the canonical model in hand-written SQL</name>
  <files>supabase/migrations/040_outreach_product_consistency.sql, src/db/schema.ts, src/server/lib/__tests__/outreach-sequences-migration.db.test.ts</files>
  <read_first>
    - supabase/migrations/037_reconcile_schema_only_unique_indexes.sql (latest migration style)
    - supabase/migrations/020_consolidate_rls.sql (constraint/RLS precedent)
    - src/db/schema.ts (current Drizzle declarations)
    - 20-RESEARCH.md migration strategy
  </read_first>
  <action>
    First list `supabase/migrations/` and revalidate the next free integer. `040` is reserved by planning after Phase 18/19, not guaranteed; rename this plan's migration references if another migration landed.

    Write one hand-authored, transaction-safe SQL migration. Add diagnostic preflight blocks for duplicate sequences and conflicting step positions. Select the oldest sequence per campaign as canonical. Reparent only non-conflicting steps; abort with campaign/sequence ids for ambiguous conflicts instead of silently dropping content. Remove verified-empty surplus sequences, then add a unique constraint/index on `sequences(campaign_id)` and SQL checks matching the final step schema (`step_order >= 1`, `delay_hours >= 0`, percentage bounds, and explicit type/content validity). Make reruns safe where practical.

    Add `outreach-sequences-migration.db.test.ts`. It must obtain the guarded disposable PostgreSQL URL from the Phase 18 harness and pass that URL explicitly to the harness migration helper; the helper/test must never read or fall back to application `DATABASE_URL`. Apply migration 040 twice through that helper and assert the constraints/indexes. For a manual production rollout only, document this PowerShell runbook command: `psql $env:DATABASE_URL -v ON_ERROR_STOP=1 -f supabase/migrations/040_outreach_product_consistency.sql`.

    Mirror every final constraint/index in `src/db/schema.ts`. The protected-harness migration test is the automated drift assertion for this change; do not invoke `npm run db:audit`, because that script reads application `DATABASE_URL`. Do not use Drizzle to generate or apply SQL. Do not run `db:push`.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-sequences-migration.db.test.ts</automated>
  </verify>
  <done>Migration applies twice on the protected harness, no campaign has more than one sequence, invalid steps are rejected by PostgreSQL, and explicit schema assertions show no new drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the canonical service/API and migrate all UI callers</name>
  <files>src/server/lib/outreach-sequences.ts, src/server/routes/outreach/campaigns.ts, src/pages/outreach/SequencesPage.tsx, src/pages/outreach/sequences/NewSequencePage.tsx, src/pages/outreach/campaigns/tabs/SequenceTab.tsx</files>
  <read_first>
    - database tests from Tasks 1-2
    - src/server/lib/access.ts and current outreach membership helpers
    - src/server/jobs/processOutreachSequences.ts (sequence lookup/step transition consumer)
    - all three listed React sequence surfaces
  </read_first>
  <action>
    Create a shared Zod discriminated-union payload and transactional service. `getCanonicalSequence(campaignId, organizationId)` must scope through the campaign organization. `replaceCanonicalSequence` must lock the campaign/sequence, accept only draft/paused campaigns, validate a complete ordered array, preserve ids for unchanged existing positions, and refuse destructive changes to steps with send history/current lead references unless a safe remap is explicit.

    Make campaign creation and default sequence insertion one transaction. Replace the many-sequence write surface with `PUT /api/outreach/campaigns/:campaignId/sequence` and a singular GET response. Keep old endpoints only as temporary compatibility adapters that cannot create a second row and return deprecation metadata. Update enrollment/activation to call the canonical resolver, never `findFirst`/`sequences[0]`.

    Update all sequence React screens to load/save the singular resource in one mutation, invalidate campaign/sequence queries, and show validation/conflict details. Remove client append loops and the "reuse first sequence" workaround.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-sequences.db.test.ts src/server/routes/outreach/__tests__/campaign-sequences.db.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>All sequence consumers use the singular contract, tests pass, and build/lint succeed without an ordering convention.</done>
</task>

</tasks>

<verification>
- Query `GROUP BY campaign_id HAVING count(*) &gt; 1` returns zero.
- Invalid `step_order=0`, negative delay, duplicate position, and body-less email fail at both API and SQL boundaries.
- Repeating the same PUT returns the same ordered step identities and does not increase row count.
- Simulated mid-transaction failure leaves the prior complete sequence intact.
- `rg "sequences\[0\]|findFirst\(.*sequences" src/pages src/server/routes/outreach src/server/jobs` finds no production canonical-sequence selection.
- `npm run test -- src/server/lib/__tests__/outreach-sequences.db.test.ts src/server/lib/__tests__/outreach-sequences-migration.db.test.ts src/server/routes/outreach/__tests__/campaign-sequences.db.test.ts`, `npm run build`, and `npm run lint` pass; all migration assertions execute against the protected harness URL.
</verification>

<success_criteria>
1. One campaign maps to exactly one database-enforced sequence.
2. Complete sequence edits are atomic, validated, and history-safe.
3. Enrollment, activation, processor, API, and UI resolve the same canonical resource.
4. SQL, Drizzle, Zod, and tests agree on every step invariant.
</success_criteria>

<output>
After completion, create `.planning/phases/20-outreach-product-and-api-consistency/20-01-SUMMARY.md` with the actual migration number, legacy-row reconciliation counts, final API payload, and history-preservation policy.
</output>
