---
phase: 20-outreach-product-and-api-consistency
plan: 03
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
files_modified:
  - src/server/lib/outreach-access.ts
  - src/server/index.ts
  - src/server/routes/outreach/campaigns.ts
  - src/server/routes/outreach/leads.ts
  - src/server/routes/outreach/email-accounts.ts
  - src/server/routes/outreach/settings.ts
  - src/main.tsx
  - src/components/outreach/OutreachLayout.tsx
  - src/server/lib/__tests__/outreach-access.db.test.ts
  - src/server/__tests__/service-auth.db.test.ts
autonomous: true
requirements:
  - CONS-02
  - CONS-06
must_haves:
  truths:
    - "Authorized organization members can enter outreach without platform-admin privileges"
    - "Viewer/read and member/admin write behavior is consistent across every outreach route"
    - "A valid service key maps to a server-configured principal and organization; client identity/scope headers cannot override it"
    - "All tenant-scoped outreach routes import one canonical JS-side access helper"
  artifacts:
    - path: "src/server/lib/outreach-access.ts"
      provides: "Canonical outreach organization read/write authorization helpers"
      exports: ["checkOutreachAccess", "requireOutreachRead", "requireOutreachWrite"]
    - path: "src/server/index.ts"
      provides: "Service-key middleware with bound principal and organization"
    - path: "src/main.tsx"
      provides: "OutreachCheck route guard separate from platform AdminCheck"
  key_links:
    - from: "src/server/routes/outreach/*"
      to: "src/server/lib/outreach-access.ts"
      via: "every tenant route checks read/write scope before data access"
      pattern: "requireOutreach"
    - from: "src/server/index.ts"
      to: "outreach route organizationId"
      via: "bound service scope overwrites identity and rejects scope mismatch"
      pattern: "XMAIL_SERVICE_ORGANIZATION_ID"
---

<objective>
Align outreach UI/API authorization and close the service-key identity trust boundary.

Purpose: The backend permits organization roles while the frontend requires platform admin, and machine auth currently trusts caller-supplied identity.

Output: Canonical outreach access module, route migration, bound service principal middleware, dedicated React guard, and isolation tests.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/20-outreach-product-and-api-consistency/20-CONTEXT.md
@.planning/phases/20-outreach-product-and-api-consistency/20-RESEARCH.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add role matrix and service-scope security tests</name>
  <files>src/server/lib/__tests__/outreach-access.db.test.ts, src/server/__tests__/service-auth.db.test.ts</files>
  <read_first>
    - src/server/index.ts API auth middleware
    - src/server/lib/access.ts
    - every file under src/server/routes/outreach
    - src/main.tsx AdminCheck and outreach routes
    - organization context/auth hooks used by OutreachLayout
  </read_first>
  <action>
    Add database-backed tests for platform admin, organization admin/member/viewer, non-member, and cross-tenant ids on representative GET and mutation routes. Add service-auth tests proving: unset config fails closed; wrong key is 401; correct key ignores/overwrites forged `x-user-id`; missing bound principal/org fails startup or request closed; query/body/resource organization mismatches are 403; and logs/errors never echo the key.

    Include a route inventory assertion or static test that every registered outreach router uses the canonical access import rather than a local `checkOrgMembership` copy.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-access.db.test.ts src/server/__tests__/service-auth.db.test.ts</automated>
  </verify>
  <done>Tests fail on AdminCheck mismatch, duplicated access helpers, and forged service identity before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize outreach authorization and bind service identity server-side</name>
  <files>src/server/lib/outreach-access.ts, src/server/index.ts, src/server/routes/outreach/campaigns.ts, src/server/routes/outreach/leads.ts, src/server/routes/outreach/email-accounts.ts, src/server/routes/outreach/settings.ts</files>
  <read_first>
    - tests from Task 1
    - Phase 18/19 changed outreach routes and shared delivery-policy APIs
    - all other outreach route files not listed explicitly (unsubscribe, send-message, index)
  </read_first>
  <action>
    Implement the canonical public outreach helper in `src/server/lib/outreach-access.ts`, returning organization + membership and explicit read/write assertions. Platform admins bypass membership; viewer is read-only; member/admin can write. `src/server/lib/access.ts` may be read and reused internally as an existing primitive, but must not re-export outreach helpers. Every outreach route imports the public contract directly from `outreach-access.ts`, including files added/changed by Phases 18–19, and lookup-by-resource routes derive organization before accessing related content.

    In `src/server/index.ts`, require `XMAIL_SERVICE_KEY`, `XMAIL_SERVICE_USER_ID`, and `XMAIL_SERVICE_ORGANIZATION_ID` as one valid set. After timing-safe verification, overwrite `x-user-id`, set an internal service-principal marker and bound organization header, and reject organization scope mismatches in query/body. Resource-id routes must still validate their derived organization through the shared helper. Do not accept identity from any client header and do not log secrets.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-access.db.test.ts src/server/__tests__/service-auth.db.test.ts</automated>
  </verify>
  <done>Role tests pass, forged identities/scopes fail, and no outreach router owns a divergent membership helper.</done>
</task>

<task type="auto">
  <name>Task 3: Replace platform-admin routing with organization-aware outreach access</name>
  <files>src/main.tsx, src/components/outreach/OutreachLayout.tsx</files>
  <read_first>
    - src/main.tsx AuthCheck/AdminCheck/MailCheck patterns
    - src/hooks/useAuth.tsx
    - organization provider/context implementation
    - backend role contract from Task 2
  </read_first>
  <action>
    Add `OutreachCheck`: require an authenticated user, mount `OrganizationProvider`, wait for organization/membership resolution, permit platform admins and organization admin/member/viewer, and redirect users with no accessible organization to a safe page with a clear message. Do not grant write capability in the frontend; pages continue to handle API 403 and may hide mutations for viewers based on the same membership signal.

    Wrap all `/outreach/*` routes with the new guard and leave `/admin/*` under `AdminCheck`. Update OutreachLayout navigation/actions for viewer read-only behavior without hiding data pages. Avoid navigation side effects during React render; use an effect or declarative redirect.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run test -- src/server/lib/__tests__/outreach-access.db.test.ts src/server/__tests__/service-auth.db.test.ts</automated>
  </verify>
  <done>Non-platform organization members reach outreach, viewers see read-only screens, admin routes remain platform-only, and backend tests remain authoritative.</done>
</task>

</tasks>

<verification>
- Org member without platform admin can load outreach API/UI; non-member cannot.
- Viewer GET succeeds and mutation returns 403.
- Forged `x-user-id` with a valid service key acts only as configured principal; mismatched organization fails.
- `rg "function checkOrgMembership" src/server/routes/outreach` returns zero.
- `/admin/*` still uses `AdminCheck`; `/outreach/*` uses `OutreachCheck`.
- Security tests, full outreach suite, build, and lint pass.
</verification>

<success_criteria>
1. UI access matches backend organization roles.
2. One JS-side helper enforces outreach tenant access everywhere.
3. Service authentication chooses identity and tenant scope server-side.
4. Platform administration remains isolated.
</success_criteria>

<output>
After completion, create `.planning/phases/20-outreach-product-and-api-consistency/20-03-SUMMARY.md` with the role matrix, required service environment variables, migrated route inventory, and security-test evidence.
</output>
