---
phase: 20-outreach-product-and-api-consistency
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/server/lib/outreach-settings.ts
  - src/server/lib/outreach-campaign-metrics.ts
  - src/server/routes/outreach/settings.ts
  - src/server/routes/outreach/campaigns.ts
  - src/server/routes/outreach/email-accounts.ts
  - src/server/routes/outreach/leads.ts
  - src/server/jobs/processReplies.ts
  - src/server/jobs/processBounces.ts
  - src/server/routes/outreach/unsubscribe.ts
  - src/pages/outreach/LeadsPage.tsx
  - src/pages/outreach/campaigns/tabs/LeadsTab.tsx
  - src/pages/outreach/SettingsPage.tsx
  - src/pages/outreach/AnalyticsPage.tsx
  - src/pages/outreach/OutreachDashboard.tsx
  - src/pages/outreach/campaigns/tabs/OverviewTab.tsx
  - src/pages/outreach/campaigns/tabs/StatsTab.tsx
  - src/server/lib/__tests__/outreach-settings.db.test.ts
  - src/server/lib/__tests__/outreach-campaign-metrics.db.test.ts
  - src/server/jobs/__tests__/outreach-notification-policy.db.test.ts
  - src/server/routes/outreach/__tests__/campaign-metrics.db.test.ts
  - src/server/routes/outreach/__tests__/leads-query.db.test.ts
autonomous: true
requirements:
  - CONS-03
  - CONS-04
  - CONS-05
must_haves:
  truths:
    - "New campaigns/accounts consume organization defaults only for omitted fields"
    - "Every visible notification setting has a real event-policy consumer or is removed end-to-end"
    - "Lead search/filter/sort/pagination is server-side, tenant-scoped, stable, and bounded to 100 rows"
    - "Campaign responses expose explicit eligible/contacted/sent denominators and consistent rates across screens"
  artifacts:
    - path: "src/server/lib/outreach-settings.ts"
      provides: "Single resolved organization settings contract"
      exports: ["OUTREACH_SETTINGS_DEFAULTS", "resolveOutreachSettings"]
    - path: "src/server/routes/outreach/leads.ts"
      provides: "Validated list/search/filter/sort contract"
    - path: "src/server/lib/outreach-campaign-metrics.ts"
      provides: "Named cohort metric DTO reused by list/detail/analytics"
  key_links:
    - from: "src/server/routes/outreach/campaigns.ts"
      to: "src/server/lib/outreach-settings.ts"
      via: "omitted create fields inherit resolved defaults"
      pattern: "resolveOutreachSettings"
    - from: "src/pages/outreach/LeadsPage.tsx"
      to: "src/server/routes/outreach/leads.ts"
      via: "matching search/filter/sort/page query schema"
      pattern: "search"
---

<objective>
Make settings, leads, and analytics tell the same truth at the UI and API boundaries.

Purpose: Remove decorative configuration, client-side unbounded workarounds, and denominator ambiguity.

Output: Shared settings resolver, bounded lead query contract, explicit campaign metric cohorts, updated clients, and tests.
</objective>

<execution_context>
@C:/Users/Vanildo/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/20-outreach-product-and-api-consistency/20-CONTEXT.md
@.planning/phases/20-outreach-product-and-api-consistency/20-RESEARCH.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock settings, list, and cohort behavior in tests</name>
  <files>src/server/lib/__tests__/outreach-settings.db.test.ts, src/server/lib/__tests__/outreach-campaign-metrics.db.test.ts, src/server/jobs/__tests__/outreach-notification-policy.db.test.ts, src/server/routes/outreach/__tests__/campaign-metrics.db.test.ts, src/server/routes/outreach/__tests__/leads-query.db.test.ts</files>
  <read_first>
    - src/server/routes/outreach/settings.ts
    - src/server/routes/outreach/campaigns.ts create schemas
    - src/server/routes/outreach/email-accounts.ts create/import flows
    - src/server/routes/outreach/leads.ts list route
    - src/server/lib/outreach-campaign-metrics.ts
    - src/server/jobs/processReplies.ts and processBounces.ts
    - src/server/routes/outreach/unsubscribe.ts
    - src/pages/outreach/LeadsPage.tsx and campaign LeadsTab.tsx
    - src/pages/outreach/AnalyticsPage.tsx, OutreachDashboard.tsx, OverviewTab.tsx, and StatsTab.tsx
  </read_first>
  <action>
    Write failing Vitest cases for: no-row defaults; stored overrides; explicit create values beating defaults; omitted campaign/account fields inheriting settings; notification flags gating existing reply/bounce/unsubscribe event delivery; trimmed/escaped case-insensitive multi-field lead search; organization/list/status filters; invalid sort/status/cursor/page/limit; hard limit 100; stable tie-break order; cross-tenant exclusion; and metric cohorts where pre-send suppressed/unsubscribed leads do not count as contacted/sent.

    Assert notification-policy tests at the actual reply, bounce, and unsubscribe event call sites: enabled emits exactly once, disabled emits zero, replay does not duplicate, and one organization's setting cannot gate another's event.

    Assert the same metric DTO from campaign list, dashboard stats, campaign detail stats, and analytics routes. Seed leads covering `new`, pre-send `unsubscribed`, `contacted`, `replied`, and `bounced`, with and without an actually sent outreach email.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-settings.db.test.ts src/server/lib/__tests__/outreach-campaign-metrics.db.test.ts src/server/jobs/__tests__/outreach-notification-policy.db.test.ts src/server/routes/outreach/__tests__/campaign-metrics.db.test.ts src/server/routes/outreach/__tests__/leads-query.db.test.ts</automated>
  </verify>
  <done>Tests express all three public contracts and fail on the known storage-only settings, ignored search, and cohort mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize settings and consume defaults/event policy</name>
  <files>src/server/lib/outreach-settings.ts, src/server/routes/outreach/settings.ts, src/server/routes/outreach/campaigns.ts, src/server/routes/outreach/email-accounts.ts, src/server/jobs/processReplies.ts, src/server/jobs/processBounces.ts, src/server/routes/outreach/unsubscribe.ts, src/pages/outreach/SettingsPage.tsx, src/server/jobs/__tests__/outreach-notification-policy.db.test.ts</files>
  <read_first>
    - settings tests from Task 1
    - src/server/jobs/processReplies.ts and processBounces.ts event/notification hooks
    - src/server/routes/outreach/unsubscribe.ts event path
    - Phase 19 shared send-policy/provider work
  </read_first>
  <action>
    Move constants and DB resolution to `src/server/lib/outreach-settings.ts`. Return a typed flat internal object and keep nesting only as an API presentation concern. Change campaign/account create schemas so defaultable fields remain optional until after parsing, merge `explicit ?? resolved`, and preserve existing records when settings change.

    Connect reply/bounce/unsubscribe notification emission to the relevant resolved toggle without duplicating ingestion side effects. If no real notification transport/event exists for a displayed setting, remove that setting from Zod, API formatting, and React form instead of claiming it works. Document which settings apply only at resource creation.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-settings.db.test.ts src/server/jobs/__tests__/outreach-notification-policy.db.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>There is one default source; omitted fields inherit it; explicit fields win; and every displayed notification setting is consumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement bounded lead queries and explicit campaign cohorts</name>
  <files>src/server/routes/outreach/leads.ts, src/server/lib/outreach-campaign-metrics.ts, src/server/routes/outreach/campaigns.ts, src/pages/outreach/LeadsPage.tsx, src/pages/outreach/campaigns/tabs/LeadsTab.tsx, src/pages/outreach/AnalyticsPage.tsx, src/pages/outreach/OutreachDashboard.tsx, src/pages/outreach/campaigns/tabs/OverviewTab.tsx, src/pages/outreach/campaigns/tabs/StatsTab.tsx, src/server/routes/outreach/__tests__/campaign-metrics.db.test.ts</files>
  <read_first>
    - lead/metrics tests from Task 1
    - src/server/lib/pagination.ts
    - current campaign list/stats/analytics aggregations
    - current client query keys and 1,000-row enrollment workarounds
  </read_first>
  <action>
    Add one Zod query schema for lead list/search with `page >= 1`, `1 <= limit <= 100`, enumerated status/sort/order, optional lead-list id, and trimmed max-length search. Build tenant-first SQL conditions, escape ILIKE wildcards, search the agreed fields, order by requested field plus `id`, and return consistent pagination metadata. Replace client-side 1,000-row scans/search-by-create workarounds with the server contract and include all query state in React Query keys.

    Refactor campaign metrics into an explicit DTO containing counts and denominators. Compute eligible, contacted, sent, opened, clicked, replied, bounced, and pre-send excluded cohorts with organization/campaign scoping. Make campaign list, OutreachDashboard, AnalyticsPage, OverviewTab, StatsTab, their route handlers, and their React types/renderers consume the same DTO and denominator definitions. Remove local rate recomputation from all five UI surfaces.
  </action>
  <verify>
    <automated>npm run test -- src/server/lib/__tests__/outreach-campaign-metrics.db.test.ts src/server/routes/outreach/__tests__/campaign-metrics.db.test.ts src/server/routes/outreach/__tests__/leads-query.db.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>Search/filter/pagination is bounded and matching end-to-end; every campaign surface reports the same named cohorts and rates.</done>
</task>

</tasks>

<verification>
- A request with `limit=101`, invalid sort, or malformed filter returns 400; no route silently falls back.
- `%`, `_`, and backslash in search are treated as literal input.
- Two tenants with the same lead email never see each other's result/count.
- Creating resources with omitted and explicit fields demonstrates the precedence contract.
- List/stats/analytics return identical cohort values for the same fixture.
- Targeted tests, `npm run build`, and `npm run lint` pass.
</verification>

<success_criteria>
1. Settings are operational defaults/policies rather than stored decoration.
2. Lead queries are bounded, searchable, stable, and tenant-safe.
3. Campaign metrics expose honest named denominators everywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/20-outreach-product-and-api-consistency/20-02-SUMMARY.md` documenting applied defaults, notification policy, lead query parameters, and metric denominator formulas.
</output>
